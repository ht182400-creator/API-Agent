# ==============================================================================
# 仓库根 .gitignore
# ------------------------------------------------------------------------------
# ⚠️ 本仓库为**公开（public）**仓库，以下内容一律不得提交。
#    子目录另有 api-platform/.gitignore 与 api-platform/web/.gitignore。
#    新增规则时请同步更新 api-platform/docs/OPTIMIZATION_BACKLOG.md 的记录。
# ==============================================================================

# ── 密钥与凭据（公开仓库严禁提交）─────────────────────────────────────────────
*.pem
*.key
*.p12
*.pfx
*.jks
.env
.env.*
!.env.example
keys/
**/keys/
# ⚠️ 例外：前端构建变量文件（.env.development / .env.production）**只含公开的 VITE_* 变量**
#    （VITE_ 前缀会被编译进浏览器产物，本就是公开信息），属"必须随仓库分发的构建输入"。
#    ⚠️ 注意：其例外规则必须写在 `api-platform/.gitignore` —— **子目录 .gitignore 优先级高于根目录**，
#       写在根目录会被 api-platform/.gitignore 的 `.env.*` 覆盖（实测无效）。
#    ⚠️ 后续**严禁**往这两个文件写入任何真正的密钥（非 VITE_ 前缀的敏感值一律放后端 .env）。

# ── 数据库文件 ────────────────────────────────────────────────────────────────
*.db
*.sqlite
*.sqlite3
*.dump
*.sql.gz

# ── AI 工作记忆（内部记录，不公开）────────────────────────────────────────────
# 说明：.codebuddy/ 存放跨会话记忆与每日工作日志，属内部过程记录，
#       且可能含环境细节，**不提交到公开仓库**。
.codebuddy/

# ── 依赖与构建产物 ────────────────────────────────────────────────────────────
node_modules/
dist/
build/
out/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
coverage/
htmlcov/
.venv/
venv/
# ⚠️ 例外：electron/build/ 是**打包源文件**（NSIS 安装脚本 / 图标），不是构建产物。
#    注意：父目录被 `build/` 整目录忽略时，不能直接 `!某文件` —— 必须先重新包含目录。
!electron/build/
!electron/build/**

# ── 运行时 / 日志 / 临时文件 ──────────────────────────────────────────────────
*.log
logs/
*.tmp
*.bak
*~
Thumbs.db
Desktop.ini
.DS_Store

# ── AI 会话导出记录（体积极大：单文件可达 70MB+，属过程产物）──────────────────
# 例：`仔细阅读 通用API服务平台文档 目录下的项目文档，开始源代_20260915200439.json`
仔细阅读*.json

# ── 测试产物 ──────────────────────────────────────────────────────────────────
test-results/
playwright-report/
playwright/.cache/

# ── 编辑器 / IDE ──────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp

# CodeBuddy 会话导出（单个 18MB，勿入库；含完整对话，也可能含敏感信息）
修复*.json
*_2026*.json
