# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pylint
.pylint
pylintrc.txt

# OS
.DS_Store
Thumbs.db

# Database
*.db
*.sqlite
*.sqlite3

# Logs
*.log
logs/

# ⚠️ 例外：测试留痕日志必须入库。
#    docs/test-logs/YYYY-MM-DD/NN-<轮次>-<描述>.log 是**交付证据**（案例 / 命令与参数 /
#    原始输出 / 失败原文），不是运行期日志 —— 用户明确要求"燕过留痕"。
#    若被 `*.log` 一并忽略，`docs/test-log-*.md` 引用的原始日志在 clone 后就不存在了。
!docs/test-logs/**/*.log

# Local configuration
config/.env

# ============================================================
# 密钥 / 证书 / 敏感文件（严禁提交到版本库）
# ------------------------------------------------------------
# 背景：支付宝私钥等曾以 keys/*.pem 形式被误提交，
#       已被 git rm --cached 移出索引（本地文件保留）。
#       请在克隆/部署后自行放置密钥文件。
# ============================================================
keys/
*.pem
*.key
*.p8
*.p12
*.pfx
*.crt
*.cer

# 环境变量文件（保留模板 .env.example）
.env
.env.*
!.env.example
!*.env.example
# ⚠️ 例外：前端构建变量文件（**仅含公开的 VITE_* 值**，会被编译进浏览器产物，
#    属必须随仓库分发的构建输入）——必须写在本文件（子目录 .gitignore 优先级高于根目录）。
#    ⚠️ 后续严禁往这两个文件写入任何真正的密钥。
!web/.env.development
!web/.env.production

# 本地数据库文件（SQLite 等）
*.db
*.sqlite
*.sqlite3

# 测试 / 调试产物
pytest_run.txt
diag*.txt
final_test.txt
test_run_output.txt
