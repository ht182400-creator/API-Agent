# 测试运行日志 — 2026-09-17（K 轮：P2「钱相关」页面用例）

> 承接 `docs/test-log-2026-09-16.md`。每条记录含 **案例ID / 目标 / 命令与参数 / 过程 / 结果**；
> 失败必须保留原始报错，不许只记"通过/失败"。
> 原始输出见 `docs/test-logs/2026-09-17/NN-*.log`（自 2026-09-17 起已纠正 `.gitignore`，**纳入版本库**）。

环境基线：commit `2b15cd3b`，前端 **202 passed / 21 文件**，typecheck 0，警告预算 act 102 + jsdom 90。

---

## 轮次 K：`admin/PricingConfig`（P2，直接决定计费金额）

### K-0 | 选它的理由

剩余 P2 里按"价值/风险"排序，本页排第一：它是**直接决定计费金额**的管理页 —— 配置错了就是资损。
原计划 2 条用例太笼统（"配置回显与必填校验"），拆成 4 条更可验收：

| 用例 | 目标 |
|---|---|
| TC-FE-PRICING-001 | 列表**派生文案**回显（三种计费模式的价格/免费额度；VIP 折扣仅前 2 档；状态→操作按钮映射） |
| TC-FE-PRICING-002 | 编辑回显：数值字段精度 + 计费模式**联动字段**（call 不出现 token/package 字段） |
| TC-FE-PRICING-003 | 套餐类配置的 `packages` 被**序列化为可解析 JSON 文本**回显 |
| TC-FE-PRICING-004 | 必填校验拦截 → 补全后创建成功 → **重新拉取列表** |

- **文件**：`web/src/pages/admin/PricingConfig.spec.tsx`（新建）
- **命令**：`npx vitest run src/pages/admin/PricingConfig.spec.tsx`

### K-1 | ⚠️ 环境债 1：补测才发现该页仍带废弃属性

本页有 `<Modal destroyOnClose>` —— antd v5 已废弃（应为 `destroyOnHidden`）。
**为什么一直没被发现**：2026-09-16 的"警告清零"只覆盖了**被测试渲染到**的代码；
本页此前**无任何测试渲染**，所以那条废弃警告从未出现过。

> 教训复述（9-16 已记过一次）：要区分「代码里有多少处」和「测试实际触发多少条」。
> 补测等于把这页从"盲区"搬进"被渲染区" → 隐藏的废弃属性会立刻顶穿警告预算。

- **处理**：`PricingConfig.tsx` 的 `destroyOnClose` → `destroyOnHidden`。

**同类残留盘点**（本轮顺带扫出，供后续轮次按序处理 —— 这些页一旦补测就必须先清）：

| 文件 | 残留废弃属性 |
|---|---|
| `admin/Reconciliation.tsx` | `Card bodyStyle` ×3（移动端卡片） |
| `admin/AdminLogs.tsx` | `Card bodyStyle` ×5+ |
| `developer/RepoDetail.tsx` | `Card bodyStyle` ×5 |
| `developer/Usage.tsx` / `Quota.tsx` / `Logs.tsx` / `Keys.tsx` | `Card bodyStyle` 各 1~2 |
| `superadmin/SuperAdminUsers.tsx` | `Card bodyStyle` ×1 |

### K-2 | ⚠️ 环境债 2：测试跑的是 antd **英文** locale（与生产不一致）

`main.tsx` 用 `<ConfigProvider locale={zhCN}>`，而 `renderWithProviders` **没带 locale**
→ 测试里 Modal 按钮是 `OK`/`Cancel`、Table 空态是 `No data`，真实界面是 `确定`/`取消`/`暂无数据`。
若用例断言这些文案，等于"测了一个线上不存在的界面"。

- **修复**：`renderWithProviders` 加 `locale={zhCN}`，与 `main.tsx` 对齐。
- **回归确认**：既有 200 余条用例**全部照常通过**（此前无任何用例依赖英文文案）。

### K-3 | 首跑失败（保留原始报错）→ 修正

- **命令**：`npx vitest run src/pages/admin/PricingConfig.spec.tsx`（日志 `01-K轮-PricingConfig首跑.log`）
- **结果**：`4 tests | 1 failed`（其余 3 条一次通过）
- **失败原文**：
  ```
  TestingLibraryElementError: Unable to find an element with the text: 按调用.
  This could be because the text is broken up by multiple elements.
  ```
- **分析**：我把两处文案搞混了 —— 表格 Tag 用列 render 里的**短文案**（`按调用`），
  而弹窗里的 Select 回显的是 option 的 **label**（`按调用计费`）。断言应取后者。
- **修正**：断言改为 `within(modal).getByText('按调用计费')` 并加注释说明两处文案不同。

### K-4 | 复跑失败（保留原始报错）→ 修正

- **命令**：同上（日志 `02-K轮-PricingConfig复跑.log`）
- **结果**：`4 tests | 1 failed`
- **失败原文**：
  ```
  AssertionError: expected '0.0200' to be '0.02' // Object.is equality
  ```
- **分析**：价格字段带 `precision={4}`，InputNumber 回显是 `0.0200` 而非 `0.02`。
- **修正**：断言改为 `'0.0200'`（并把这条"精度"写进注释）。

### K-5 | 三跑通过

- **命令**：同上（日志 `03-K轮-PricingConfig三跑.log`）
- **结果**：`4 passed` ✅

### K-6 | 全量：唯一失败是用例库自检（预期内，已同步）

- **命令**：`npm run test:budget`（日志 `04-K轮-全量预算-zhCN与Pricing.log`）
- **结果**：**1 failed** —— `TC-FE-CASES-007: meta.stats.specFiles 与磁盘上的 spec 数一致`
- **分析**：新增 1 个 spec（21 个 src spec）而登记仍是 20 → **自检按设计把"登记与现实脱节"抓了出来**。
  属预期，不是缺陷 —— 同步登记即可。
- **同步**：`FE-ADMIN-PRICING` `planned → covered`（4 条 done）；`specFiles` 20 → **21**；
  `cases` 195 → **199**（实测 206 tests − 7 条自检）；`Test_ALL_PLAN.md` §2.3/§2.5 同步 206。

### K-7 | jsdom 警告 90 → 97：更新基线 + 修"基线重建会抹掉说明"的缺陷

新 spec 会渲染更多 antd 组件 → `jsdom: getComputedStyle pseudo-elements` 由 90 增至 **97**
（act 仍 102，未变）。这两类属**已论证接受**的环境限制/组件特性，且**随 spec 数量自然增长**。

- 按实测更新基线（act 102 / jsdom 97），并在 `_note` 里写明"绝对值按轮更新、真正的硬门是新种类"。
- ⚠️ **顺带修掉一个真实缺陷**：`check-test-warnings.mjs` 的 `--update-baseline`
  （脚本注释里推荐的用法）会**整体重写**基线文件 → 把 `_note` 里"已消除种类清单"**一并抹掉**，
  文档随命令一起消失。已改为只替换 `counts` / `updatedAt`，其余字段保留。
- **验证**：实跑 `npm run test:budget -- --update-baseline`（日志 `06-K轮-基线重建保留note.log`）
  → 输出"（已保留 _note 等说明字段）"，复核文件 `has _note: True（27 行）`、counts = 102/97 ✅

### K-8 | 终验

| 项 | 命令 | 结果 |
|---|---|---|
| 类型检查 | `npm run typecheck` | 0 错误 |
| 单元测试 | `npm run test:budget` | **Test Files 22 passed (22) / Tests 206 passed (206)** |
| 警告预算 | 同上 | ✅ 预算内（act 102 / jsdom 97） |

---

## 轮次 K 小结

- 新增 1 个 spec / 4 条用例：`admin/PricingConfig`（P2 最高风险页：直连计费金额）→ **covered**
- 修掉 2 笔"只在补测时才暴露"的环境债：`destroyOnClose`、测试 locale 与生产不一致
- 修掉警告预算脚本 1 个缺陷（`--update-baseline` 抹掉 `_note`）
- 前端终态：**206 passed / 22 文件**，typecheck 0，预算通过；用例库登记 199 条组件用例

**下一轮计划（P2 续）**：`admin/Reconciliation`（对账差异必须精确，含 3 处 `bodyStyle` 待清）
→ `developer/Keys`（明文 key 只展示一次的安全语义）→ `developer/Billing` / `Logs` / `Quota`。

---

## 轮次 L：`admin/Reconciliation`（P2，对账差异必须精确）

### L-0 | 用例设计（4 条）

| 用例 | 目标 |
|---|---|
| TC-FE-RECON-001 | 首屏按「日期+渠道」查询；状态经 `STATUS_NAMES` **前端映射**；四项统计；差异>0 时告警 |
| TC-FE-RECON-002 | **404 视为"该日无对账记录"（不进统一错误弹窗）**，非 404（500）才交统一错误处理 |
| TC-FE-RECON-003 | 「执行对账」带**当前查询条件**提交，成功后提示并重新拉取结果 |
| TC-FE-RECON-004 | 差异记录按 `reconciliation_id` 查询；差异金额**正负高亮**；仅待处理行可操作 |

其中 **002 是本轮最有价值的用例**：`loadResult` 的 catch 里有
`if (error?.response?.status !== 404) showError(...)` —— 即"没对过账"是**正常状态**而非故障。
这条分类逻辑一旦改错，运营天天会看到系统错误弹窗。为此把统一错误处理 `useErrorModal`
整体换成 spy，正反两面都断言（404 不调 / 500 调）。

### L-1 | 顺带清掉的三笔"补测才暴露"的债

本页此前零测试渲染，因此：
- `Card bodyStyle` ×3（antd v5 已废弃）→ `styles={{ body: { padding: 12 } }}`；
- `import { useAuthStore } from '../../stores/auth'` 是**死导入**（全文件仅此一行，
  无任何使用）—— 因 `tsconfig` 里 `noUnusedLocals: false` 才一直没暴露 → 已移除并注释说明。

### L-2 | 首跑失败 2 条（保留原始报错）→ 修正

- **命令**：`npx vitest run src/pages/admin/Reconciliation.spec.tsx`（日志 `07-L轮-Reconciliation首跑.log`）
- **结果**：`4 tests | 2 failed`
- **失败原文**：
  ```
  TestingLibraryElementError: Found multiple elements with the text: 支付宝
  TestingLibraryElementError: Unable to find an element with the text: 暂无对账数据
  ```
- **分析**：
  ① 「支付宝」在页面上本就出现**两次**（顶栏查询条件的 Select 回显 + 结果卡片的
     `channel_name`）—— 这是**真实的界面形态**，不是缺陷 → 改为先定位结果卡片再 `within` 断言；
  ② 002 的 404 分支我**忘了先把 mock 设成 404**（`beforeEach` 让它 resolve 了）→ 补上。
- **复跑**（日志 `08-L轮-Reconciliation复跑.log`）：`4 passed` ✅

### L-3 | 全量 + 预算（日志 `09-L轮-全量预算.log`）

- **命令**：`npm run test:budget`
- **结果**：`Test Files 23 passed (23)` / `Tests 210 passed (210)` / `[warn-budget] ✅ 警告在预算内`
  （act 102 / **jsdom 98**，较上轮 +1，属"新增 spec 渲染更多 antd 组件"的预期增长 → 按实测更新基线）

### L-4 | 用例库同步

`FE-ADMIN-RECON`：`planned → covered`（4 条 done，`why` 里写入 404 特判这条关键语义）；
`meta.stats`：`specFiles` 21 → **22**、`cases` 199 → **203**（实测 210 − 7 条自检）；
`Test_ALL_PLAN.md` §2.3 / §2.5 同步 210。

## 轮次 L 小结

- 新增 1 个 spec / 4 条用例：`admin/Reconciliation` → **covered**
  （含一条"错误分类"的正反两面断言：404 = 无数据、500 = 交统一错误处理）
- 清掉 3 处废弃 `bodyStyle` + 1 个死导入
- 前端终态：**210 passed / 23 文件**，typecheck 0，预算通过；用例库登记 203 条组件用例

**下一轮计划**：`developer/Keys`（明文 key 只在创建时展示一次的安全语义，含 `bodyStyle` 待清）
→ `developer/Billing`（环境标签必须与 /health 同源，已有 2 条用例）→ `developer/Logs` / `Quota`
→ `admin/AdminLogs`（`bodyStyle` ×5+）。

---

## 轮次 M：`developer/Keys`（P2，凭据安全语义 + 不可逆操作）

### M-0 | 用例设计（5 条）

| 用例 | 目标 |
|---|---|
| TC-FE-KEYS-001 | 列表只显示 `前缀...****`；**即便后端把完整 key 带回列表也一个字符都不渲染**；派生文案 |
| TC-FE-KEYS-002 | 创建后一次性展示完整 key + 「仅显示一次」提示；关闭后不再残留；默认限流原样提交；重拉列表 |
| TC-FE-KEYS-003 | 删除二次确认：**取消不发请求**；确认后才删除并重拉 |
| TC-FE-KEYS-004 | 禁用/启用**按当前状态**给出对应操作，成功后提示并重拉 |
| TC-FE-KEYS-005 | 普通用户：显示升级引导、**隐藏创建入口**（列表仍只读可见） |

设计要点：
- 001 的 mock 数据里**故意带上完整 `api_key`** —— 用来证明"即使后端把明文带回来了，
  列表 UI 也绝不渲染它"（断言 `document.body.textContent` 不含 `FULL-SECRET`）；
- 005 通过 `useAuthStore.setState({ user: { user_type: 'user' } })` 直接驱动权限分支。

### M-1 | ⚠️ 首跑暴露一个真实问题：一次性密钥关闭后仍留在 DOM 里

- **命令**：`npx vitest run src/pages/developer/Keys.spec.tsx`（日志 `10-M轮-Keys首跑.log`）
- **结果**：`5 tests | 1 failed`
- **失败原文**：
  ```
  AssertionError: expected '...' not to contain 'FULL-SECRET-ONE-TIME'
  ```
- **分析**：antd Modal 默认**关闭后只做隐藏挂载**（`destroyOnHidden` 为 false）→ 点「我已保存」
  后明文虽然不可见，但**仍留在 DOM 里**。而该弹窗自己的文案写着"仅显示一次 / 无法找回"
  —— 行为与语义不符。
- **修法（改组件，而不是放宽断言）**：给两个**承载明文**的弹窗加 `destroyOnHidden`
  （创建成功弹窗 + 查看弹窗），关闭后真正把内容从 DOM 移除。
- **复跑**（日志 `11-M轮-Keys复跑.log`）：`5 passed` ✅

**教训**：静默"通过"的最省事做法是把断言改宽（`not.toBeVisible()`）；
但这里用户的意图是"仅显示一次"，DOM 里留明文与该意图冲突 → 应当改实现。
**断言写不下去时，先问"是断言太严，还是实现确实不对"。**

### M-2 | 顺带清掉的债

- 移动端卡片 `Card bodyStyle` → `styles={{ body: { padding: 12 } }}`
  （该分支在 jsdom 下不渲染，故一直不进警告预算 —— 一旦真机/窄屏渲染就会刷警告）。

### M-3 | 与上一轮的差异点（页面间不统一，需逐页确认）

| 页面 | 提示实现 | 拦截方式 |
|---|---|---|
| `Reconciliation` | `message.useMessage()`（hook 版） | 必须替换 `useMessage` |
| `Keys` / `PricingConfig` | 静态 `message.success` | 替换静态方法即可 |

> 结论：**mock 之前先确认该页用的是 hook 版还是静态版**，否则会"测试全绿但一条提示都没拦到"。

### M-4 | 全量 + 预算（日志 `12-M轮-全量预算.log`）

- **命令**：`npm run test:budget`
- **结果**：`Test Files 24 passed (24)` / `Tests 215 passed (215)` / `[warn-budget] ✅ 警告在预算内`
  （act 102 / **jsdom 106**，+8，属新增 spec 的预期增长 → 按实测更新基线）

### M-5 | 用例库同步

`FE-DEV-KEYS`：`planned → covered`（5 条 done）；`meta.stats`：`specFiles` 22 → **23**、
`cases` 203 → **208**（实测 215 − 7 条自检）；`Test_ALL_PLAN.md` §2.3 / §2.5 同步 215。

## 轮次 M 小结

- 新增 1 个 spec / 5 条用例：`developer/Keys` → **covered**
- 发现并修复 1 个真实问题：**一次性明文密钥关闭后仍留在 DOM** → 两个明文弹窗加 `destroyOnHidden`
- 清掉 1 处废弃 `bodyStyle`
- 前端终态：**215 passed / 24 文件**，typecheck 0，预算通过；用例库登记 208 条组件用例

**下一轮计划**：`developer/Billing`（环境标签须与 `/health` 同源，已有 2 条用例，含 5 个统计卡
`bodyStyle`）→ `developer/Usage` / `Quota` / `Logs` / `CreateRepo` / `RepoDetail`
→ `admin/AdminLogs`（`bodyStyle` ×5+）→ 尾部 `MISC-001/003` / `UI-001~002`。

---

## 轮次 N：`developer/Billing`（P2，金额与统计；扩写既有 spec）

### N-0 | 新增 4 条用例（总 6 条）

| 用例 | 目标 |
|---|---|
| TC-FE-BILLING-001 | 账单列表派生文案（类型 Tag / 正负金额 / 变动后余额 / 时间到秒 / 两处合计）+ **首屏只请求一次** |
| TC-FE-BILLING-002 | 切换每页条数后带新 `page_size` 重新查询 |
| TC-FE-BILLING-003 | 月度汇总卡片（本月充值/消费/调用）与**当前月份回显**；无记录时空态与引导 |
| TC-FE-BILLING-004 | 日期筛选带 `start_date/end_date` 重查；**「导出」沿用同一区间** |

> 该 spec 原先只有 2 条（环境标签同源回归），本轮**扩写**而非新建。

### N-1 | ⚠️ 抓到一个真实的"重复请求"缺陷（先让测试说出真相）

- **做法**：先写"首屏只应拉一次账单"的断言，**不改代码**，跑出证据。
- **失败原文**（日志 `16-N轮-Billing三跑.log`）：
  ```
  AssertionError: expected "spy" to be called 1 times, but got 2 times
  ```
- **根因**：本页有两个 effect 都会拉账单 ——
  `fetchData()`（内部调用 `fetchBills()`）与 `useEffect(fetchBills, [page,pageSize,dateRange])`；
  挂载时两者都触发 → 首屏账单被请求 2 次。
- **修法**：把 `fetchBills()` 从 `fetchData()` 中摘出，新增 `handleRefresh()` 让「刷新」按钮
  同时重拉概览与账单（保持手动刷新语义不变），首屏由单一 effect 负责。
- **复跑**（日志 `17-N轮-Billing四跑.log`）：`6 passed` ✅

### N-2 | 顺带修一处金额显示顺序

金额列原写法 `{amount>=0?'+':''}¥{amount.toFixed(2)}` → 负数渲染为 **`¥-12.50`**
（符号夹在货币符号之后）。补测时由断言 `-¥12.50` 失败暴露，已改为
`{amount>=0?'+':'-'}¥{Math.abs(amount).toFixed(2)}`（桌面表格 + 移动端卡片同步）；
另清移动端 `Card bodyStyle`。

### N-3 | 三个 jsdom 技法（本轮新踩）

① **同一文案既是"类型标签"又是"页面按钮"时要按行作用域断言**：`充值` 同时是账单类型 Tag
   与顶部充值按钮 → 全局 `getByText('充值')` 报多元素（首跑即此错）→ 改为
   `within(该行 tr).getByText(...)`；并让 mock 的描述文案与类型文案**不重叠**（`会员充值` vs `充值`）。
② **antd `RangePicker` 必须用真实键盘输入**：`fireEvent.change` 只改 DOM value，
   rc-picker 的内部选择态不更新 → 不触发 onChange（实测）。正确写法：
   `await user.click(input); await user.type(input, '2026-09-01'); await user.keyboard('{Enter}')`。
③ **金额断言要留意 JSX 文本节点拼接**：`{a}{b}{c}` 会渲染成多个文本节点，
   Testing Library 的 `getNodeText` 会拼接——所以 `+¥100.00` 能匹配，但**顺序**由代码决定
   （这正是 `¥-12.50` 这个缺陷能被断言捉到的原因）。

### N-4 | 全量 + 预算（日志 `18-N轮-全量预算.log`）

`Test Files 24 passed (24)` / `Tests 219 passed (219)` / `[warn-budget] ✅ 警告在预算内`
（act 102 / **jsdom 110**，+4，按实测更新基线）

### N-5 | 用例库同步

`FE-DEV-BILLING` 保持 `partial`（图表绘制与普通用户引导仍无覆盖，已写进 `why`）；
4 条 planned → done；`meta.stats.cases` 208 → **212**（实测 219 − 7）；
`specFiles` 不变（扩写既有 spec）；`Test_ALL_PLAN.md` §2.3 / §2.5 同步 219。

## 轮次 N 小结

- 新增 4 条用例（Billing 共 6 条）：账单列表 / 分页 / 月度汇总 / 日期筛选 + 导出
- **抓到并修复 1 个真实缺陷：首屏账单重复请求（2 次）**
- 修复 1 处金额显示顺序（`¥-12.50` → `-¥12.50`）+ 清 1 处 `bodyStyle`
- 前端终态：**219 passed / 24 文件**，typecheck 0，预算通过；用例库登记 212 条组件用例

**下一轮计划**：`developer/Usage`（含 `bodyStyle`）→ `developer/Quota` → `developer/Logs`
→ `developer/RepoDetail`（`bodyStyle` ×5）→ `admin/AdminLogs`（`bodyStyle` ×5+）
→ 尾部 `MISC-001/003` / `UI-001~002`。
