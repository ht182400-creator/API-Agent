# 测试运行日志 — 2026-09-17（K 轮：P2「钱相关」页面用例）

> 承接 `docs/test-log-2026-09-16.md`。每条记录含 **案例ID / 目标 / 命令与参数 / 过程 / 结果**；
> 失败必须保留原始报错，不许只记"通过/失败"。
> 原始输出见 `docs/test-logs/2026-09-17/NN-*.log`（自 2026-09-17 起已纠正 `.gitignore`，**纳入版本库**）。

环境基线：commit `2b15cd3b`，前端 **202 passed / 21 文件**，typecheck 0，警告预算 act 102 + jsdom 90。

---

## 轮次 K：`admin/PricingConfig`（P2，直接决定计费金额）

### K-0 | 选它的理由

剩余 P2 里按"价值/风险"排序，本页排第一：它是**直接决定计费金额**的管理页 —— 配置错了就是资损。
原计划 2 条用例太笼统（"配置回显与必填校验"），拆成 4 条更可验收：

| 用例 | 目标 |
|---|---|
| TC-FE-PRICING-001 | 列表**派生文案**回显（三种计费模式的价格/免费额度；VIP 折扣仅前 2 档；状态→操作按钮映射） |
| TC-FE-PRICING-002 | 编辑回显：数值字段精度 + 计费模式**联动字段**（call 不出现 token/package 字段） |
| TC-FE-PRICING-003 | 套餐类配置的 `packages` 被**序列化为可解析 JSON 文本**回显 |
| TC-FE-PRICING-004 | 必填校验拦截 → 补全后创建成功 → **重新拉取列表** |

- **文件**：`web/src/pages/admin/PricingConfig.spec.tsx`（新建）
- **命令**：`npx vitest run src/pages/admin/PricingConfig.spec.tsx`

### K-1 | ⚠️ 环境债 1：补测才发现该页仍带废弃属性

本页有 `<Modal destroyOnClose>` —— antd v5 已废弃（应为 `destroyOnHidden`）。
**为什么一直没被发现**：2026-09-16 的"警告清零"只覆盖了**被测试渲染到**的代码；
本页此前**无任何测试渲染**，所以那条废弃警告从未出现过。

> 教训复述（9-16 已记过一次）：要区分「代码里有多少处」和「测试实际触发多少条」。
> 补测等于把这页从"盲区"搬进"被渲染区" → 隐藏的废弃属性会立刻顶穿警告预算。

- **处理**：`PricingConfig.tsx` 的 `destroyOnClose` → `destroyOnHidden`。

**同类残留盘点**（本轮顺带扫出，供后续轮次按序处理 —— 这些页一旦补测就必须先清）：

| 文件 | 残留废弃属性 |
|---|---|
| `admin/Reconciliation.tsx` | `Card bodyStyle` ×3（移动端卡片） |
| `admin/AdminLogs.tsx` | `Card bodyStyle` ×5+ |
| `developer/RepoDetail.tsx` | `Card bodyStyle` ×5 |
| `developer/Usage.tsx` / `Quota.tsx` / `Logs.tsx` / `Keys.tsx` | `Card bodyStyle` 各 1~2 |
| `superadmin/SuperAdminUsers.tsx` | `Card bodyStyle` ×1 |

### K-2 | ⚠️ 环境债 2：测试跑的是 antd **英文** locale（与生产不一致）

`main.tsx` 用 `<ConfigProvider locale={zhCN}>`，而 `renderWithProviders` **没带 locale**
→ 测试里 Modal 按钮是 `OK`/`Cancel`、Table 空态是 `No data`，真实界面是 `确定`/`取消`/`暂无数据`。
若用例断言这些文案，等于"测了一个线上不存在的界面"。

- **修复**：`renderWithProviders` 加 `locale={zhCN}`，与 `main.tsx` 对齐。
- **回归确认**：既有 200 余条用例**全部照常通过**（此前无任何用例依赖英文文案）。

### K-3 | 首跑失败（保留原始报错）→ 修正

- **命令**：`npx vitest run src/pages/admin/PricingConfig.spec.tsx`（日志 `01-K轮-PricingConfig首跑.log`）
- **结果**：`4 tests | 1 failed`（其余 3 条一次通过）
- **失败原文**：
  ```
  TestingLibraryElementError: Unable to find an element with the text: 按调用.
  This could be because the text is broken up by multiple elements.
  ```
- **分析**：我把两处文案搞混了 —— 表格 Tag 用列 render 里的**短文案**（`按调用`），
  而弹窗里的 Select 回显的是 option 的 **label**（`按调用计费`）。断言应取后者。
- **修正**：断言改为 `within(modal).getByText('按调用计费')` 并加注释说明两处文案不同。

### K-4 | 复跑失败（保留原始报错）→ 修正

- **命令**：同上（日志 `02-K轮-PricingConfig复跑.log`）
- **结果**：`4 tests | 1 failed`
- **失败原文**：
  ```
  AssertionError: expected '0.0200' to be '0.02' // Object.is equality
  ```
- **分析**：价格字段带 `precision={4}`，InputNumber 回显是 `0.0200` 而非 `0.02`。
- **修正**：断言改为 `'0.0200'`（并把这条"精度"写进注释）。

### K-5 | 三跑通过

- **命令**：同上（日志 `03-K轮-PricingConfig三跑.log`）
- **结果**：`4 passed` ✅

### K-6 | 全量：唯一失败是用例库自检（预期内，已同步）

- **命令**：`npm run test:budget`（日志 `04-K轮-全量预算-zhCN与Pricing.log`）
- **结果**：**1 failed** —— `TC-FE-CASES-007: meta.stats.specFiles 与磁盘上的 spec 数一致`
- **分析**：新增 1 个 spec（21 个 src spec）而登记仍是 20 → **自检按设计把"登记与现实脱节"抓了出来**。
  属预期，不是缺陷 —— 同步登记即可。
- **同步**：`FE-ADMIN-PRICING` `planned → covered`（4 条 done）；`specFiles` 20 → **21**；
  `cases` 195 → **199**（实测 206 tests − 7 条自检）；`Test_ALL_PLAN.md` §2.3/§2.5 同步 206。

### K-7 | jsdom 警告 90 → 97：更新基线 + 修"基线重建会抹掉说明"的缺陷

新 spec 会渲染更多 antd 组件 → `jsdom: getComputedStyle pseudo-elements` 由 90 增至 **97**
（act 仍 102，未变）。这两类属**已论证接受**的环境限制/组件特性，且**随 spec 数量自然增长**。

- 按实测更新基线（act 102 / jsdom 97），并在 `_note` 里写明"绝对值按轮更新、真正的硬门是新种类"。
- ⚠️ **顺带修掉一个真实缺陷**：`check-test-warnings.mjs` 的 `--update-baseline`
  （脚本注释里推荐的用法）会**整体重写**基线文件 → 把 `_note` 里"已消除种类清单"**一并抹掉**，
  文档随命令一起消失。已改为只替换 `counts` / `updatedAt`，其余字段保留。
- **验证**：实跑 `npm run test:budget -- --update-baseline`（日志 `06-K轮-基线重建保留note.log`）
  → 输出"（已保留 _note 等说明字段）"，复核文件 `has _note: True（27 行）`、counts = 102/97 ✅

### K-8 | 终验

| 项 | 命令 | 结果 |
|---|---|---|
| 类型检查 | `npm run typecheck` | 0 错误 |
| 单元测试 | `npm run test:budget` | **Test Files 22 passed (22) / Tests 206 passed (206)** |
| 警告预算 | 同上 | ✅ 预算内（act 102 / jsdom 97） |

---

## 轮次 K 小结

- 新增 1 个 spec / 4 条用例：`admin/PricingConfig`（P2 最高风险页：直连计费金额）→ **covered**
- 修掉 2 笔"只在补测时才暴露"的环境债：`destroyOnClose`、测试 locale 与生产不一致
- 修掉警告预算脚本 1 个缺陷（`--update-baseline` 抹掉 `_note`）
- 前端终态：**206 passed / 22 文件**，typecheck 0，预算通过；用例库登记 199 条组件用例

**下一轮计划（P2 续）**：`admin/Reconciliation`（对账差异必须精确，含 3 处 `bodyStyle` 待清）
→ `developer/Keys`（明文 key 只展示一次的安全语义）→ `developer/Billing` / `Logs` / `Quota`。

---

## 轮次 L：`admin/Reconciliation`（P2，对账差异必须精确）

### L-0 | 用例设计（4 条）

| 用例 | 目标 |
|---|---|
| TC-FE-RECON-001 | 首屏按「日期+渠道」查询；状态经 `STATUS_NAMES` **前端映射**；四项统计；差异>0 时告警 |
| TC-FE-RECON-002 | **404 视为"该日无对账记录"（不进统一错误弹窗）**，非 404（500）才交统一错误处理 |
| TC-FE-RECON-003 | 「执行对账」带**当前查询条件**提交，成功后提示并重新拉取结果 |
| TC-FE-RECON-004 | 差异记录按 `reconciliation_id` 查询；差异金额**正负高亮**；仅待处理行可操作 |

其中 **002 是本轮最有价值的用例**：`loadResult` 的 catch 里有
`if (error?.response?.status !== 404) showError(...)` —— 即"没对过账"是**正常状态**而非故障。
这条分类逻辑一旦改错，运营天天会看到系统错误弹窗。为此把统一错误处理 `useErrorModal`
整体换成 spy，正反两面都断言（404 不调 / 500 调）。

### L-1 | 顺带清掉的三笔"补测才暴露"的债

本页此前零测试渲染，因此：
- `Card bodyStyle` ×3（antd v5 已废弃）→ `styles={{ body: { padding: 12 } }}`；
- `import { useAuthStore } from '../../stores/auth'` 是**死导入**（全文件仅此一行，
  无任何使用）—— 因 `tsconfig` 里 `noUnusedLocals: false` 才一直没暴露 → 已移除并注释说明。

### L-2 | 首跑失败 2 条（保留原始报错）→ 修正

- **命令**：`npx vitest run src/pages/admin/Reconciliation.spec.tsx`（日志 `07-L轮-Reconciliation首跑.log`）
- **结果**：`4 tests | 2 failed`
- **失败原文**：
  ```
  TestingLibraryElementError: Found multiple elements with the text: 支付宝
  TestingLibraryElementError: Unable to find an element with the text: 暂无对账数据
  ```
- **分析**：
  ① 「支付宝」在页面上本就出现**两次**（顶栏查询条件的 Select 回显 + 结果卡片的
     `channel_name`）—— 这是**真实的界面形态**，不是缺陷 → 改为先定位结果卡片再 `within` 断言；
  ② 002 的 404 分支我**忘了先把 mock 设成 404**（`beforeEach` 让它 resolve 了）→ 补上。
- **复跑**（日志 `08-L轮-Reconciliation复跑.log`）：`4 passed` ✅

### L-3 | 全量 + 预算（日志 `09-L轮-全量预算.log`）

- **命令**：`npm run test:budget`
- **结果**：`Test Files 23 passed (23)` / `Tests 210 passed (210)` / `[warn-budget] ✅ 警告在预算内`
  （act 102 / **jsdom 98**，较上轮 +1，属"新增 spec 渲染更多 antd 组件"的预期增长 → 按实测更新基线）

### L-4 | 用例库同步

`FE-ADMIN-RECON`：`planned → covered`（4 条 done，`why` 里写入 404 特判这条关键语义）；
`meta.stats`：`specFiles` 21 → **22**、`cases` 199 → **203**（实测 210 − 7 条自检）；
`Test_ALL_PLAN.md` §2.3 / §2.5 同步 210。

## 轮次 L 小结

- 新增 1 个 spec / 4 条用例：`admin/Reconciliation` → **covered**
  （含一条"错误分类"的正反两面断言：404 = 无数据、500 = 交统一错误处理）
- 清掉 3 处废弃 `bodyStyle` + 1 个死导入
- 前端终态：**210 passed / 23 文件**，typecheck 0，预算通过；用例库登记 203 条组件用例

**下一轮计划**：`developer/Keys`（明文 key 只在创建时展示一次的安全语义，含 `bodyStyle` 待清）
→ `developer/Billing`（环境标签必须与 /health 同源，已有 2 条用例）→ `developer/Logs` / `Quota`
→ `admin/AdminLogs`（`bodyStyle` ×5+）。

---

## 轮次 M：`developer/Keys`（P2，凭据安全语义 + 不可逆操作）

### M-0 | 用例设计（5 条）

| 用例 | 目标 |
|---|---|
| TC-FE-KEYS-001 | 列表只显示 `前缀...****`；**即便后端把完整 key 带回列表也一个字符都不渲染**；派生文案 |
| TC-FE-KEYS-002 | 创建后一次性展示完整 key + 「仅显示一次」提示；关闭后不再残留；默认限流原样提交；重拉列表 |
| TC-FE-KEYS-003 | 删除二次确认：**取消不发请求**；确认后才删除并重拉 |
| TC-FE-KEYS-004 | 禁用/启用**按当前状态**给出对应操作，成功后提示并重拉 |
| TC-FE-KEYS-005 | 普通用户：显示升级引导、**隐藏创建入口**（列表仍只读可见） |

设计要点：
- 001 的 mock 数据里**故意带上完整 `api_key`** —— 用来证明"即使后端把明文带回来了，
  列表 UI 也绝不渲染它"（断言 `document.body.textContent` 不含 `FULL-SECRET`）；
- 005 通过 `useAuthStore.setState({ user: { user_type: 'user' } })` 直接驱动权限分支。

### M-1 | ⚠️ 首跑暴露一个真实问题：一次性密钥关闭后仍留在 DOM 里

- **命令**：`npx vitest run src/pages/developer/Keys.spec.tsx`（日志 `10-M轮-Keys首跑.log`）
- **结果**：`5 tests | 1 failed`
- **失败原文**：
  ```
  AssertionError: expected '...' not to contain 'FULL-SECRET-ONE-TIME'
  ```
- **分析**：antd Modal 默认**关闭后只做隐藏挂载**（`destroyOnHidden` 为 false）→ 点「我已保存」
  后明文虽然不可见，但**仍留在 DOM 里**。而该弹窗自己的文案写着"仅显示一次 / 无法找回"
  —— 行为与语义不符。
- **修法（改组件，而不是放宽断言）**：给两个**承载明文**的弹窗加 `destroyOnHidden`
  （创建成功弹窗 + 查看弹窗），关闭后真正把内容从 DOM 移除。
- **复跑**（日志 `11-M轮-Keys复跑.log`）：`5 passed` ✅

**教训**：静默"通过"的最省事做法是把断言改宽（`not.toBeVisible()`）；
但这里用户的意图是"仅显示一次"，DOM 里留明文与该意图冲突 → 应当改实现。
**断言写不下去时，先问"是断言太严，还是实现确实不对"。**

### M-2 | 顺带清掉的债

- 移动端卡片 `Card bodyStyle` → `styles={{ body: { padding: 12 } }}`
  （该分支在 jsdom 下不渲染，故一直不进警告预算 —— 一旦真机/窄屏渲染就会刷警告）。

### M-3 | 与上一轮的差异点（页面间不统一，需逐页确认）

| 页面 | 提示实现 | 拦截方式 |
|---|---|---|
| `Reconciliation` | `message.useMessage()`（hook 版） | 必须替换 `useMessage` |
| `Keys` / `PricingConfig` | 静态 `message.success` | 替换静态方法即可 |

> 结论：**mock 之前先确认该页用的是 hook 版还是静态版**，否则会"测试全绿但一条提示都没拦到"。

### M-4 | 全量 + 预算（日志 `12-M轮-全量预算.log`）

- **命令**：`npm run test:budget`
- **结果**：`Test Files 24 passed (24)` / `Tests 215 passed (215)` / `[warn-budget] ✅ 警告在预算内`
  （act 102 / **jsdom 106**，+8，属新增 spec 的预期增长 → 按实测更新基线）

### M-5 | 用例库同步

`FE-DEV-KEYS`：`planned → covered`（5 条 done）；`meta.stats`：`specFiles` 22 → **23**、
`cases` 203 → **208**（实测 215 − 7 条自检）；`Test_ALL_PLAN.md` §2.3 / §2.5 同步 215。

## 轮次 M 小结

- 新增 1 个 spec / 5 条用例：`developer/Keys` → **covered**
- 发现并修复 1 个真实问题：**一次性明文密钥关闭后仍留在 DOM** → 两个明文弹窗加 `destroyOnHidden`
- 清掉 1 处废弃 `bodyStyle`
- 前端终态：**215 passed / 24 文件**，typecheck 0，预算通过；用例库登记 208 条组件用例

**下一轮计划**：`developer/Billing`（环境标签须与 `/health` 同源，已有 2 条用例，含 5 个统计卡
`bodyStyle`）→ `developer/Usage` / `Quota` / `Logs` / `CreateRepo` / `RepoDetail`
→ `admin/AdminLogs`（`bodyStyle` ×5+）→ 尾部 `MISC-001/003` / `UI-001~002`。

---

## 轮次 N：`developer/Billing`（P2，金额与统计；扩写既有 spec）

### N-0 | 新增 4 条用例（总 6 条）

| 用例 | 目标 |
|---|---|
| TC-FE-BILLING-001 | 账单列表派生文案（类型 Tag / 正负金额 / 变动后余额 / 时间到秒 / 两处合计）+ **首屏只请求一次** |
| TC-FE-BILLING-002 | 切换每页条数后带新 `page_size` 重新查询 |
| TC-FE-BILLING-003 | 月度汇总卡片（本月充值/消费/调用）与**当前月份回显**；无记录时空态与引导 |
| TC-FE-BILLING-004 | 日期筛选带 `start_date/end_date` 重查；**「导出」沿用同一区间** |

> 该 spec 原先只有 2 条（环境标签同源回归），本轮**扩写**而非新建。

### N-1 | ⚠️ 抓到一个真实的"重复请求"缺陷（先让测试说出真相）

- **做法**：先写"首屏只应拉一次账单"的断言，**不改代码**，跑出证据。
- **失败原文**（日志 `16-N轮-Billing三跑.log`）：
  ```
  AssertionError: expected "spy" to be called 1 times, but got 2 times
  ```
- **根因**：本页有两个 effect 都会拉账单 ——
  `fetchData()`（内部调用 `fetchBills()`）与 `useEffect(fetchBills, [page,pageSize,dateRange])`；
  挂载时两者都触发 → 首屏账单被请求 2 次。
- **修法**：把 `fetchBills()` 从 `fetchData()` 中摘出，新增 `handleRefresh()` 让「刷新」按钮
  同时重拉概览与账单（保持手动刷新语义不变），首屏由单一 effect 负责。
- **复跑**（日志 `17-N轮-Billing四跑.log`）：`6 passed` ✅

### N-2 | 顺带修一处金额显示顺序

金额列原写法 `{amount>=0?'+':''}¥{amount.toFixed(2)}` → 负数渲染为 **`¥-12.50`**
（符号夹在货币符号之后）。补测时由断言 `-¥12.50` 失败暴露，已改为
`{amount>=0?'+':'-'}¥{Math.abs(amount).toFixed(2)}`（桌面表格 + 移动端卡片同步）；
另清移动端 `Card bodyStyle`。

### N-3 | 三个 jsdom 技法（本轮新踩）

① **同一文案既是"类型标签"又是"页面按钮"时要按行作用域断言**：`充值` 同时是账单类型 Tag
   与顶部充值按钮 → 全局 `getByText('充值')` 报多元素（首跑即此错）→ 改为
   `within(该行 tr).getByText(...)`；并让 mock 的描述文案与类型文案**不重叠**（`会员充值` vs `充值`）。
② **antd `RangePicker` 必须用真实键盘输入**：`fireEvent.change` 只改 DOM value，
   rc-picker 的内部选择态不更新 → 不触发 onChange（实测）。正确写法：
   `await user.click(input); await user.type(input, '2026-09-01'); await user.keyboard('{Enter}')`。
③ **金额断言要留意 JSX 文本节点拼接**：`{a}{b}{c}` 会渲染成多个文本节点，
   Testing Library 的 `getNodeText` 会拼接——所以 `+¥100.00` 能匹配，但**顺序**由代码决定
   （这正是 `¥-12.50` 这个缺陷能被断言捉到的原因）。

### N-4 | 全量 + 预算（日志 `18-N轮-全量预算.log`）

`Test Files 24 passed (24)` / `Tests 219 passed (219)` / `[warn-budget] ✅ 警告在预算内`
（act 102 / **jsdom 110**，+4，按实测更新基线）

### N-5 | 用例库同步

`FE-DEV-BILLING` 保持 `partial`（图表绘制与普通用户引导仍无覆盖，已写进 `why`）；
4 条 planned → done；`meta.stats.cases` 208 → **212**（实测 219 − 7）；
`specFiles` 不变（扩写既有 spec）；`Test_ALL_PLAN.md` §2.3 / §2.5 同步 219。

## 轮次 N 小结

- 新增 4 条用例（Billing 共 6 条）：账单列表 / 分页 / 月度汇总 / 日期筛选 + 导出
- **抓到并修复 1 个真实缺陷：首屏账单重复请求（2 次）**
- 修复 1 处金额显示顺序（`¥-12.50` → `-¥12.50`）+ 清 1 处 `bodyStyle`
- 前端终态：**219 passed / 24 文件**，typecheck 0，预算通过；用例库登记 212 条组件用例

**下一轮计划**：`developer/Usage`（含 `bodyStyle`）→ `developer/Quota` → `developer/Logs`
→ `developer/RepoDetail`（`bodyStyle` ×5）→ `admin/AdminLogs`（`bodyStyle` ×5+）
→ 尾部 `MISC-001/003` / `UI-001~002`。

---

## 轮次 O：`developer/Quota`（P2，配额换算与风险信号）

### O-0 | 用例设计（5 条）

| 用例 | 目标 |
|---|---|
| TC-FE-QUOTA-001 | 首屏「取 Key 列表 → 自动选中首个 → 按该 Key 拉配额/历史/Top 仓库」（**参数**正确） |
| TC-FE-QUOTA-002 | 百分比与状态分档：**0% / 70% / 90% / 100% 四个边界 + 超额 120% 截断为 100 + limit=null 不画进度** |
| TC-FE-QUOTA-003 | RPM/RPH 达限标「已达限」；限额缺失时**回退 1000 / 10000** |
| TC-FE-QUOTA-004 | 余额不足 1 元 → **error 级**提醒 + 充值引导；未启用余额扣费则**不提醒** |
| TC-FE-QUOTA-005 | 总调用合计、Top 仓库占比与排名徽标；无 Key 时给空态与创建引导 |

为什么值得测：本页把**原始计数换算成"人能看到的风险信号"**（百分比 / 分档 / 达限 / 余额告警），
换算错了运营会误判要不要扩容或充值。

### O-1 | 首跑失败 1 条（保留原始报错）→ 修正

- **命令**：`npx vitest run src/pages/developer/Quota.spec.tsx`（日志 `19-O轮-Quota首跑.log`）
- **结果**：`5 tests | 1 failed`
- **失败原文**：
  ```
  TestingLibraryElementError: Found multiple elements with the text: 无限制
  ```
- **分析**：`limit=null` 时，「无限制」在**同一张卡片内出现两次** —— 状态 Tag + 底部"剩余: 无限制"。
  同类风险还有「正常」会同时出现在状态 Tag 与 RPM/RPH 的 Badge 上。
- **修正**：断言改为读卡片内**唯一的 `.ant-tag`**（`[...card.querySelectorAll('.ant-tag')]`）→
  既避开重复文案，又比 `getByText` 更精确。
- **复跑**（日志 `20-O轮-Quota复跑.log`）：`5 passed` ✅

### O-2 | 两条断言技法（本轮新踩）

① **`showInfo={false}` 的进度条没有百分比文字** → 只能读 `.ant-progress-bg` 的 `style.width`
   （`Math.round(parseFloat(bg.style.width || '0'))`）；而带 `format` 的进度条会渲染
   `.ant-progress-text`（如 `75%`，可直接 `getByText`）。
② **「正常 / 无限制」这类通用文案跨卡片、跨元素重复** → 一律按卡片作用域
   （`within(cardOf('每日配额'))`）或按 `.ant-tag` 精确断言，不要用全局 `getByText`。

### O-3 | 顺带清掉的债

移动端卡片 `Card bodyStyle` → `styles={{ body: { padding: 12 } }}`。

### O-4 | 全量 + 预算（日志 `21-O轮-全量预算.log`）

`Test Files 25 passed (25)` / `Tests 224 passed (224)` / `[warn-budget] ✅ 警告在预算内`
（act 102 / **jsdom 121**，+11 —— 本页 antd 组件密度高（4 张统计卡 + 2 个表 + 4 个进度条），
属预期增长，按实测更新基线）

### O-5 | 用例库同步

`FE-DEV-QUOTA`：`planned → covered`（5 条 done，`why` 里写入两条断言要点与未覆盖面）；
`meta.stats`：`specFiles` 23 → **24**、`cases` 212 → **217**（实测 224 − 7）；
`Test_ALL_PLAN.md` §2.3 / §2.5 同步 224。

## 轮次 O 小结

- 新增 1 个 spec / 5 条用例：`developer/Quota` → **covered**
  （含 4 个百分比边界 + 超额截断 + 限额回退 1000/10000 + 余额告警级别）
- 清掉 1 处 `bodyStyle`
- 前端终态：**224 passed / 25 文件**，typecheck 0，预算通过；用例库登记 217 条组件用例

**下一轮计划**：`developer/Logs`（含 `bodyStyle`）→ `developer/RepoDetail`（`bodyStyle` ×5）
→ `admin/AdminLogs`（`bodyStyle` ×5+）→ `developer/Usage`（属 FE-DEV-MISC 聚合套件）
→ 尾部 `MISC-001/003` / `UI-001~002`。

---

## 轮次 P：`developer/Logs` + `developer/CreateRepo` + `developer/RepoDetail`（一次做完）

### P-0 | 先做一次全库废弃属性清零

用脚本对 `src/**/*.tsx` 做机械替换 `bodyStyle={{ padding: 12 }}` → `styles={{ body: { padding: 12 } }}`：
共修正 **6 个文件**（`admin/analytics/RepoDetailModal`、`admin/AdminLogs`、`developer/Logs`、
`developer/RepoDetail`、`developer/Usage`、`superadmin/SuperAdminUsers`）。
复核：`bodyStyle=` **0** 处、`destroyOnClose` **0** 处。

### P-1 | 三个新 spec / 14 条用例

| Suite | 用例数 | 重点 |
|---|---|---|
| `FE-DEV-LOGS` | 4 | 排障入口：状态码→文案（2xx/4xx/5xx/缺省）、耗时→分档（0.5/1/2s）、列表字段降级（空仓库名→未知、路径>30 截断、无 request_id 降级用记录 id、IP 空→`-`）、**筛选参数**（选 Key→`key_id`、「全部 Keys」→`undefined`、日期→区间、清除筛选复原、均重置到第 1 页）、分页切换、空态 |
| `FE-DEV-CREATEREPO` | 5 | 校验（3 条必填 / 名称长度与字符集 / 端点 URL 格式）；**管理员 vs 非管理员**的提示·按钮·跳转目标；后端 `detail` 优先展示；图标上传两道硬限制（仅图片、≤200KB） |
| `FE-DEV-REPODETAIL` | 5 | 基本信息与 SLA；统计/限流（**limits 缺失回退 1000/10000/100000**）；定价（模式映射 + `toFixed(4)` 精度 + 免费额度）；端点表空值降级与空态、**单端点时指南自动展开**；仓库不存在（返回空 / 请求失败两条路径） |

### P-2 | 三个 jsdom 技法（本轮新踩）

① **下拉弹层会留在 DOM 里**，选项文本与选中值同名（如「生产应用」）→ 按可见文本定位 Select
   必须用 `getAllByText` 再挑出属于 `.ant-select` 的那个（`getByText` 会报多元素）。
② **同一个 `<input type="file">` 连续两次 `fireEvent.change` 不会二次触发 `beforeUpload`**
   （rc-upload 内部状态，实测 `Number of calls: 1`）→ 多场景要**分开渲染**。
③ **同一文案在多个区块出现是常态**（协议 `HTTP` 在页头 + 「仓库信息」；端点路径在端点表 +
   调用指南折叠标题）→ 用 `getAllByText` 或按作用域断言，不要假设唯一。

### P-3 | 全量 + 预算（日志 `27-P轮-全量预算.log`）

`Test Files 28 passed (28)` / `Tests 238 passed (238)` / `[warn-budget] ✅ 警告在预算内`
（act 102 / **jsdom 133**，+12，按实测更新基线）

### P-4 | 用例库同步

三个 suite 均 `planned → covered`（4 + 5 + 5 条 done，`why` 里写入关键语义与断言要点）；
`meta.stats`：`specFiles` 24 → **27**、`cases` 217 → **231**（实测 238 − 7）；
`Test_ALL_PLAN.md` §2.3 / §2.5 同步 238。

## 轮次 P 小结

- 新增 3 个 spec / 14 条用例；全库废弃属性（`bodyStyle` / `destroyOnClose`）**清零**
- 前端终态：**238 passed / 28 文件**，typecheck 0，预算通过；用例库登记 231 条组件用例
- ⚠️ **剩余未覆盖**（下一步）：`FE-ADMIN-ADMINLOGS`（`admin/AdminLogs` 904 行）、
  `FE-ADMIN-MISC-001/003`、`FE-DEV-MISC-001`、`UI-001~002`

---

## 轮次 Q：`admin/AdminLogs`（P2，904 行 —— 日志内容 + 备份管理）

### Q-0 | 用例设计（5 条）

| 用例 | 目标 |
|---|---|
| TC-FE-ADMINLOGS-001 | 首屏三个数据源各拉一次；统计卡（含格式化体积）与日志/备份两个列表按数据渲染；自动备份徽标 |
| TC-FE-ADMINLOGS-002 | 查看日志按 `startLine=当前页×500 / maxLines=500` 拉取；切级别与输入关键字后**带 `level`/`keyword` 重拉**；日志行与行数文案渲染 |
| TC-FE-ADMINLOGS-003 | 备份配置**回显四项**（启用 / 文件大小 / 最大数量 / 自动清理 / 清理阈值）；保存**整体提交**并提示、关弹窗、重读统计 |
| TC-FE-ADMINLOGS-004 | 手动备份按文件 `module` 调用；清理备份**需二次确认**（未确认不发请求） |
| TC-FE-ADMINLOGS-005 | 删除备份：**取消不发请求**、确认才删除并重新拉取备份列表 |

本页价值：日志内容参数错了会"看起来没有这条日志"（排障误判）；备份配置回显/保存错了会改坏配置；
删除与清理都是不可逆操作 → 必须二次确认。

### Q-1 | 首跑失败 2 条（保留原始报错）→ 修正

- **命令**：`npx vitest run src/pages/admin/AdminLogs.spec.tsx`（日志 `28-Q轮-AdminLogs首跑.log`）
- **结果**：`5 tests | 2 failed`
- **失败原文**：
  ```
  TestingLibraryElementError: Found multiple elements with the text: 1.0 KB
  AssertionError: expected '...' to contain 'connection timeout'
  ```
- **分析**：
  ① `1.0 KB` 在**日志文件表与备份表各出现一次**（两个 mock 文件大小恰好相同）→ 不能 `getByText`；
  ② 弹窗打开后先渲染「加载中...」，**此时 body 里还没有日志行** —— 我在时间窗内取了文本。
- **修正**：① 改 `getAllByText('1.0 KB')` 并加注释；② 用 `await screen.findByText('connection timeout')`
  等待内容加载完成，再断言「共 1 行，当前 1 行」。
- **复跑**（日志 `29-Q轮-AdminLogs复跑.log`）：`5 passed` ✅

### Q-2 | 一条 mock 注意事项

`src/api/adminLogs.ts` 是**具名函数导出**（不是对象）→ 必须逐个替换
（`getLogFiles` / `getLogContent` / …），并 `...actual` 保留 `LOG_LEVELS` / `getLevelColor`
等纯数据导出（否则 Select 的选项渲染不出来）。已写入用例库 `why`。

### Q-3 | 全量 + 预算（日志 `30-Q轮-全量预算.log`）

`Test Files 29 passed (29)` / `Tests 243 passed (243)` / `[warn-budget] ✅ 警告在预算内`
（act 102 / **jsdom 147**，+14，按实测更新基线）

### Q-4 | 用例库同步

`FE-ADMIN-ADMINLOGS`：`planned → covered`（5 条 done）；
`meta.stats`：`specFiles` 27 → **28**、`cases` 231 → **236**（实测 243 − 7）；
`Test_ALL_PLAN.md` §2.3 / §2.5 同步 243。

## 轮次 Q 小结

- 新增 1 个 spec / 5 条用例：`admin/AdminLogs` → **covered**
- 前端终态：**243 passed / 29 文件**，typecheck 0，预算通过；用例库登记 236 条组件用例
- ⚠️ **剩余未覆盖**：`FE-ADMIN-MISC-001/003`、`FE-DEV-MISC-001`、`UI-001~002`（三组聚合项）

---

## 轮次 R：聚合套件收尾（21 页「首屏成功渲染」，用例库 planned 清零）

### R-0 | 口径修正

`UI-001~002` 经查**早已 done**（在 `FE-SHARED-UI` 里，由 `App.spec.tsx` 覆盖路由守卫）——
之前把它列为"剩余"是我读用例库时的误判。**真正剩下的是两个 MISC 套件**。

### R-1 | 做法：自动解析式 mock（不枚举方法名）

新建 `src/pages/misc/MiscPagesSuccess.spec.tsx`（与既有 `MiscPages.spec.tsx` 分工：
后者测「接口全挂不白屏」，本文件测「接口全部成功返回时渲染出内容 + 确实调了数据源」）。

核心是 `resolveAll(obj)`：用 `importOriginal` 拿到真实 api 对象后，
**把对象上的所有函数换成「成功返回」的 vi.fn()**，并登记进 registry 供
`anyCalled()`（有没有真的拉数据）与 `reapply()`（restoreMocks 会清实现，须重装）使用。
好处：**不需要枚举方法名**，也不用 Proxy（vitest 对 Proxy mock 的导出校验会挂死 —— 9-16 已踩过）。

### R-2 | 首跑失败 5 条（保留原始报错）→ 修正

- **命令**：`npx vitest run src/pages/misc/MiscPagesSuccess.spec.tsx`（日志 `31-R轮-MiscSuccess首跑.log`）
- **结果**：`40 tests | 5 failed`
- **失败原文（摘）**：
  ```
  TypeError: __vite_ssr_import_3__.message.useMessage is not a function or its return value is not iterable
  expected 0 to be greater than 20            // owner/Analytics、superadmin/SuperAdminRoles
  expected false to be true                   // admin/Settings
  TypeError: quotaOverview.reduce is not a function
  TypeError: (weeklyData || []).map is not a function
  TypeError: rawData.some is not a function
  ```
- **三个根因与修法**：
  ① antd 的 `message` mock **只给了静态方法**，缺 `useMessage` → 用 hook 版提示的页会炸
     → 改为 `{ ...messageSpies, useMessage: () => [messageSpies, null] }`；
  ② **各页面的接口形态不统一**：有的返回 `{ items, total }`，有的**直接返回数组**。
     我只给了对象 → 后者在渲染期抛 `reduce/map/some is not a function` → React 卸载整棵树
     → 页面变成空白（`textContent` 长度 0）→ 把通用载荷改成「**带数组方法的空对象**」
     （既能取字段，也能调 `map/reduce/some/...` 并拿到空结果）；
  ③ `admin/Settings` 用的是 `api/adminLogs` 的**具名函数导出**，而我那几个 mock 没登记进 registry
     → `anyCalled()` 看不到 → 把具名导出也交给 `resolveAll` 生成并登记。
- **复跑**（`32-R轮-MiscSuccess复跑.log`）：`39 passed | 1 failed`（只剩 ③）
- **三跑**（`33-R轮-MiscSuccess三跑.log`）：**40 passed** ✅

### R-3 | 全量 + 预算（日志 `34-R轮-全量预算.log`）

`Test Files 30 passed (30)` / `Tests 283 passed (283)` / `[warn-budget] ✅ 警告在预算内`
（act 102 / **jsdom 168**，+21 —— 21 个页面一次性渲染，属预期增长，按实测更新基线）

### R-4 | 用例库：**planned 清零**

- `FE-ADMIN-MISC`：001/003 转 done，`partial → covered`，`specFile` 登记为两个 spec
- `FE-DEV-MISC`：001 转 done，`partial → covered`
- `meta.stats`：`specFiles` 28 → **29**、`cases` 236 → **276**（实测 283 − 7）
- `Test_ALL_PLAN.md` §2.3 / §2.5 同步 283
- ✅ **用例库中已无 `planned` 项**（全库 33 个 suite 全部 covered/partial 收口）

## 轮次 R 小结

- 新增 1 个聚合 spec / **40 条用例**（21 页首次渲染 + 19 次"确实调了数据源"）
- 顺手修正了两处认知偏差：`UI-001~002` 其实早已覆盖；通用载荷必须兼容"对象/数组"两种接口形态
- 前端终态：**283 passed / 30 文件**，typecheck 0，预算通过；用例库登记 276 条组件用例、**无 planned**

---

## 轮次 S：变异检验补齐（证明本批新用例不是"空测试"）

### S-0 | 为什么做这件事

用例通过**不能证明**它真在测目标行为（断言可能太弱／测错对象／没跑到那段代码）。
项目原有 4 条变异规则（`FIX-1~4`）**全部来自 Recharge**，而本批（K~R 轮）改了 4 处真实缺陷 ——
**一条变异规则都没有**。按 `MEMORY.md` ㉒ 的约定，变异规则必须与代码形态同步维护，
否则检验形同虚设（`find` 命中多处时会安全跳过且静默）。

### S-1 | 先核对仓库真实状态（不依赖记忆）

| 检查项 | 结果 |
|---|---|
| 最近提交 | `088c4881` … 历史完整，工作区变更 **0** |
| 已入库留痕日志 | **101** 个 |
| 用例库 `planned` | **（无）** ✅ |
| `meta.stats.specFiles` | 登记 **29** vs 磁盘 **29** ✅ 一致 |
| 仍为 `partial` | `FE-LAYOUT` / `FE-ADMIN-ANALYTICS` / `FE-DEV-BILLING` —— 但**三者的用例全部 done**，`partial` 是"页面本身仍有未覆盖范围"的诚实标记，非遗留待办 |

> 小插曲：我第一次用 `-Filter '*.spec.tsx'` 数出 25 个，与登记 29 不符 —— 实际是**漏数了 4 个
> `.spec.ts`**（`client.spec.ts` / `permissions.spec.ts` / `useDevice.spec.ts` / `chartData.spec.ts`）。
> 用与自检脚本一致的规则（`/\.spec\.tsx?$/`）复核后一致。**教训：核对口径要与校验脚本一致，别自己另立一套。**

### S-2 | 新增 3 条变异规则并全量执行

| 变异 | 目标修复 | 变异手法 | 结论 |
|---|---|---|---|
| `FIX-5` | 账单首屏重复请求 | 把 `fetchBills()` 放回 `fetchData` → 回到"两个 effect 各拉一次" | ✅ 用例变红 |
| `FIX-6` | 负数金额符号在前 | 回到 `{amount>=0?'+':''}¥{amount.toFixed(2)}`（渲染成 `¥-12.50`） | ✅ 用例变红 |
| `FIX-7` | 一次性明文密钥 DOM 残留 | 创建成功弹窗去掉 `destroyOnHidden` | ✅ 用例变红 |

- **命令**：`npm run verify:fixes -- --log ../docs/test-logs/2026-09-17/35-S轮-变异检验.json`
  （日志 `35-S轮-变异检验.log`）
- **结果**：
  ```
  汇总：7 个有效 / 0 个空测试 / 0 个其它
  ```
  7 条全部"变异后变红、还原后变绿" → **本批新用例确实锁住了各自的行为**。
- **两条写规则的注意点**（已落到脚本注释）：
  ① `FIX-6` 的 `find` **不带行首缩进**，靠 `bill.` 前缀区分桌面表格列与移动端卡片两处；
  ② `FIX-7` 的 `destroyOnHidden` 在文件中出现 **2 次**（创建成功弹窗 + 查看弹窗），
     必须带上注释上下文才能唯一命中 —— 否则会被安全跳过（脚本对 `出现次数≠1` 直接 SKIPPED）。

> `check-test-warnings.mjs` 的 `_note` 修复属**脚本级**改动，vitest 用例覆盖不到，
> 不纳入变异检验；其证据是 K 轮日志 `06-K轮-基线重建保留note.log`。

### S-3 | 还原确认 + 终验（日志 `36-S轮-全量预算终验.log`）

- 变异脚本会改源文件，故**必须反查还原**：`git diff` 对 `Billing.tsx` / `Keys.tsx` 的差异数 = **0** ✅
  （工作区仅剩 `verify-fixes.mjs` 与 2 个新日志文件）
- `npm run typecheck` → 0 错误；`npm run test:budget` →
  **Test Files 30 passed (30) / Tests 283 passed (283)**，`[warn-budget] ✅ 警告在预算内`
  （act 102 / jsdom 168，与上轮一致 → **本轮零新警告**）

## 轮次 S 小结

- 变异检验规则 **4 → 7 条**，覆盖本批 3 处真实修复；**7/7 有效、0 空测试、0 跳过**
- 反查确认源码零残留、全量 283 passed、预算通过
- 无新增用例（本轮是"证明既有用例有效"的元验证）

---

## 轮次 C：三个 `partial` 套件收口（新增 9 条用例）

### C-0 | 目标与选点

三个仍标 `partial` 的套件（`FE-LAYOUT` / `FE-ADMIN-ANALYTICS` / `FE-DEV-BILLING`）里，
用例本身**全部已 done**，`partial` 是"页面仍有未覆盖范围"的标记。本轮把其中**真实可达但从未跑到**的分支补齐。

| 套件 | 补的分支 | 条数 |
|---|---|---|
| `FE-LAYOUT` | 移动端骨架（Drawer 抽屉）/ 桌面折叠 / 通知未读徽标与已读链路 | 4 |
| `FE-DEV-BILLING` | 普通用户升级引导 / 消费分布的空态与非空两分支 | 2 |
| `FE-ADMIN-ANALYTICS` | 趋势周期与天数、明细分页、弹窗内天数 —— 三条"改条件→带参重查" | 3 |

### C-1 | 首跑：5 条失败，每一处都指向一个真问题（日志 `37`）

**① `restoreMocks: true` 会清空工厂里的 mock 实现（波及既有用例）**
`vitest.config.ts` 开了 `restoreMocks`/`clearMocks` → `vi.mock` 工厂里写的 `mockResolvedValue`
在每个用例前被 `mockRestore()` 清掉，函数返回 `undefined`。
我顺手把通知 mock 的形状修正为与 `src/api/notification.ts` 一致（原先只有 `getUnreadCount: () => 0`，
缺 `getRecent`）后，**LAYOUT-008 立刻炸**：

```
TypeError: Cannot read properties of undefined (reading 'length')
 ❯ Layout src/components/Layout.tsx:545:40
   {recentNotifications.length === 0 ? (
```
原因：以前 `getRecent` **不是函数** → 调用即抛 → 被 Layout 的 `catch` 吞掉 → 状态保持初始 `[]`（于是"看似正常"）；
改成真 mock 后它返回 `undefined` → `setRecentNotifications(undefined)` → 打开面板即崩。
**修正**：mock 实现统一放到 `beforeEach` 里给（这是本项目的硬约定，工厂里给不算数）。

**② `MenuOutlined` 的 class 是 `anticon-menu`（不带 `-outlined`）**
`.anticon-menu-outlined` 查不到元素 → 汉堡按钮定位失败。（折叠/展开那对才是 `anticon-menu-fold` / `anticon-menu-unfold`。）

**③ 同页存在**多个同文案 Select**：`近7天` 命中 3 处**
`OverviewTab.tsx`（概览 Tab）/ `Analytics.tsx`（趋势 Tab 的卡片 extra）/ `RepoDetailModal.tsx`（弹窗）各有一个，
而 antd Tabs 会保留已挂载的面板 → `getByText('近7天')` 直接报 `Found multiple elements`。
**修正**：改为**限定作用域**查找（趋势 Tab 用激活面板 `.ant-tabs-tabpane-active`，弹窗用 `.ant-modal-wrap`）。

**④ 顺带发现一处真实不一致（已记 `knownDefects`）**
概览 Tab 里**也有一张同名「调用与收入趋势」卡片**，同样带周期 + 天数选择器，且**共用同一份状态**
（两处 `onPeriodChange`/`onDaysChange` 都接到 `setTrendPeriod`/`setTrendDays`）——
但周期选项文案不一样：概览是「按天」/「按小时」，趋势 Tab 是「按天统计」/「按小时统计」。
首跑正是"天数能改（状态共用）、周期文案对不上"才暴露的。
→ 记为 `FE-BUG-ANALYTICS-DUP-TREND-CARD`（status: open，建议统一文案或抽共享组件）。

**⑤ `mockResolvedValue` 覆盖后未还原**
BILLING-006 里我把 `getMonthlySummary` 改成空数据测空态，② 直接沿用同一 mock 测非空 → 仍是空数据。
**修正**：② 显式重新给出带 `by_repository` 的 mock。

### C-2 | 复跑 → 三跑（日志 `38` / `39`）

- `38`：`1 failed | 29 passed` —— 仅剩 `ANA-009` 第二处 pick 报
  `作用域内文案为「按天统计」的 Select 命中 0 个`（即 C-1④ 那条，改用激活面板后解决）
- `39`：**`3 passed (3)` / `30 passed (30)`** ✅

### C-3 | 全量 + 预算（日志 `40` / `41`）

```
Test Files  30 passed (30)
Tests      292 passed (292)
[warn-budget] ✅ 警告在预算内
    177  jsdom: getComputedStyle pseudo-elements
    102  act: update not wrapped in act
```
- 新增 9 条用例（283 → 292），**act 警告数完全未变（102）** → 零新 act 警告
- jsdom 类 168 → 177（更长的渲染链），远在容差（168×1.3≈219）内

### C-4 | 用例库同步

- `FE-LAYOUT` 4 条 + `FE-DEV-BILLING` 2 条 + `FE-ADMIN-ANALYTICS` 3 条 → 全部 `done`
- `FE-LAYOUT` / `FE-DEV-BILLING`：`partial → covered`；`FE-ADMIN-ANALYTICS` 仍 `partial`
  （理由已改写：子组件已抽出、数据链路已覆盖，剩 P1-4 拆分收尾 —— 即下一轮 B）
- `meta.stats.cases` 276 → **285**（实测 292 − 7 自检）；`specFiles` 29 不变
- `knownDefects` 新增 `FE-BUG-ANALYTICS-DUP-TREND-CARD`

### C-5 | 可复用结论（已入长期记忆）

1. **mock 实现必须写在 `beforeEach`**：本仓库 `restoreMocks: true` 会清掉工厂里的实现，
   而失败形态是"被 try/catch 吞掉的静默降级"，不会让测试变红 —— 只有把 `undefined` 喂进真组件才炸出来。
2. **同文案控件必须限定作用域**：`getByText` 在"多卡片/多 Tab/弹窗"页面几乎必踩；用
   `.ant-tabs-tabpane-active`（激活面板）或 `.ant-modal-wrap`（弹窗）做 scope。
3. **`MenuOutlined` 的 class 无 `-outlined` 后缀**（`anticon-menu`），而 `MenuFold/UnfoldOutlined` 有。

---

## 轮次 B：P1-4 前端巨型组件拆分（`admin/Analytics.tsx` 收尾）

### B-0 | 起点：拆分其实已经做了一大半

`Analytics.tsx` 已从 **964 行降到 477 行** —— `constants` / `chartData` / `repoDetailColumns` /
`OverviewTab` / `RepoDetailModal` 五块此前已抽出，只剩**两个 Tab 的内容**仍内联：
趋势 Tab（约 112 行）与仓库明细 Tab（约 70 行）。

### B-1 | 本轮动作

| 新文件（`src/pages/admin/analytics/`） | 行数 | 内容 |
|---|---|---|
| `TrendTab.tsx` | 134 | 调用/收入双 Y 轴折线图 + 趋势数据明细表 |
| `RepoDetailsTab.tsx` | 105 | 状态/排序筛选 + 明细表格 + 服务端分页 |
| `TrendControls.tsx` | 53 | 周期 + 天数选择器（**概览卡与趋势卡共用**） |

- `Analytics.tsx`：**477 → 309 行**，只剩状态、数据加载、Tab 组合（`loading/overview/trend/details/
  pagination/筛选/弹窗` 全套 state 与三个 effect 逐行保留，注释原样留下）
- **纯搬运、零行为改变**：唯一两处等价简化 ——
  ① 趋势图与明细表原先各自调用一次 `buildTrendChartData(trendData)`，现算一次复用；
  ② `TrendControls` 统一了概览卡/趋势卡的宽度取值（`compact` → 110，否则 120）
- ⚠️ 顺手修掉 C 轮记录的缺陷 `FE-BUG-ANALYTICS-DUP-TREND-CARD`：两处控件合并为同一个受控组件，
  option 常量上移到 `analytics/constants.ts`（`TREND_PERIOD_OPTIONS` / `TREND_DAYS_OPTIONS`），
  周期文案统一为「按小时统计」/「按天统计」；明细弹窗的天数选项也改为复用 `TREND_DAYS_OPTIONS`

### B-2 | 拆分暴露的回归（已修，且是"预期副作用"）

`ANA-005` 断言 `screen.getByText('按天统计')` 的原注释写着"该文本唯一" —— 文案统一后
**同一文本在页面上出现两处**（概览卡 + 趋势卡），`getByText` 立即报 multiple elements：

```
× TC-FE-ANA-005: 切到「趋势分析」展示趋势卡片与周期控件
```
**修正**：改为 `getAllByText('按天统计').length > 0`，并在用例注释里写清"为什么不再是唯一"
（否则后人会以为是断言写松了而改回去）。**这正说明 C 轮的用例网起了作用：拆分若有副作用当场变红。**

### B-3 | 验证（日志 `42` → `44`）

| 步骤 | 结果 |
|---|---|
| `42` Analytics 单文件（拆分后首跑） | `1 failed | 10 passed` —— 即 B-2 |
| `43` Analytics 单文件复跑 | **`11 passed`** ✅ |
| `44` `npm run test:budget` 全量 | **`Test Files 30 passed` / `Tests 292 passed`**，`[warn-budget] ✅`（act 102 / jsdom 177，**与拆分前完全一致**）|
| `45` 用例库自检 | `7 passed` ✅（`specFiles` 29 与实际一致）|
| `npm run typecheck` | 0 错误（pre-commit 钩子也会再跑一次）|

### B-4 | 用例库同步

- `FE-ADMIN-ANALYTICS`：`lines` 964 → **309**，`partial → covered`（`why` 重写为"拆分已完成"）
- `knownDefects` 的 `FE-BUG-ANALYTICS-DUP-TREND-CARD`：`open → fixed`（含修法与回归副作用说明）
- `meta.stats.sourceFiles` 51 → **54**（新增 3 个源文件）；`covered` 列表补登 5 个 `analytics/` 子组件
- `cases` 285 **不变**（本轮是重构，未新增用例）

### B-5 | 剩余 P1-4 巨型页（供后续排期）

`Recharge.tsx` 1752 行（最大）> `AdminLogs.tsx` 904 > `owner/Repos.tsx` 813 >
`ConsumptionDetails.tsx` 780 > `Reconciliation.tsx` 760 > `PricingConfig.tsx` 670。

---

## 轮次 B2：P1-4 再收口一个文件（`owner/Repos.tsx` 1059 → 704 行）

### B2-0 | 为什么选它

B 轮后剩余目标里，`Recharge.tsx`（1752 行）依赖面最大；`owner/Repos.tsx` 的 `EndpointsTab`
是**最后一块**（`repoColumns` / `BasicInfoTab` / `LimitsTab` 早已抽出）——
风险最小、且能**整文件收口**（把 P1-4 前端进度从 2/4 推到 3/4）。

### B2-1 | 动作

| 新文件（`src/pages/owner/repos/`） | 行数 | 内容 |
|---|---|---|
| `EndpointsTab.tsx` | 92 | 空态 + 添加按钮 +（移动端卡片 / 桌面端表格）两条分支 |
| `endpointColumns.tsx` | 91 | 端点表格列工厂 + `getMethodColor`（HTTP 方法配色） |

- `owner/Repos.tsx`：**813 → 704 行**，Tab 内容全部外置，只剩状态、数据加载与组合
- **为什么 `getMethodColor` 放在 `endpointColumns` 而不是 `EndpointsTab`**：
  它在页面里有**两处**消费方 —— 端点 Tab 与「仓库详情」抽屉里的端点表格
  （`Repos.tsx` 的 653/730 行附近）。放进 Tab 组件会让抽屉反向依赖 Tab（依赖方向错误）。
  形态上与既有 `repoColumns.tsx`（列工厂 + 状态映射）保持一致
- ⚠️ **一处顺带改进（非"零行为改变"）**：原先它是页面内定义的**闭包组件**
  （`const EndpointsTab = () => (...)`，每次父组件渲染都产生**新的组件类型**）→ React 会当成
  "另一个组件"而**整棵子树卸载重建**（移动端卡片上的滚动位置、后续若加输入框的状态都会丢）。
  现改为**模块级组件 + props 受控**，组件类型稳定。这是拆分带来的实际收益，不只是搬家

### B2-2 | 验证（日志 `46` / `47`）

| 步骤 | 结果 |
|---|---|
| `46` `src/pages/owner/Repos.spec.tsx` | **`9 passed`** ✅ |
| `47` `npm run test:budget` 全量 | **`Test Files 30 passed` / `Tests 292 passed`**，`[warn-budget] ✅`（act 102 / jsdom 177，**与拆分前完全一致**）|
| `npm run typecheck` | 0 错误（pre-commit 钩子再跑一次）|

### B2-3 | 用例库与文档同步

- `FE-OWNER-REPOS`：`lines` 1059 → **704**，`why` 补写拆分结果
- `meta.stats.sourceFiles` 54 → **56**；`covered` 列表补登 `owner/repos/` 全部 5 个模块

---

## 轮次 D：文档一致性巡检与同步（用户要求"及时更新所有文档"）

### D-1 | 巡检发现的漂移（都是"文档停在旧数字"）

| 文档 | 漂移 | 处理 |
|---|---|---|
| `docs/Test_ALL_PLAN.md` §2.3 / §2.5 | 仍写 `283 passed`、`195 条组件用例` | 改为 **292 passed / 285 条**，并写明推导（292 − 7 自检）|
| `docs/Test_ALL_PLAN.md` §2.2 | P1-4 目标行数写 `Analytics`(964)、`Repos`(1059/924) | 改为实测值：`Analytics` 309、`owner/Repos` 704、`admin/Repos` 589、`Recharge` 1752 (拆分中) |
| `docs/OPTIMIZATION_BACKLOG.md` §1 总表 | 写"前端 4/4 完成"且行数 964→477 / 2001→1744 | 改为 **前端 3/4**（Analytics 309 ✅ / admin-Repos 589 ✅ / owner-Repos 704 ✅ / Recharge 1752 🔄）|
| `docs/OPTIMIZATION_BACKLOG.md` §2.25 与 §3.1 | **同一文件两处数字互相矛盾**：§2.25 说 `owner/Repos` 1059→813、`admin/Repos` 924→589，§3.1 却说 1059→985、`admin/Repos` "📋 待开始（924 行）" | 以**磁盘实测**为准统一（owner/Repos 704、admin/Repos 589 已完成），§3.1 标题同步为"前端 3/4" |
| `docs/OPTIMIZATION_BACKLOG.md` §3.1 建议做法 | 写"前端 142 条" | 改为 **292 条** |

### D-2 | 顺带核对的（确认无需改）

- `docs/regression-2026-09-16.md` 的 `144 passed` 是**当日回归报告的时点数据**，属历史证据 →
  **不回填**（改写历史报告会让"当时到底验了什么"失真）
- `docs/test-log-2026-09-17.md` 开头的"环境基线：202 passed / 21 文件"同理，是当日**起点**记录

### D-3 | 结论：数字类断言的"单一信源"原则

本轮漂移的共同成因：**同一事实（用例数 / 行数）散落在多处文档里，各自手写**。
约定：① 用例数只以 `npm run test:unit` 的实测输出为准，登记处必须写出推导；
② 行数一律以磁盘实测为准，禁止"拆分前 → 估算值"；
③ 改完代码要**同一轮**同步 `frontend_cases.json` / `Test_ALL_PLAN.md` / `OPTIMIZATION_BACKLOG.md` 三处。

---

## 轮次 B3：最后一个 P1-4 目标 `Recharge.tsx` 起步（2001 → 1697 行）

### B3-0 | 为什么先切"扫码轮询"这一块

`Recharge.tsx` 1752 行里最大的是支付弹窗与支付结果轮询，依赖面很宽。
按 §2.25 的既定策略（**按依赖数量从少到多切**），扫码轮询这一簇最内聚：
`startQrcodePolling`(8 次递进间隔) + `handleQrcodePaymentSuccess` + `stopQrcodePolling`
+ 那个"是否继续"的 ref，共 4 个成员，入参 6 个。

| 新文件（`src/pages/developer/recharge/`） | 行数 | 内容 |
|---|---|---|
| `useQrcodePolling.ts` | 131 | 扫码轮询 start/stop、成功回调、是否继续的 ref |

`Recharge.tsx`：**1752 → 1697 行**（`recharge/` 目录累计 8 个模块 / 697 行）。

### B3-1 | 两个设计约束（都是为了不破坏既有防线）

**① `qrcodePollingRef` 必须交还给父组件**
父组件的**卸载清理 effect** 里有一句 `qrcodePollingRef.current = false`，
而**变异规则 `FIX-3` 正是盯这一行**（其 `find` 是三行组合）。若把 ref 留在 hook 内部，
这句清理就得整体重写 → `FIX-3` 的 `find` 命中 0 次 → 脚本打印"❌ 跳过"但**退出码仍为 0**，
很容易让人以为"还在验证"。故：hook **导出** `qrcodePollingRef`，父组件清理**一字未动**。

**② hook 的调用点必须在 `closePayWindow` 定义之后**
hook 入参在调用时**立即求值**，而 `closePayWindow` 是组件内的 `const`（定义在 1234 行附近）
→ 调用点放前面会直接抛 TDZ。这也解释了为什么 hook 调用写在了文件中部而不是状态声明区。

### B3-2 | 变异规则随代码搬迁（本轮最关键的一条纪律）

`FIX-2` 的靶子（`stopQrcodePolling` 函体）已随代码搬进 `useQrcodePolling.ts`，
故把规则的 `file` 一并改指新路径，并**逐条复验**（日志 `50` / `51`）：

```
[FIX-2] 变异文件: src\pages\developer\recharge\useQrcodePolling.ts
  变异后 🔴 失败 · 还原后 🟢 通过 · 结论 ✅ 用例有效
[FIX-3] 变异文件: src\pages\developer\Recharge.tsx
  变异后 🔴 失败 · 还原后 🟢 通过 · 结论 ✅ 用例有效
```

**两条都仍是"有效"，0 跳过** —— 若只搬代码不改规则，`FIX-2` 会静默失效。

### B3-3 | 过程留痕：两次失败都留了原始输出

| 日志 | 现象 | 处理 |
|---|---|---|
| `48` | `typecheck` 报 `useQrcodePolling.ts(109,5): error TS2304: Cannot find name 'setPaySuccess'` | 抽 hook 时漏传一个 setter（成功回调里 `setPaySuccess(true)`）→ 补入参并复跑 |
| `49` | Recharge 用例 **`18 passed`** ✅、typecheck 0 | — |

⚠️ 复跑日志里出现的 `Warning: [antd: message] Static function can not consume context`
**不是本轮引入的**：核查历史日志后确认，Recharge 单文件跑一直是 **11 条**
（`2026-09-16/02-act包裹后-Recharge单文件96条.log`、`55-G轮五跑-Recharge标签.log` 同为 11）。
该文本未被预算脚本分类（`classify` 无对应规则），故也从未计入预算。

### B3-4 | 全量 + 预算（日志 `52`）

```
Test Files  30 passed (30)
Tests      292 passed (292)
[warn-budget] ✅ 警告在预算内
    177  jsdom: getComputedStyle pseudo-elements
    102  act: update not wrapped in act
```
与拆分前**完全一致** → 本次搬迁零行为改变。

### B3-5 | 文档同步（四处）

- `frontend_cases.json`：`FE-DEV-RECHARGE` 的 `lines` 2001 → **1697**、`why` 补拆分进度与
  "变异规则随代码搬迁"提醒；`covered` 补登 `recharge/useQrcodePolling.ts`；`sourceFiles` 56 → **57**
- `Test_ALL_PLAN.md` §2.2：`Recharge`(2001→**1697**，拆分中)
- `OPTIMIZATION_BACKLOG.md` §1 / §2.25（表 + 模块清单）/ §3.1：同步行数、模块数（8 个 / 697 行）、
  剩余项，并记下"为何 `qrcodePollingRef` 必须交还父组件"

### B3-6 | `Recharge.tsx` 剩余（下一轮）

支付结果轮询（`startPaymentPoll` / `stopPaymentPoll` / `pollPaymentStatus`，约 230 行，
依赖 10+ 个状态）→ `usePaymentPolling`；其后是支付弹窗的 JSX 分块。
**盯防点**：`FIX-1`（`rechargeLogger`）与 `FIX-4`（`PaymentSummary`）分别落在已抽出的文件中，
本轮未触碰；下一轮若动 `startPaymentPoll`，需检查是否与 `FIX-3` 的三行 `find` 交叠。

---

## 轮次 B4：`Recharge.tsx` 继续拆 —— 支付结果轮询（1697 → 1667 行）

### B4-1 | 动作

| 新文件（`src/pages/developer/recharge/`） | 行数 | 内容 |
|---|---|---|
| `usePaymentPolling.ts` | 78 | `startPaymentPoll`（每 3 秒一次、只在"弹窗打开且未成功"时真查）/ `stopPaymentPoll` / 定时器 ref |

`Recharge.tsx` **1697 → 1667**；`recharge/` 累计 **9 个模块 / 775 行**。
沿用 B3 的做法：`paymentPollIntervalRef` **交还父组件**，因为父组件卸载清理里的
`if (paymentPollIntervalRef.current) { clearInterval(...) }` 与 `qrcodePollingRef.current = false`
一起构成 **FIX-3 的 find**（三行组合）。

### B4-2 | ⚠️ 首跑 18 条用例全红：`Rendered fewer hooks than expected`（日志 `53`）

```
Tests  18 failed (18)
Error: Rendered fewer hooks than expected. This may be caused by an accidental early return statement.
```

**根因**：本组件里有个**早退分支**

```tsx
if (loading) {
  return (<div className={styles.loading}>…加载套餐列表…</div>)
}
```

我把 hook 调用放在了它**之后**（因为要满足"在 `handleRefreshStatus` 定义之后"的 TDZ 约束）。
于是：首屏 `loading=true` → 早退 → hook 没调用；数据到达后重渲染 → hook 才调用 →
hook 数量与上次不一致 → React 直接抛错。

**修正**：把 `usePaymentPolling(...)` 移到 `if (loading)` **之前**（仍在其依赖
`handleRefreshStatus` 之后）。**结论：hook 调用点有两条硬约束，必须同时满足** ——

| 约束 | 原因 |
|---|---|
| 在其依赖的 const **之后** | hook 入参在调用时立即求值；而 `closePayWindow` / `handleRefreshStatus` 都是组件内的 const → 放前面抛 **TDZ** |
| 在 `if (loading) return …` 早退分支 **之前** | 否则首屏提前 return，hook 时有时无 → **Rendered fewer hooks than expected** |

（两条约束已写入两处代码注释与 `docs/OPTIMIZATION_BACKLOG.md` §2.25。）

### B4-3 | 复跑与变异复验（日志 `54` / `55` / `56`）

- `54`：Recharge 用例 **`18 passed`** ✅（`Recharge.tsx` 1667 行）、typecheck 0
- `55` `FIX-3`（靶子在 `Recharge.tsx` 的清理 effect，本轮**一字未动**）→ ✅ 用例有效
- `56` `FIX-2`（靶子已随 B3 搬入 `useQrcodePolling.ts`）→ ✅ 用例有效
- 两条均 **0 跳过** —— 说明"交还 ref + 不重写清理"的策略奏效

### B4-4 | 全量 + 预算（日志 `57`）

```
Test Files  30 passed (30)
Tests      292 passed (292)
[warn-budget] ✅ 警告在预算内
    177  jsdom: getComputedStyle pseudo-elements
    102  act: update not wrapped in act
```
与拆分前**完全一致** → 零行为改变。

### B4-5 | 文档同步（四处）

`frontend_cases.json`（`lines` 1697 → **1667**、`why` 进度、`covered` 补登新 hook、`sourceFiles` 57 → **58**）、
`Test_ALL_PLAN.md` §2.2、`OPTIMIZATION_BACKLOG.md` §1 / §2.25（表 + 模块清单 + **hook 调用点双重约束**）/ §3.1、
本文件（B4 轮）。

### B4-6 | `Recharge.tsx` 剩余

只剩**支付弹窗的 JSX 分块**（`payModalVisible` 那棵子树，含扫码二维码区、跳转支付区、
成功大界面等分支）。它的依赖是"一堆 state + 上面几个 handler"，属于**最后再切**的那类；
`FIX-1` / `FIX-4` 的靶子都在已抽出的文件里，不受影响。

---

## 轮次 B5：`Recharge.tsx` 最后一块 JSX —— 支付弹窗（1667 → 1571 行）

### B5-1 | 动作

| 新文件 | 行数 | 内容 |
|---|---|---|
| `recharge/components/PaymentModal.tsx` | 188 | 支付弹窗三态（确认中 / 成功大界面 / 下单信息）+ 二维码区 + 跳转支付区 + 刷新/取消 |

`Recharge.tsx` **1667 → 1571**；`recharge/` 累计 **10 个模块 / 963 行**。
这是 Recharge 里**依赖最多**的一块（17 个 props），按 §2.25「按依赖数量从少到多切」的策略**放最后**。

**为保持组件纯展示**（与 `PackageCard` / `PaymentSummary` / `PaySuccessView` 一致）：
原先内联在「取消订单」按钮上的动作逻辑（`stopQrcodePolling` → `cancelPayment` → 提示 → 关弹窗）
**上移到页面**成为 `handleCancelOrder`，组件只收 `onCancelOrder`。

### B5-2 | 过程中的两处小坑（都留了原始输出）

| 现象 | 处理 |
|---|---|
| `typecheck`：`PaymentModal.tsx(13,30): error TS2307: Cannot find module '../../../api/payment'` | 新文件在 `recharge/components/` 下，回退层级应为**四级** → 对照既有 `PaySuccessView.tsx` 的 `'../../../../api/payment'` 修正 |
| `Descriptions` 在 `Recharge.tsx` 里仅剩导入语句（全文出现 1 处） | 随弹窗一起抽走后已成死导入 → 从 antd 导入列表移除 |

> 复用的判断法：**用 `Select-String` 数每个可疑导入在整个文件里的出现次数**，
> 只剩 1 处（导入行本身）即为死导入 —— 比逐个肉眼查快且可靠。

### B5-3 | 验证（日志 `58` / `59` / `60`）

- `58`：Recharge 用例 **`18 passed`** ✅、typecheck 0
- `59`：`FIX-3`（靶子在 `Recharge.tsx` 清理 effect，本轮**未触碰该区域**）→
  变异后 🔴 / 还原后 🟢 → ✅ 有效、0 跳过
- `60`：全量 **`Test Files 30 passed` / `Tests 292 passed`**，`[warn-budget] ✅`
  （act 102 / jsdom 177，**与拆分前完全一致**）

### B5-4 | 文档同步（四处）

`frontend_cases.json`（`lines` 1667 → **1571**、`covered` 补登 `PaymentModal`、`sourceFiles` 58 → **59**、
`why` 改为"剩页面骨架 / 支付方式卡片 / 各事件处理器"）、`Test_ALL_PLAN.md` §2.2、
`OPTIMIZATION_BACKLOG.md` §1 / §2.25（表 + 模块清单）/ §3.1、本文件（B5 轮）。

### B5-5 | `Recharge.tsx` 的 P1-4 收口评估（重要结论）

**已外置的部分**：2 个纯逻辑模块（`constants` / `rechargeLogger`）+ 3 个 hook
（`useRechargeData` / `useQrcodePolling` / `usePaymentPolling`）+ 1 个会话模块（`paymentSession`）
+ 4 个展示组件（`PackageCard` / `PaymentSummary` / `PaySuccessView` / `PaymentModal`），
**合计 963 行**，页面 **2001 → 1571 行（-21.5%）**。

**剩余 1571 行的性质已经变了** —— 不再是"可独立切出的视图/逻辑块"，而是：
① 十余个事件处理器（支付回调、storage/focus/visibility/message 四类监听、下单、开支付页…）；
② 支付方式卡片与套餐区骨架；③ 大量状态与 ref 的编排。

**结论**：继续搬运式硬拆的收益递减而风险上升（这块正是 §2.25 所说"状态集中在组件内部"的典型）。
建议把 `Recharge` 的 P1-4 标注为「**已抽尽可独立的部分**」；若还想降行数，
应走**行为重构**（把支付流程收敛为状态机 / 更细的 hook 组合）而非搬家式拆分 —— 那是另一个议题。
