# 测试运行日志 — 2026-09-16（D/A/B 优化轮）

> 按 2026-09-16 回归报告的要求建立：**每条记录含 案例ID / 目标 / 命令与参数 / 过程 / 结果**，
> 失败必须保留原始报错，不许只记"通过/失败"。
> 本文件为**追加式**日志：每轮验证先写计划与参数，跑完立即回填结果。

环境基线：commit `2831f2e`，前端 144 passed / 14 spec，typecheck 0，警告预算 act 96 + jsdom 61。

---

## 轮次 D：用例库 JSON 合法性冒烟校验

### D-1 | 目标：给 `frontend_cases.json` 建立"损坏即变红"的测试防线

- **背景**：该 JSON 曾因两处未转义英文双引号整体损坏（`require()` SyntaxError），且无任何测试消费、损坏无警报。
- **方案**：新建 `web/tests/cases/frontend_cases.spec.ts`（TC-FE-CASES-001~007），
  覆盖：① JSON 可解析 ② 顶层结构 ③ suite 必填字段与合法 status ④ 全库用例 id 唯一
  ⑤ suite.file 真实存在 ⑥ covered/partial 的 specFile 存在 ⑦ `meta.stats.specFiles` 与磁盘 spec 数一致。

### D-2 | 过程记录：自检 spec 没有被收集

- **命令**：`npm run test:unit`（全量）
- **参数**：vitest 3.x，`vitest.config.ts`
- **结果**：`Test Files 14 passed (14) / Tests 144 passed` —— 与改动前完全一致，**自检 spec 未被收集**。
- **定位**：单跑 `npx vitest run tests/cases/frontend_cases.spec.ts` → `No test files found, exiting with code 1`。
- **根因**：`vitest.config.ts` 的 `test.include` 写死为 `['src/**/*.{test,spec}.{ts,tsx}']`（此前读 vite.config.ts 时未见 test 段，实际配置在独立的 vitest.config.ts，**侦察遗漏**）。
- **修复**：include 追加 `'tests/**/*.{test,spec}.{ts,tsx}'`（e2e/ 仍被 exclude 排除）。
- **状态**：✅ 修复后自检可收集。

### D-3 | 过程记录：自检首跑抓到一个真实数据点（防线立即生效）

- **命令**：`npx vitest run tests/cases/frontend_cases.spec.ts`
- **结果**：`7 tests | 1 failed`
- **失败原文**：
  ```
  AssertionError: suite FE-ADMIN-MISC 缺被测文件 file: expected undefined to be truthy
  ```
- **分析**：`FE-ADMIN-MISC` 是聚合型 suite（覆盖其余 21 个页面骨架用例），**没有单一被测文件**，缺 file 属合理 —— 是校验规则过严，不是数据错误。
- **修复**：CASES-003 放宽为"file 可选；一旦提供，由 CASES-005 校验其存在"。
- **状态**：✅ 待复跑确认（见 D-4）。

### D-4 | 复跑

- **命令**：`npx vitest run src/components/Layout.spec.tsx src/pages/admin/Analytics.spec.tsx tests/cases/frontend_cases.spec.ts`
- **结果**：cases 自检 `7 passed`（从三文件混合输出确认无 FAIL 项；终态以本轮末尾全量为准）。
- **状态**：✅ 通过。

---

## 轮次 A：statusMap 三处重复收敛（含一处重要发现）

### A-1 | 侦察发现：三处 statusMap **不是**简单重复

| 位置 | approved 文案 | 判定 |
|---|---|---|
| `admin/repos/repoColumns.tsx` | 已审核 | admin 视角（关心"审没审过"） |
| `admin/analytics/constants.ts` | statusText: 已审核 | 同 admin 视角（消费形状不同：color/text 两个拆开的 map） |
| `owner/repos/repoColumns.tsx` | **已审核（待上线）** | owner 视角（提醒"还差你上线"） |
| `owner/Dashboard.tsx` | **已审核（待上线）** | 同 owner 视角；且 fallback 为 `status \|\| '未知'` |

- **决策**：**不能盲目统一成一个 map** —— admin/owner 文案差异是业务语义。
  方案：新建 `src/config/repoStatus.ts`，导出 `REPO_STATUS_MAPS`（admin/owner 两个变体）
  与 `repoStatusConfig(status, variant)`（纯数据、不含 JSX，fallback 语义留在调用点）。
  `analytics/constants.ts` 的拆分形状（statusColors/statusText）保留不动，值一致。

### A-2 | 改造记录

- `src/config/repoStatus.ts` 新建（含"不要顺手统一"的警示注释）；
- `admin/repos/repoColumns.tsx` `getStatusTag` → `repoStatusConfig(status, 'admin')`；
- `owner/repos/repoColumns.tsx` 状态列 → `repoStatusConfig(status, 'owner')`；
- `owner/Dashboard.tsx` 状态列 → `repoStatusConfig(status || '未知', 'owner')`（保留原兜底语义）；
- 三处各自补 import。

### A-3 | 验证

- **命令**：`npm run typecheck` → `0 错误`；
- **命令**：`npm run test:unit` → `Test Files 14 passed / Tests 144 passed`（含 D 自检 7 条；
  ⚠️ 注意口径：自检 spec 位于 tests/ 不计入 `meta.stats.specFiles=14`（那是 src 下口径），全量 Test Files 因包含自检实为 15 个文件、151 条用例）。
- **状态**：✅ 通过。

---

## 轮次 B：补尾巴用例（ANA-008 / LAYOUT-007~008）

### B-1 | TC-FE-ANA-008（明细状态筛选与排序带参重查）

- **目标**：切「仓库明细」Tab 后，状态筛选（Select）→ `status`、排序字段 → `sort_by`、
  排序次序 → `sort_order` 任一变化都必须触发 `getRepoDetails` 带参重查（effect 依赖已声明）。
- **首跑命令**：`npx vitest run src/pages/admin/Analytics.spec.tsx`
- **首跑结果**：`1 failed` —— `Unable to find role="option" and name "待审核"`。
- **排查**：页面共 5 个 antd Select（概览 Tab 2 个 + 明细 Tab 3 个，antd Tabs 默认保留已渲染 Tab）。
  用"当前可见文本"定位 Select 成功，但**点击 `.ant-select-selection-search-input` 不触发下拉打开**。
- **修复 1**：改为点击 `.ant-select-selector`（antd 的 mousedown 目标）。
- **复跑结果**：仍 `Unable to find role="option" and name "待审核"` → 下拉仍未打开，**未通过，继续排查**。
- **下一步计划**：改用 antd 官方测试写法 `fireEvent.mouseDown(selector)` 触发打开；
  若仍失败，检查 userEvent 与 antd v5 的 pointer-events 兼容（setup.ts 是否有 mock）。

### B-2 | TC-FE-LAYOUT-007/008（组件级渲染与登出）

- **首跑命令**：`npx vitest run src/components/Layout.spec.tsx`（与上同批）
- **首跑结果**：`2 failed`
- **失败原文**：
  ```
  → vi.mocked(...).mockResolvedValue is not a function
  ```
- **根因（有价值）**：`Layout.spec.tsx` 原有的 `vi.mock('../../api/auth', ...)` 路径从
  `src/components/` 解析到**不存在的模块**（应为 `'../api/auth'`）→ **mock 从未生效**。
  001~006 只测纯函数 `getMenuItems`、不碰网络层，所以这个潜伏问题从未暴露；
  007/008 一旦渲染组件、碰到真模块，立即炸出。
- **修复**：mock 路径改为 `'../api/auth'` / `'../api/notification'`（附注释说明原因）。
- **复跑**：Layout `8 passed`（fin5 批次三文件汇总中 Layout 无 FAIL 项，stderr 出现
  "Failed to parse URL from /health /auth/me" 为 Layout 内相对 URL fetch 的预期报错、被 catch，不影响断言）。
- **状态**：✅ 通过（细节复确认见全量）。

### B-3 | ANA-008 第二轮修复（mouseDown 触发）

- **改动**：`pick()` 改为 `fireEvent.mouseDown(selector)` 打开下拉 + `user.click(option)` 选中。
- **结果**：待执行（见下一条记录）。

---

### B-3 | ANA-008 第二轮修复（mouseDown 触发）→ 仍失败

- **改动**：`pick()` 改用 `fireEvent.mouseDown(selector)` 打开下拉（antd 官方测试写法）。
- **结果**：仍 `Unable to find role="option" and name "待审核"`（原始输出：`22-ANA008-motion关闭后.log`）。
- **追加实验**：`renderPage()` 包 `ConfigProvider motion:false`（与 renderWithProviders 对齐）→ 仍失败。

### B-4 | ANA-008 深挖：DOM dump 定位真因（**不猜，看 DOM**）

- **手段**：临时在 `pick()` 里 dump `document.querySelectorAll('.ant-select-dropdown')` 的
  className / aria-expanded / option 数与文本（原始输出：`21-…DOMdump.log`、`23-…option渲染诊断.log`）。
- **发现**：
  1. 下拉**已打开**（`aria-expanded: true`、弹层存在）；
  2. className 停在 `ant-slide-up-appear-prepare`（**motion 动画帧卡住**，jsdom 无 transitionend）；
  3. **option 渲染完全正常**（5 个：全部状态/已上线/待审核/已下线/已拒绝）。
- **结论**：`findByRole('option')` 找不到不是"没渲染"，而是 **RTL 的可访问性判定**：
  弹层容器卡在动画帧被视为不可见（`ConfigProvider motion:false` 的 token **禁不掉** rc-motion 的 slide-up）。
- **修法**：绕开可访问性查询 —— `mouseDown` 打开 → 在未 hidden 的弹层里按文本找
  `.ant-select-item-option` → `fireEvent.click`（不做可见性检查，onSelect 照常触发）。
- **复跑**：`Analytics.spec 8 passed`（`24-ANA008-DOM选option.log`）✅

### B-5 | LAYOUT-007/008 复跑 ✅

mock 路径修复后 `Layout.spec 8 passed`（6 条纯函数 + 2 条组件级）。
⚠️ stderr 中 "Failed to parse URL from /health" 是 Layout 内相对 URL fetch 被预期的报错（已 catch），不影响断言。

### D-5 | FE-CASES suite 登记用例库 + 自检复跑

- 用例库新增 `FE-CASES`（P0，7 条 done）→ suites 25 / 登记用例 149；
- `meta.stats.note` 更新（vitest 151 = 144 组件 + 7 自检；specFiles=14 为 src 口径）；
- 自检复跑 `7 passed`（`27-自检复跑含新suite.log`）—— 自检新 suite 自身也被自己校验 ✅。

### 全量终验

- **命令**：`npm run typecheck`（0 错误）+ `npm run test:budget`
- **首次**：❌ 预算超标 —— `新增警告种类：react-router: future flag（2 次）`：
  **我在 renderLayout 里新写的 MemoryRouter 漏加 future flags** —— 已清零种类 2 条即拦截，
  预算机制连续第二天抓到同一类疏忽 ✅；
- **修复后**：✅ 在预算内，`act 102 / jsdom 62`（较 96/61 的 +6/+1 为新用例的合理增量）→ 基线同步。

---

## 本轮结论（D/A/B）

| 项 | 结果 |
|---|---|
| D 用例库自检 | 新增 `TC-FE-CASES-001~007`（7 passed），JSON 损坏/结构漂移今后直接变红 |
| A statusMap 收敛 | `src/config/repoStatus.ts`（admin/owner 双变体），3 调用点接入，analytics 拆分形状保留 |
| B 尾巴用例 | `ANA-008` ✅（antd Select 交互三步深挖）、`LAYOUT-007/008` ✅ —— **P0/P1 planned 清零** |
| 全量 | vitest **151 passed**（15 文件）· typecheck 0 · 预算内（act 102/jsdom 62，基线已同步） |

## 约定：原始输出归档

**所有测试原始输出一律写入 `docs/test-logs/<日期>/<序号>-<说明>.log`**（TEMP 会丢、不进 git）。
本日归档 27 份（01~27），日志正文每条记录可回查对应原始文件。

---

## 轮次 E：P2/P3 层（路由守卫 + 21 页骨架）

**计划（先写后测）**：
- E-1 | `TC-FE-UI-002` 路由守卫：未登录访问受保护路由 → `/login`；越权（developer 访问 /admin）→ 按角色重定向；`user_type` 未知放行（V6.0 行为）。命令：新建 `src/App.spec.tsx`，渲染 `<App/>` + MemoryRouter 定向。
- E-2 | `TC-FE-UI-001` 按 props 渲染（App 侧 defaultPath 重定向）。
- E-3 | `TC-FE-ADMINMISC/DEVMISC` 骨架：21 页参数化「报错不白屏」（API 全 reject → 页面骨架仍在）+ 尽可能的首屏成功渲染。命令：新建 `src/pages/misc/MiscPages.spec.tsx`（it.each）。
- 验证：typecheck + 全量 budget；结果回填本文件；原始输出归档 `docs/test-logs/2026-09-16/28+`。

**侦察参数**：FE-ADMIN-MISC 8 页（Users/Settings/RechargeRecords/PlatformAccounts/Dashboard/ChannelSummary/AdminMonthlyBills/DevTools）；FE-DEV-MISC 13 页（developer/Dashboard·Usage·Repos、user/UserDashboard、notifications、owner/Dashboard·Analytics·Settlement、PaymentSuccess、superadmin×4）。
守卫逻辑（App.tsx L49-100）：未登录→/login；`allowedUserTypes` 不含 user_type → `getDefaultPath`；user_type undefined → 放行。

## E 轮结果回填

### E-1/E-2 | 路由守卫（App.spec，TC-FE-UI-001/002）✅ 3 passed

未登录→/login ✅；developer 访问 /admin → 按角色重定向 ✅；任意未知路径兜底 /login ✅。

### E-3 | 骨架 21 页（MiscPages.spec，ADMINMISC-002 / DEVMISC-002）✅ 21 passed

### E 轮踩坑记录（4 次，全部留有原始输出）

| # | 坑 | 修法 |
|---|---|---|
| 1 | **vi.mock 工厂 hoisting**：工厂引用顶层 `const rejectedModule` → `Cannot access before initialization`（28 号日志） | `vi.hoisted(() => ({ rejectedModule }))` |
| 2 | **Proxy mock 挂死**：`...rejectedModule()` 展开触发 ownKeys（空）→ 具名导出丢失（29 号）→ 改工厂直接返回 Proxy → **vitest 校验 mock 导出时递归卡死**（30 号，14 行停滞 3.5 分钟） | **弃用 Proxy**，按 grep 成员清单显式 mock（api 对象=空对象，调用 undefined → TypeError → 页面 catch 照测"不白屏"；纯数据导出空 obj） |
| 3 | **JSX 注释位置**：`&& ( {/*注释*/} <Card/> )` 的 `{` 被解析为对象字面量 → esbuild Transform failed（37 号） | 注释移到语句位置 |
| 4 | **预算连环拦截**：① CASES-007 抓到 specFiles 14≠16（自检生效）；② 拦截 UserDashboard 残留 `Card.bordered`（21 页首次被渲染即暴露无测试时期的漏网） | specFiles→16；bordered→variant="borderless" |

### E 轮终态

vitest **175 passed**（17 个 spec 文件 = 14 组件 + 1 自检 + 1 守卫 + 1 骨架）
typecheck 0 · 预算内 act 102 / jsdom 79（+17 为骨架测试增量，基线同步）
用例库：FE-UI → covered、ADMINMISC/DEVMISC-002 → done（001/003 首屏成功与关键操作留 planned）

### E-4 | 用户质疑："为何没看到 pass 字样" → budget 脚本补摘要透传

- **用户观察**：`test:budget` 输出只有警告统计、没有 pass —— 质疑"你确定测试通过了？"
- **核实**：budget 脚本逻辑上 vitest 非零退出会先报"单元测试未通过"，但**通过时没有任何摘要展示**
  → "通过"缺可见证据，是脚本展示层缺陷。
- **修复**：`check-test-warnings.mjs` 增加两段：
  ① vitest 非零退出时打印 exit code + 前 20 条 FAIL 行（不再只有一句笼统提示）；
  ② 通过时透传 `[vitest] Test Files / Tests / Duration` 摘要行。
- **终验（前台直跑，EXIT=0）**：
  ```
  [vitest] Test Files  17 passed (17)
  [vitest] Tests  178 passed (178)
  [vitest] Duration  42.22s
  [warn-budget] ✅ 警告在预算内
      102  act: update not wrapped in act
       79  jsdom: getComputedStyle pseudo-elements
  ```
- **过程杂项**：全量 budget 曾两次"输出空/截断"（Out-File 与 watch 服务超时交互 +
  多次启动遗留的 13 个僵尸 node worker 互相干扰）→ 清理僵尸进程、改 Start-Process 后台 +
  最终前台直跑拿到证据。（原始输出：40~42 号日志，41 号为被污染样本保留备查。）

---

## 轮次 F：P3 共享组件（FE-SHARED-UI）

**计划（先写后测）**：
- F-1 | `ResponsiveTable`：按 props 渲染（columns/data 透传、空数据、移动/桌面分支）。
- F-2 | `RepoLogo`：logo_url 有无、repoType 兜底。
- F-3 | `DynamicForm`：按 schema 渲染控件、空 schema。
- F-4 | `EndpointList`：列表渲染与空态。
- 命令：新建 `src/components/shared/SharedComponents.spec.tsx`（参数化）+ 逐组件断言；
- 验证：typecheck + 全量 budget；结果回填本节；原始输出归档 `docs/test-logs/2026-09-16/43+`。
- 前置侦察：读 4 个组件的 Props 定义与使用方式。

### F 轮结果回填（P3 共享组件）

- **首跑 6 失败**（43 号）→ 逐个定位修复：
  RT-004 改用 `window.innerWidth=375` 切移动端（useDevice 读 innerWidth，零 mock）；
  LOGO-002 背景实为 linear-gradient → 断言包含色值；LOGO-003 未知类型改为不崩断言；
  FORM-001/002 的 label 无 htmlFor → 改「参数名文本向上找 wrapper 再取控件」；
  EP-002 同端点多分组出现 → getAllByText。
- **三跑 1 失败**：「查看」按钮找不到 → **DOM dump 揭示 accessible name 是「查 看」**
  —— antd Button 对两字中文自动插空格（`/查\s*看/` 修，45~48 号日志）。
- **四跑**：`12 passed` ✅（49 号）。
- **CASES-006 再次拦截**：FE-SHARED-UI 登记两个 specFile 用逗号分隔 → 校验按整体路径查
  失败 → 升级校验支持逗号分隔多 specFile ✅。
- **终验**：vitest **190 passed / 18 文件**，EXIT=0，预算内（act 102 / jsdom 82，基线同步）。
  用例库：FE-SHARED-UI → covered（+12 条）、meta.stats 同步（specFiles 17 / cases 161）。

---

## 轮次 G：用户实测两个矛盾的修复（环境标签 + 余额不一致）

**用户报告（附图）**：① 账单中心显示「生产环境/真实账户」而顶栏是「测试环境·SIMULATION」；
② 账单中心余额 ¥20、开发者工作台 ¥0（同一用户 test2，已领 20 元试用金）。

**根因（两个独立缺陷）**：
- ① **环境标签读错数据源**：Billing 用 `account.mock_mode`（**支付模拟模式**）判断环境，
  而顶栏用 `/health` 的 `billing_environment` —— 二者是不同概念（9-15 已解耦），
  simulation 下接真实支付网关时 mock_mode=false → 页面显示「生产环境/真实账户」；
- ② **Dashboard 故障不隔离**：`fetchData` 用裸 `Promise.all`，quota 侧任一接口失败
  会把 account 一起清成 null → 余额 ¥0 +「模拟」标签。

**修复**：
1. 抽 `src/hooks/useEnvInfo.ts`（/health 的环境信息）→ Layout / Billing / Dashboard 三处同源；
2. Billing 两枚标签改 billing_environment；Dashboard 余额标签改 billing_environment；
   Recharge 的支付模式标签文案去「生产环境」字样（改为「模拟支付/真实支付通道（非模拟）」）；
3. Dashboard 三个数据源各自 `.catch(() => null)`（单源失败只降级该卡片）。

**用例**（新建 Dashboard.spec 4 条 + Billing.spec 2 条）：
- DASH-001 单源失败不影响余额 / DASH-002 全挂不白屏 / DASH-003 正常路径 / **DASH-004 标签同源**；
- BILL-001 simulation 不得出现「生产环境/真实账户」/ BILL-002 production 正确显示。

**过程踩坑**（50~52 号日志）：antd `Statistic` 把 20 与 .00 拆成两个节点 →
`getByText('20.00')` 找不到 → 改 `textContent` 断言；Billing.spec 漏 import `waitFor`。

**用户二轮反馈**：截图确认账单中心已对 ✅、工作台余额已恢复 ¥20 ✅，但**工作台标签仍显示「真实」**
→ 定位为同一类错误（Dashboard 也用 mock_mode）→ 已完成 DASH-004 修复（本轮的标签同源）。

**终验**：vitest **196 passed / 20 文件**，EXIT=0，预算内（jsdom 90，基线已同步）。

### G-3 | 用户第三轮反馈：充值中心也显示「真实支付通道」

**用户要求**（附图）：*测试环境下应显示「测试支付通道」，真实环境才是「真实支付通道」*。

**修复**：`Recharge.tsx` 的通道标签同样改读 `envInfo.billing_environment`
（production → 🛡️ 真实支付通道 / 否则 → 🧪 测试支付通道（模拟）），
**不再读 `rechargeConfig.mock_mode`**（那是下单流程的行为分支，不是环境标识）。
⚠️ 下单流程内部的 `isMockMode` 分支**未动**（行为不变，仅 UI 标签同源）。

**用例**：`TC-FE-RECHARGE-017/018`（测试环境不得出现「真实支付通道」/ 生产环境反向）。
踩坑：Tag 文本带 emoji 前缀（🧪）→ `getByText` 完整匹配失败 → 改**正则部分匹配**（55/56 号日志）。

**终验（含本轮）**：vitest **198 passed / 20 文件**，EXIT=0，预算内（act 102 / jsdom 90）。

---

## 轮次 H：生产账号安全（P0 提权修复 + 管理员创建链路）

**计划（先写后测）**：
- H-1 | 🔴 **堵注册提权**：`auth.py` 注册白名单只允许 `user`/`developer`，忽略客户端传入的
  `user_type`（原实现 `valid_roles` 含 admin/super_admin 且直接采信 → 任何访客可自我提权为超管）。
- H-2 | 新增 `scripts/create_admin.py`：交互式创建超管（口令不回显/强度校验/随机口令选项/幂等）。
- H-3 | `init_db_with_data.py` 生产守卫：environment=production 时拒绝写入明文默认账号。
- H-4 | 自锁防护：`PUT /superadmin/users/{id}` 禁止把自己降级/停用；禁止停用最后一个 super_admin。
- H-5 | 前端 `Register.tsx` 是否暴露角色字段（若有则移除）。
- 验证：后端新增 pytest 用例（提权拒绝 / 自锁防护）+ 全量 pytest + 前端 budget；
  结果回填本节；原始输出归档 `docs/test-logs/2026-09-16/57+`。

## H 轮结果回填（生产账号安全）

### H-1 注册提权封堵 ✅

- 实现：`src/schemas/request.py` 的 `UserCreate` 加 `SELF_REGISTER_ROLES` 白名单
  （user/developer/owner）+ field_validator；`auth.py` 端点二次校验，越权 `role` 回退为 user_type。
  ⚠️ pydantic 要求该常量标注 `ClassVar[Set[str]]`（否则视为模型字段报 PydanticUserError，实测踩过）；
- 关键细节：`user_type` 默认 "developer" → 原 `user_type or role` 恒取 user_type，
  所以**攻击面在 user_type**（role 分支形同虚设）；
- 用例：TC-SEC-REG-001/002（传 super_admin、admin 必须 4xx）/ 003（role 越权被回退）。
- 踩坑：断言 `assert "super_admin" not in response.text` 失败 —— 422 错误详情会**回显输入值**，
  应断言状态码。（57/58 号日志）

### H-2 create_admin.py ✅

交互式建超管：口令不回显 + 二次确认、强度校验（生产 ≥12 位）、弱口令黑名单、
`--random-password`、`--reset-password`（幂等）、`ADMIN_PASSWORD` 环境变量支持。

### H-3 种子脚本生产守卫 ✅

`init_db_with_data.py` 在 `settings.is_production()` 时拒绝写入明文默认账号
（`I_KNOW_THIS_IS_NOT_PRODUCTION=1` 可显式放行）。

### H-4 自锁与最后超管防护 ✅

`PUT /superadmin/users/{id}`：禁止降级/停用自己；活跃超管 ≤1 时禁止降级/停用。
用例 TC-SEC-ADMIN-001~004（含"正常提升用户仍可用且留审计"）。

### H-5 前端 ✅ 安全

`Register.tsx` 只暴露 user/owner/developer（从未提供 admin/super_admin 选项）——
前端无问题，风险全在后端接口（已封堵）。

### H 轮终态

后端 **276 passed**（269 + 7 条安全用例）；前端未改动（budget 免跑，因仅后端与脚本变更）。
文档：`docs/SECURITY_HARDENING.md` 新增「生产环境管理员账号管理」章节（默认账号/
创建方式/日常管理/已实施防护/上线检查清单）。

---

## 轮次 I：仓库市场 UI 调整 + 新模型仓库内容

**用户需求（附图）**：① 仓库市场右上角红框统计（可用仓库 / API端点）位置需调整；
② 加入国外最新模型（GPT-6 / Fable 5.0 / Gemini）与国内模型（图 2：Hy4 preview、Hy3、
DeepSeek-V4.1-Flash、DeepSeek-V4-Pro、GLM-5.x、Kimi-K3/K2.7 等）。

**计划**：
- I-1 | UI：`developer/Repos.tsx` 的两个 Statistic 改为横向紧凑卡片（`.statsRow` 现状
  `text-align:right` + xs=12 在窄容器纵向堆叠、贴右溢出）→ flex + gap + 卡片化；
- I-2 | 数据：新建 `scripts/seed_ai_models.py`，向仓库市场写入这批模型仓库
  （含端点 POST /v1/chat/completions 等 + 限流/价格占位 + 幂等）；状态 online 以便市场可见；
- 验证：前端 typecheck + budget；脚本执行输出留痕（`docs/test-logs/2026-09-16/61+`）。

## I 轮结果回填（仓库市场 UI + 模型仓库内容）

### I-1 UI 调整 ✅

`developer/Repos.tsx`：右上角两个 Statistic（Row/Col + `text-align:right`）在窄容器下
**纵向堆叠并贴右溢出**（用户截图红框）→ 改为横向紧凑卡片（`.statRow` = flex + gap 12 +
右对齐 + 窄屏可换行；`.statItem` 白底圆角 + 图标 + 标签 + 数值）。
typecheck 0；前端全量 **198 passed / 20 spec**，预算内（act 102 / jsdom 90）。

### I-2 模型仓库写入 ✅

新增 `scripts/seed_ai_models.py`（幂等）：
- 国外 3：**GPT-6 / Fable 5.0 / Gemini**；
- 国内 11：**Hy4 preview / Hy3 / DeepSeek-V4.1-Flash / DeepSeek-V4-Pro /
  GLM-5.3 / GLM-5.3-Flash / GLM-5.2 / GLM-5.1 / GLM-5v-Turbo / Kimi-K3 / Kimi-K2.7-Code**；
- 每个仓库含 3 个端点（POST /v1/chat/completions、POST /v1/embeddings、GET /v1/models）
  + 限流 + 定价；状态 `online`（市场可见）；所有者 = owner 账号；
- `price_per_token = 0.01 × 倍率`（倍率取自用户提供的价目表，**演示占位，上线前按商务价调整**）。

**执行留痕**（61/62 号日志）：dry-run 14 个全部"新建" → 写入 `[OK] 新建 14，更新 0`
→ 复跑 `新建 0，更新 14`（幂等 ✅）。

## 本轮（D/A/B/E）待办全部完成 ✅
