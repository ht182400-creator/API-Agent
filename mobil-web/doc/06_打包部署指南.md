# 打包部署指南

## 文档信息

| 项目信息 | 详情 |
|---------|------|
| 文档名称 | 打包部署指南 |
| 版本 | V1.0 |
| 日期 | 2026-04-25 |
| 用途 | 指导移动端适配后的打包和部署工作 |

---

## 目录

1. [打包配置](#1-打包配置)
2. [本地构建](#2-本地构建)
3. [Docker部署](#3-docker部署)
4. [Nginx配置](#4-nginx配置)
5. [CDN部署](#5-cdn部署)
6. [CI/CD集成](#6-cicd集成)
7. [环境变量管理](#7-环境变量管理)

---

## 1. 打包配置

### 1.1 Vite生产配置

**文件**：`api-platform/web/vite.config.ts`

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'
import { VitePWA } from 'vite-plugin-pwa'
import compression from 'vite-plugin-compression'

export default defineConfig(({ mode }) => {
  const isProduction = mode === 'production'
  
  return {
    plugins: [
      react({
        // 移动端不需要fast refresh
        fastRefresh: !isProduction,
      }),
      
      // PWA支持（可选）
      VitePWA({
        registerType: 'autoUpdate',
        includeAssets: ['favicon.ico', 'apple-touch-icon.png', 'maskable-icon.png'],
        manifest: {
          name: 'API服务平台',
          short_name: 'API平台',
          description: '通用API服务平台',
          theme_color: '#1890ff',
          background_color: '#ffffff',
          display: 'standalone',
          orientation: 'any',
          scope: '/',
          start_url: '/',
          icons: [
            {
              src: '/icons/icon-192x192.png',
              sizes: '192x192',
              type: 'image/png',
            },
            {
              src: '/icons/icon-512x512.png',
              sizes: '512x512',
              type: 'image/png',
            },
            {
              src: '/icons/icon-512x512.png',
              sizes: '512x512',
              type: 'image/png',
              purpose: 'maskable',
            },
          ],
        },
        workbox: {
          // 缓存策略
          runtimeCaching: [
            {
              urlPattern: /^https:\/\/api\./,
              handler: 'NetworkFirst',
              options: {
                cacheName: 'api-cache',
                expiration: {
                  maxEntries: 100,
                  maxAgeSeconds: 60 * 60 * 24, // 1天
                },
              },
            },
            {
              urlPattern: /\.(?:png|jpg|jpeg|svg|gif|webp)$/,
              handler: 'CacheFirst',
              options: {
                cacheName: 'image-cache',
                expiration: {
                  maxEntries: 1000,
                  maxAgeSeconds: 30 * 24 * 60 * 60, // 30天
                },
              },
            },
          ],
        },
      }),
      
      // Gzip压缩
      compression({
        algorithm: 'gzip',
        ext: '.gz',
        threshold: 10240, // 只压缩大于10KB的文件
      }),
      
      // Brotli压缩（可选）
      compression({
        algorithm: 'brotliCompress',
        ext: '.br',
        threshold: 10240,
      }),
    ],
    
    resolve: {
      alias: {
        '@': path.resolve(__dirname, './src'),
      },
    },
    
    server: {
      port: 3000,
      host: '0.0.0.0',
      proxy: {
        '/api': {
          target: 'http://localhost:8000',
          changeOrigin: true,
        },
      },
    },
    
    build: {
      outDir: 'dist',
      sourcemap: !isProduction, // 生产环境不生成sourcemap
      minify: isProduction ? 'terser' : false,
      
      // 代码分割
      rollupOptions: {
        output: {
          manualChunks: {
            // React相关
            'react-vendor': ['react', 'react-dom', 'react-router-dom'],
            
            // UI组件库
            'antd-vendor': ['antd', '@ant-design/icons'],
            
            // 图表库
            'chart-vendor': ['echarts', 'echarts-for-react'],
            
            // 工具库
            'utils-vendor': ['axios', 'dayjs', 'zustand', 'recharts'],
          },
          
          // 确保vendor chunk的hash稳定
          chunkFileNames: isProduction ? 'assets/js/[name]-[hash].js' : 'assets/js/[name].js',
          entryFileNames: isProduction ? 'assets/js/[name]-[hash].js' : 'assets/js/[name].js',
          assetFileNames: isProduction ? 'assets/[ext]/[name]-[hash].[ext]' : 'assets/[ext]/[name].[ext]',
        },
      },
      
      // 增大chunk大小警告阈值
      chunkSizeWarningLimit: 1000,
    },
    
    // 预览配置
    preview: {
      port: 4173,
      host: '0.0.0.0',
    },
  }
})
```

### 1.2 环境变量配置

**文件**：`api-platform/web/.env.development`

```bash
# 开发环境
VITE_API_BASE_URL=http://localhost:8000
VITE_APP_TITLE=API平台（开发环境）
VITE_APP_DESCRIPTION=API统一服务平台
VITE_ENABLE_MOCK=false
VITE_ENABLE_PWA=false
```

**文件**：`api-platform/web/.env.production`

```bash
# 生产环境
VITE_API_BASE_URL=https://api.yourdomain.com
VITE_APP_TITLE=API平台
VITE_APP_DESCRIPTION=API统一服务平台
VITE_ENABLE_MOCK=false
VITE_ENABLE_PWA=true
```

**文件**：`api-platform/web/.env.staging`

```bash
# 预发布环境
VITE_API_BASE_URL=https://api-staging.yourdomain.com
VITE_APP_TITLE=API平台（预发布）
VITE_APP_DESCRIPTION=API统一服务平台
VITE_ENABLE_MOCK=false
VITE_ENABLE_PWA=false
```

### 1.3 TypeScript配置调整

**文件**：`api-platform/web/tsconfig.json`

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": false,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },
    
    // 移动端适配：允许使用现代API
    "lib": ["ES2020", "DOM", "DOM.Iterable", "ESNext.AsyncIterable"]
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

---

## 2. 本地构建

### 2.1 构建脚本配置

**文件**：`api-platform/web/package.json`（部分）

```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:staging": "vite build --mode staging",
    "build:production": "vite build --mode production",
    "preview": "vite preview",
    "preview:production": "vite preview --mode production",
    "lint": "eslint src --ext ts,tsx",
    "type-check": "tsc --noEmit"
  }
}
```

### 2.2 构建步骤

```bash
# 步骤1：进入项目目录
cd d:\Work_Area\AI\API-Agent\api-platform\web

# 步骤2：安装依赖（如果未安装）
npm install

# 步骤3：类型检查
npm run type-check

# 步骤4：代码检查
npm run lint

# 步骤5：构建（开发环境）
npm run build

# 步骤6：构建（预发布环境）
npm run build:staging

# 步骤7：构建（生产环境）
npm run build:production

# 步骤8：预览构建结果
npm run preview
```

### 2.3 构建结果分析

```bash
# 安装分析工具
npm install --save-dev rollup-plugin-visualizer

# 修改vite.config.ts
import { visualizer } from 'rollup-plugin-visualizer';

export default defineConfig({
  plugins: [
    // ...其他插件
    visualizer({
      filename: './dist/stats.html',
      open: true,
      gzipSize: true,
      brotliSize: true,
    }),
  ],
})

# 构建后自动打开分析页面
npm run build
```

### 2.4 构建优化检查清单

| 检查项 | 命令 | 目标 |
|--------|------|------|
| **包大小** | `du -sh dist/` | < 5MB |
| **Gzip后大小** | `du -sh dist/**/*.gz` | < 1MB |
| **首次加载JS** | Chrome DevTools Network | < 200KB |
| **未使用代码** | `npx unimported` | 0文件 |
| **重复依赖** | `npx depcheck` | 0重复 |

---

## 3. Docker部署

### 3.1 多阶段构建Dockerfile

**文件**：`api-platform/web/Dockerfile`

```dockerfile
# 阶段1：构建
FROM node:18-alpine AS builder

# 设置工作目录
WORKDIR /app

# 复制依赖文件
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY . .

# 构建
ARG VITE_API_BASE_URL=https://api.yourdomain.com
ENV VITE_API_BASE_URL=$VITE_API_BASE_URL

RUN npm run build

# 阶段2：运行
FROM nginx:alpine

# 复制Nginx配置
COPY nginx.conf /etc/nginx/nginx.conf

# 复制构建产物
COPY --from=builder /app/dist /usr/share/nginx/html

# 复制entrypoint脚本
COPY docker-entrypoint.sh /usr/local/bin/

RUN chmod +x /usr/local/bin/docker-entrypoint.sh

# 暴露端口
EXPOSE 80

# 启动Nginx
CMD ["docker-entrypoint.sh"]
```

### 3.2 Nginx配置

**文件**：`api-platform/web/nginx.conf`

```nginx
worker_processes 1;

events {
    worker_connections 1024;
}

http {
    include mime.types;
    default_type application/octet-stream;
    
    # 日志格式
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
    
    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;
    
    # 基本配置
    sendfile on;
    keepalive_timeout 65;
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_min_length 1024;
    gzip_comp_level 6;
    
    server {
        listen 80;
        server_name localhost;
        
        # 根目录
        root /usr/share/nginx/html;
        index index.html;
        
        # 启用gzip
        gzip_vary on;
        gzip_proxied any;
        gzip_comp_level 6;
        gzip_buffers 16 8k;
        gzip_http_version 1.1;
        gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript application/vnd.ms-fontobject application/x-font-ttf font/opentype image/svg+xml image/x-icon;
        
        # 缓存策略
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            add_header Access-Control-Allow-Origin *;
        }
        
        # PWA支持
        location = /sw.js {
            add_header Cache-Control "no-cache";
            try_files $uri $uri/ /sw.js;
        }
        
        # SPA路由支持
        location / {
            try_files $uri $uri/ /index.html;
            
            # 添加安全头
            add_header X-Frame-Options "SAMEORIGIN" always;
            add_header X-Content-Type-Options "nosniff" always;
            add_header X-XSS-Protection "1; mode=block" always;
            
            # HSTS（如果需要HTTPS）
            # add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        }
        
        # API代理（如果需要）
        location /api {
            proxy_pass http://backend:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        # 健康检查
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }
    }
}
```

### 3.3 Docker Compose配置

**文件**：`api-platform/web/docker-compose.yml`

```yaml
version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000}
    image: api-platform-web:latest
    container_name: api-platform-web
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    volumes:
      - ./logs:/var/log/nginx
    restart: unless-stopped
    networks:
      - api-platform-network

networks:
  api-platform-network:
    driver: bridge
```

### 3.4 构建和运行

```bash
# 步骤1：构建Docker镜像
cd d:\Work_Area\AI\API-Agent\api-platform\web
docker build -t api-platform-web:latest .

# 步骤2：运行容器
docker run -d \
  --name api-platform-web \
  -p 3000:80 \
  --restart unless-stopped \
  api-platform-web:latest

# 步骤3：使用Docker Compose
docker-compose up -d

# 步骤4：查看日志
docker logs -f api-platform-web

# 步骤5：停止容器
docker stop api-platform-web
docker rm api-platform-web
```

---

## 4. Nginx配置

### 4.1 生产环境Nginx配置

**文件**：`/etc/nginx/sites-available/api-platform`

```nginx
server {
    listen 80;
    listen 443 ssl http2;
    server_name api.yourdomain.com;
    
    # SSL配置（如果使用HTTPS）
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    
    # 根目录
    root /var/www/api-platform;
    index index.html;
    
    # 日志
    access_log /var/log/nginx/api-platform.access.log;
    error_log /var/log/nginx/api-platform.error.log;
    
    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
    
    # 缓存控制
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    # 不允许缓存HTML（确保用户总是获取最新版本）
    location = /index.html {
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate, max-age=0";
    }
    
    # SPA路由支持
    location / {
        try_files $uri $uri/ /index.html;
        
        # 安全头
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "no-referrer-when-downgrade" always;
    }
    
    # API代理
    location /api {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # 健康检查
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
    
    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}
```

### 4.2 启用配置

```bash
# 步骤1：复制配置文件
sudo cp api-platform /etc/nginx/sites-available/

# 步骤2：创建符号链接
sudo ln -s /etc/nginx/sites-available/api-platform /etc/nginx/sites-enabled/

# 步骤3：测试配置
sudo nginx -t

# 步骤4：重新加载Nginx
sudo systemctl reload nginx

# 步骤5：部署前端文件
sudo cp -r dist/* /var/www/api-platform/
```

---

## 5. CDN部署

### 5.1 使用阿里云CDN

**步骤**：

```
步骤1：上传构建产物到OSS
  1. 安装阿里云CLI
     npm install -g @alicloud/ossutil
  
  2. 配置OSS
     ossutil config
  
  3. 上传文件
     ossutil cp -r dist/ oss://your-bucket-name/

步骤2：配置CDN加速
  1. 登录阿里云控制台
  2. 进入CDN
  3. 添加域名：cdn.yourdomain.com
  4. 配置源站：OSS域名
  5. 配置缓存策略

步骤3：配置CNAME
  1. 在域名服务商处添加CNAME记录
  2. 指向CDN提供的CNAME地址

步骤4：验证
  curl -I https://cdn.yourdomain.com/
```

### 5.2 使用腾讯云CDN

**步骤**：

```bash
# 步骤1：安装COSCMD
pip install coscmd

# 步骤2：配置COS
coscmd config -a <SecretId> -s <SecretKey> -b <BucketName> -r <Region>

# 步骤3：上传文件
coscmd upload -r dist/ /

# 步骤4：配置CDN（在控制台操作）
```

### 5.3 自动化部署脚本

**文件**：`api-platform/web/scripts/deploy-cdn.sh`

```bash
#!/bin/bash

# 设置变量
ENV=$1  # production 或 staging
REGION="ap-guangzhou"
BUCKET="api-platform-$ENV"
CDN_DOMAIN="cdn-$ENV.yourdomain.com"

echo "开始部署到 $ENV 环境..."

# 步骤1：构建
echo "步骤1：构建项目..."
if [ "$ENV" == "production" ]; then
  npm run build:production
else
  npm run build:staging
fi

# 步骤2：上传到COS/OSS
echo "步骤2：上传到COS..."
if [ "$ENV" == "production" ]; then
  coscmd upload -r dist/ /
else
  coscmd upload -r dist/ /staging/
fi

# 步骤3：刷新CDN缓存
echo "步骤3：刷新CDN缓存..."
# 调用腾讯云CDN API刷新缓存
# 这里需要配置腾讯云CLI

echo "部署完成！"
echo "访问地址：https://$CDN_DOMAIN"
```

---

## 6. CI/CD集成

### 6.1 GitHub Actions配置

**文件**：`.github/workflows/deploy.yml`

```yaml
name: Deploy to Production

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run linter
      run: npm run lint
    
    - name: Run type check
      run: npm run type-check
    
    - name: Run tests
      run: npm test
    
    - name: Run E2E tests
      run: npx playwright test
  
  build:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Build
      run: npm run build:production
      env:
        VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL }}
    
    - name: Upload build artifacts
      uses: actions/upload-artifact@v3
      with:
        name: dist
        path: dist/
  
  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - name: Download build artifacts
      uses: actions/download-artifact@v3
      with:
        name: dist
        path: dist/
    
    - name: Deploy to server
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          cd /var/www/api-platform
          rm -rf *
          
    - name: Upload to server
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        source: "dist/*"
        target: "/var/www/api-platform"
        strip_components: 1
    
    - name: Reload Nginx
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          sudo nginx -t
          sudo systemctl reload nginx
```

### 6.2 GitLab CI配置

**文件**：`.gitlab-ci.yml`

```yaml
stages:
  - test
  - build
  - deploy

variables:
  NODE_VERSION: "18"

cache:
  paths:
    - node_modules/

test:
  stage: test
  image: node:$NODE_VERSION
  script:
    - npm ci
    - npm run lint
    - npm run type-check
    - npm test
  only:
    - merge_requests
    - main

build:
  stage: build
  image: node:$NODE_VERSION
  script:
    - npm ci
    - npm run build:production
  artifacts:
    paths:
      - dist/
    expire_in: 1 week
  only:
    - main

deploy:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache openssh-client
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | ssh-add -
    - mkdir -p ~/.ssh
    - echo "$SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
  script:
    - scp -r dist/* $SSH_USER@$SSH_HOST:/var/www/api-platform/
    - ssh $SSH_USER@$SSH_HOST "sudo nginx -t && sudo systemctl reload nginx"
  only:
    - main
```

---

## 7. 环境变量管理

### 7.1 环境变量优先级

```
优先级（从高到低）：
1. 命令行参数
   VITE_API_BASE_URL=https://api.example.com npm run build

2. .env.production / .env.development
   项目根目录下的环境文件

3. .env
   通用环境变量

注意：只有 VITE_ 开头的变量会被暴露到客户端！
```

### 7.2 敏感信息管理

**不要提交到代码仓库的文件**：

```bash
# .gitignore
.env
.env.local
.env.development.local
.env.production.local
```

**使用Vault管理敏感信息**（推荐）

```bash
# 步骤1：安装Vault
# 参考官方文档：https://www.vaultproject.io/

# 步骤2：存储敏感信息
vault kv put secret/api-platform \
  VITE_API_BASE_URL=https://api.example.com \
  VITE_SOME_SECRET_KEY=secret-key

# 步骤3：构建时获取
export VITE_API_BASE_URL=$(vault kv get -field=VITE_API_BASE_URL secret/api-platform)
npm run build
```

**使用GitLab CI/CD Variables**

```
步骤1：进入项目设置
  Settings → CI/CD → Variables

步骤2：添加变量
  Key: VITE_API_BASE_URL
  Value: https://api.example.com
  Type: Variable
  Environment: production
  Protect: ✓
  Mask: ✓（如果可能）

步骤3：在CI脚本中使用
  build:
    script:
      - echo "VITE_API_BASE_URL=$VITE_API_BASE_URL" > .env.production
      - npm run build
```

---

## 8. 回滚方案

### 8.1 保留历史版本

**部署脚本（保留最近5个版本）**

```bash
#!/bin/bash
# deploy-with-rollback.sh

DEPLOY_DIR="/var/www/api-platform"
BACKUP_DIR="/var/backups/api-platform"
MAX_VERSIONS=5

# 步骤1：创建备份
echo "创建备份..."
mkdir -p $BACKUP_DIR
cp -r $DEPLOY_DIR $BACKUP_DIR/$(date +%Y%m%d_%H%M%S)

# 步骤2：清理旧备份
echo "清理旧备份..."
ls -dt $BACKUP_DIR/* | tail -n +$((MAX_VERSIONS + 1)) | xargs -r rm -rf

# 步骤3：部署新版本
echo "部署新版本..."
cp -r dist/* $DEPLOY_DIR/

# 步骤4：重新加载Nginx
echo "重新加载Nginx..."
sudo nginx -t && sudo systemctl reload nginx

echo "部署完成！"
```

### 8.2 快速回滚

```bash
#!/bin/bash
# rollback.sh

DEPLOY_DIR="/var/www/api-platform"
BACKUP_DIR="/var/backups/api-platform"

# 列出可用备份
echo "可用的备份版本："
select BACKUP in $(ls -dt $BACKUP_DIR/* | xargs -n 1 basename); do
  if [ -n "$BACKUP" ]; then
    echo "回滚到版本：$BACKUP"
    
    # 备份当前版本
    cp -r $DEPLOY_DIR $BACKUP_DIR/before_rollback_$(date +%Y%m%d_%H%M%S)
    
    # 回滚
    rm -rf $DEPLOY_DIR/*
    cp -r $BACKUP_DIR/$BACKUP/* $DEPLOY_DIR/
    
    # 重新加载Nginx
    sudo nginx -t && sudo systemctl reload nginx
    
    echo "回滚完成！"
    break
  fi
done
```

---

## 9. 总结

### 9.1 部署检查清单

| 检查项 | 完成 |
|--------|------|
| **构建配置** | ☐ |
| **环境变量** | ☐ |
| **Nginx配置** | ☐ |
| **SSL证书** | ☐ |
| **CDN配置** | ☐ |
| **CI/CD配置** | ☐ |
| **回滚方案** | ☐ |
| **监控配置** | ☐ |

### 9.2 推荐部署流程

```
1. 本地构建并测试 ✅
   npm run build && npm run preview

2. 推送到Git仓库 ✅
   git push origin main

3. CI/CD自动测试 ✅
   GitHub Actions自动运行

4. 自动部署到预发布环境 ✅
   自动化脚本完成

5. 在预发布环境验证 ✅
   手动或自动化测试

6. 部署到生产环境 ✅
   手动触发或自动部署

7. 生产环境验证 ✅
   健康检查、冒烟测试

8. 监控观察 ✅
   观察监控指标
```

---

完成进度总结
1️⃣ 部署方案对比文档 ✅
📄 mobil-web/doc/09_一键部署方案完整对比.md
包含三种方案详细对比、架构图、成本分析、推荐方案
2️⃣ Docker Compose 一键部署 ✅
文件	说明
api-platform/docker/docker-compose.yml	完整服务编排（前端+后端+数据库+Redis+MinIO）
api-platform/docker/frontend.Dockerfile	前端多阶段构建
api-platform/docker/Dockerfile	后端镜像（已存在，已优化）
api-platform/docker/nginx.conf	Nginx 主配置
api-platform/docker/conf.d/default.conf	Nginx 站点配置
api-platform/docker/.env.example	环境变量示例
api-platform/docker/deploy.sh	一键部署脚本
api-platform/docker/docker-compose.dev.yml	开发环境覆盖配置
使用方法：

bash
cd api-platform/docker
cp .env.example .env
# 修改 .env 中的密码配置
./deploy.sh prod up
3️⃣ CI/CD 自动化部署 ✅
文件	说明
.github/workflows/cicd.yml	GitHub Actions 完整流水线
流水线阶段：

测试阶段：后端测试 + 前端测试
构建阶段：构建并推送 Docker 镜像
部署阶段：自动部署到测试/生产环境
回滚阶段：失败时自动回滚
4️⃣ Electron 安装包 ✅
文件	说明
electron/package.json	Electron 项目配置
electron/main.js	主进程（管理窗口、Docker服务）
electron/preload.js	预加载脚本（安全暴露API）
electron/build/installer.nsh	NSIS 安装脚本
electron/README.md	使用文档
electron/.gitignore	Git 忽略配置
使用方法：

bash
cd electron
npm install
npm run dist:win  # 生成 Windows 安装包
📁 完整文件清单
code
api-platform/
├── docker/
│   ├── docker-compose.yml          ✅ Docker Compose 配置
│   ├── docker-compose.dev.yml      ✅ 开发环境配置
│   ├── frontend.Dockerfile         ✅ 前端 Dockerfile
│   ├── Dockerfile                  ✅ 后端 Dockerfile (已存在)
│   ├── nginx.conf                  ✅ Nginx 主配置
│   ├── conf.d/default.conf         ✅ Nginx 站点配置
│   ├── .env.example                ✅ 环境变量示例
│   └── deploy.sh                   ✅ 部署脚本
├── .github/workflows/
│   └── cicd.yml                    ✅ CI/CD 流水线
└── electron/
    ├── package.json                ✅ Electron 配置
    ├── main.js                     ✅ 主进程
    ├── preload.js                  ✅ 预加载脚本
    ├── build/
    │   └── installer.nsh          ✅ NSIS 安装脚本
    ├── README.md                   ✅ 使用文档
    └── .gitignore                  ✅ Git 忽略配置

mobil-web/doc/
└── 09_一键部署方案完整对比.md      ✅ 部署方案对比文档
🎯 下一步建议
立即测试 Docker Compose 部署
bash
cd d:\Work_Area\AI\API-Agent\api-platform\docker
cp .env.example .env
# 编辑 .env 修改密码
./deploy.sh prod up
配置 CI/CD
在 GitHub 仓库设置中添加 Secrets：
DOCKER_USERNAME
DOCKER_PASSWORD
STAGING_HOST, STAGING_USER, STAGING_SSH_KEY
PRODUCTION_HOST, PRODUCTION_USER, PRODUCTION_SSH_KEY
构建 Electron 安装包
bash
cd d:\Work_Area\AI\API-Agent\electron
npm install
npm run dist:win





**文档结束**
