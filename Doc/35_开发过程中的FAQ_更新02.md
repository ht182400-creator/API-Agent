

## 问题125：Developer测试工具调用Weather API失败

### 125.1 问题描述

**发现时间**：2026-04-21 22:08

使用 Developer 测试工具调用 Weather API 时遇到多种错误：

**错误1：直接打开HTML文件时的 CORS 错误**
```
Access to fetch at 'http://localhost:8000/api/v1/...' from origin 'file:///...'
has been blocked by CORS policy
```

**错误2：file:// 协议安全问题**
```
unsafe attempt to load URL file:///... from frame with URL file:///...
'file:' URLs are treated as unique security origins.
```

**错误3：通过 API 平台调用返回 404**
```
{"detail":"Repository 'weather' not found"}
```

**错误4：测试工具显示的接口路径错误**
```
当前接口：GET /weather/current
```
实际应该是：
```
当前接口：GET /api/v1/weather/current
```

### 125.2 问题原因分析

#### 原因1：测试工具必须通过HTTP服务器访问

直接双击打开 HTML 文件会使用 `file://` 协议，浏览器会阻止向 `http://` 地址发送请求。

**解决方案**：必须启动 Python HTTP 服务器：
```bash
cd D:\Work_Area\AI\API-Agent\Developer\CallWeatherTest Tool\src
python -m http.server 8080
```
然后访问 `http://localhost:8080`

#### 原因2：API平台缺少动态路由

API 平台后端原本只支持固定的几个路由（如 `/chat`、`/translate`），不支持动态路径代理。

**解决方案**：添加了通用路由代理功能 `/{repo_slug}/{path:path}`

#### 原因3：weather-api仓库端点数据丢失

数据库中 weather-api 仓库的端点 `repo_id` 与仓库实际 `id` 不匹配，导致端点查询返回 0 条记录。

**解决方案**：运行数据库修复脚本重建端点数据

#### 原因4：weather-api仓库后端地址未配置

仓库的 `endpoint_url` 字段为空，导致请求无法代理到 Weather API (8001)。

**解决方案**：设置后端地址为 `http://localhost:8001`

#### 原因5：测试工具端点路径缺少前缀

测试工具硬编码的端点路径为 `/weather/current`，但 API 平台路由需要完整路径 `/api/v1/weather/current`。

**解决方案**：修改测试工具的端点配置

### 125.3 解决方案

#### 修复1：添加API平台动态路由代理

**修改文件**：`api-platform/src/api/v1/repositories.py`

在路由定义中添加通用路由代理：

```python
@router.api_route("/{repo_slug}/{path:path}", methods=["GET", "POST", "PUT", "DELETE", "PATCH"])
async def proxy_repository_endpoint(
    repo_slug: str,
    path: str,
    request: Request,
    x_access_key: str = Header(None, description="API Access Key"),
    db: AsyncSession = Depends(get_db),
):
    """
    通用仓库端点代理
    
    动态代理请求到仓库的实际后端服务。
    支持 GET、POST、PUT、DELETE、PATCH 方法。
    
    Args:
        repo_slug: 仓库 slug
        path: 仓库内部路径（如 weather/current）
    
    Returns:
        后端服务响应
    """
    import httpx
    from src.services.auth_service import AuthService
    
    # 1. 获取仓库信息
    repo_result = await db.execute(select(Repository).where(Repository.slug == repo_slug))
    repo = repo_result.scalar_one_or_none()
    
    if not repo:
        raise HTTPException(status_code=404, detail=f"Repository '{repo_slug}' not found")
    
    # 2. 检查仓库状态
    if repo.status != "online":
        raise HTTPException(status_code=403, detail="Repository is not online")
    
    # 3. 如果没有配置后端URL，返回模拟响应
    if not repo.endpoint_url:
        return BaseResponse(
            code=200,
            message="Success",
            data={
                "mock": True,
                "repo": repo.name,
                "path": f"/{path}",
                "message": "后端服务未配置，返回模拟响应",
            }
        )
    
    # 4. 验证 API Key
    auth_service = AuthService(db)
    user = None
    if x_access_key:
        try:
            user, _ = await auth_service.verify_api_key(x_access_key, repo_id=str(repo.id))
        except Exception:
            pass
    
    # 5. 转发请求到后端
    try:
        backend_url = f"{repo.endpoint_url.rstrip('/')}/{path}"
        query_params = dict(request.query_params)
        body = await request.body()
        
        async with httpx.AsyncClient(timeout=30.0) as client:
            headers = dict(request.headers)
            headers.pop("host", None)
            
            if x_access_key:
                headers["X-API-Key"] = x_access_key
            
            resp = await client.request(
                method=request.method,
                url=backend_url,
                params=query_params,
                content=body if body else None,
                headers=headers,
            )
            
            try:
                resp_data = resp.json()
                return resp_data
            except Exception:
                return {"status_code": resp.status_code, "content": resp.text}
                
    except httpx.TimeoutException:
        raise HTTPException(status_code=504, detail="Backend service timeout")
    except httpx.RequestError as e:
        raise HTTPException(status_code=502, detail=f"Backend service error: {str(e)}")
```

#### 修复2：创建端点修复脚本

**创建文件**：`api-platform/scripts/fix_weather_endpoints_sync.py`

```python
#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
修复 weather-api 仓库端点数据的脚本
"""

import sys
import os
import io

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from sqlalchemy import create_engine, select, delete
from sqlalchemy.orm import Session
from src.models.repository import Repository, RepoEndpoint

DATABASE_URL = os.getenv(
    "DATABASE_URL",
    "postgresql+psycopg2://postgres:postgres@localhost:5432/api_platform"
)


def main():
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
    
    engine = create_engine(DATABASE_URL, echo=False)
    
    with Session(engine) as db:
        # 查找 weather-api 仓库
        result = db.execute(select(Repository).where(Repository.slug == "weather-api"))
        repo = result.scalar_one_or_none()
        
        if not repo:
            print("ERROR: 未找到 weather-api 仓库！")
            return
        
        repo_id = str(repo.id)
        
        # 删除旧端点
        db.execute(delete(RepoEndpoint).where(RepoEndpoint.repo_id == repo_id))
        
        # 创建正确的端点
        endpoints_data = [
            {"path": "/api/v1/weather/current", "method": "GET", "description": "获取当前天气"},
            {"path": "/api/v1/weather/forecast", "method": "GET", "description": "获取天气预报"},
            {"path": "/api/v1/weather/aqi", "method": "GET", "description": "获取AQI数据"},
            {"path": "/api/v1/weather/alerts", "method": "GET", "description": "获取预警信息"}
        ]
        
        for ep_data in endpoints_data:
            endpoint = RepoEndpoint(
                repo_id=repo_id,
                path=ep_data["path"],
                method=ep_data["method"],
                description=ep_data["description"]
            )
            db.add(endpoint)
        
        db.commit()
        print("OK: 端点修复完成！")


if __name__ == "__main__":
    main()
```

#### 修复3：设置后端地址脚本

**创建文件**：`api-platform/scripts/set_weather_backend_url.py`

```python
#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
设置 weather-api 仓库的后端地址
"""

import sys
import os
import io

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from sqlalchemy import create_engine, select, update
from sqlalchemy.orm import Session
from src.models.repository import Repository

DATABASE_URL = os.getenv(
    "DATABASE_URL",
    "postgresql+psycopg2://postgres:postgres@localhost:5432/api_platform"
)


def main():
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
    
    engine = create_engine(DATABASE_URL, echo=False)
    
    with Session(engine) as db:
        result = db.execute(select(Repository).where(Repository.slug == "weather-api"))
        repo = result.scalar_one_or_none()
        
        if not repo:
            print("ERROR: 未找到 weather-api 仓库！")
            return
        
        # 设置后端地址
        backend_url = "http://localhost:8001"
        db.execute(
            update(Repository)
            .where(Repository.id == str(repo.id))
            .values(endpoint_url=backend_url)
        )
        db.commit()
        print("OK: 后端地址已设置为 http://localhost:8001")


if __name__ == "__main__":
    main()
```

#### 修复4：修改测试工具端点路径

**修改文件**：`Developer/CallWeatherTest Tool/src/js/app.js`

```javascript
/** API 端点配置（完整路径） */
const API_ENDPOINTS = {
  current: '/api/v1/weather/current',
  forecast: '/api/v1/weather/forecast',
  aqi: '/api/v1/weather/aqi',
  alerts: '/api/v1/weather/alerts'
};
```

**修改文件**：`Developer/CallWeatherTest Tool/src/index.html`

将所有 Tab 中的接口显示路径从 `/weather/xxx` 改为 `/api/v1/weather/xxx`：

```html
<!-- 当前天气 Tab -->
<p class="text-sm text-gray-500 mb-2">当前接口：<code class="bg-gray-100 px-2 py-1 rounded">GET /api/v1/weather/current</code></p>

<!-- 天气预报 Tab -->
<p class="text-sm text-gray-500 mb-2">当前接口：<code class="bg-gray-100 px-2 py-1 rounded">GET /api/v1/weather/forecast</code></p>

<!-- AQI Tab -->
<p class="text-sm text-gray-500 mb-2">当前接口：<code class="bg-gray-100 px-2 py-1 rounded">GET /api/v1/weather/aqi</code></p>

<!-- 预警 Tab -->
<p class="text-sm text-gray-500 mb-2">当前接口：<code class="bg-gray-100 px-2 py-1 rounded">GET /api/v1/weather/alerts</code></p>
```

### 125.4 涉及文件清单

| 文件路径 | 修改类型 | 说明 |
|----------|----------|------|
| `api-platform/src/api/v1/repositories.py` | 修改 | 添加动态路由代理 `/{repo_slug}/{path:path}` |
| `api-platform/scripts/fix_weather_endpoints_sync.py` | 新增 | 修复 weather-api 仓库端点数据 |
| `api-platform/scripts/set_weather_backend_url.py` | 新增 | 设置 weather-api 后端地址 |
| `Developer/CallWeatherTest Tool/src/js/app.js` | 修改 | 端点路径添加 `/api/v1` 前缀 |
| `Developer/CallWeatherTest Tool/src/index.html` | 修改 | 接口显示路径添加 `/api/v1` 前缀 |

### 125.5 正确使用流程

#### 步骤1：启动所有必要服务

```bash
# 终端1：启动 API 平台后端
cd D:\Work_Area\AI\API-Agent\api-platform
uvicorn src.main:app --reload --port 8000

# 终端2：启动 Weather API
cd D:\Work_Area\AI\API-Agent\OwnerServer
uvicorn src.main:app --reload --port 8001

# 终端3：启动测试工具 HTTP 服务器
cd D:\Work_Area\AI\API-Agent\Developer\CallWeatherTest Tool\src
python -m http.server 8080
```

#### 步骤2：访问测试工具

在浏览器中访问：`http://localhost:8080`

**注意**：不要直接双击打开 HTML 文件，否则会遇到 CORS 错误。

#### 步骤3：配置测试工具

| 配置项 | 值 |
|--------|-----|
| API 地址 | `http://localhost:8000/api/v1/repositories/weather-api` |
| API Key | 有效的 API Key（从 Developer Portal 获取） |

#### 步骤4：发送请求测试

1. 选择接口类型（如"当前天气"）
2. 输入城市名称（如"北京"）
3. 点击"保存"
4. 点击"发送请求"

**正确请求路径**：
```
http://localhost:8000/api/v1/repositories/weather-api/api/v1/weather/current?city=北京
```

### 125.6 调用流程图

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Developer 测试工具                               │
│                   (浏览器访问 http://localhost:8080)                  │
│                                                                     │
│  API 地址: http://localhost:8000/api/v1/repositories/weather-api   │
│  端点路径: /api/v1/weather/current                                  │
└─────────────────────────────┬───────────────────────────────────────┘
                              │
                              │ 完整请求 URL:
                              │ http://localhost:8000/api/v1/repositories/
                              │ weather-api/api/v1/weather/current?city=北京
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    API 平台后端 (8000)                                │
│                                                                     │
│  1. 验证 API Key                                                    │
│  2. 查询仓库信息 (weather-api)                                       │
│  3. 检查端点配置                                                     │
│  4. 代理转发到后端                                                   │
└─────────────────────────────┬───────────────────────────────────────┘
                              │
                              │ 转发请求:
                              │ http://localhost:8001/api/v1/weather/current?city=北京
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                 Weather API - Owner仓库 (8001)                       │
│                                                                     │
│  返回天气数据 JSON                                                   │
└─────────────────────────────────────────────────────────────────────┘
```

### 125.7 验证方法

1. **验证 API 平台服务**：
   ```
   curl http://localhost:8000/api/v1/repositories
   ```
   应返回仓库列表，包含 weather-api

2. **验证动态路由**：
   ```
   curl http://localhost:8000/api/v1/repositories/weather-api/api/v1/weather/current?city=北京
   ```
   应返回北京天气数据

3. **验证测试工具**：
   - 访问 http://localhost:8080
   - 配置 API 地址和 Key
   - 点击发送请求
   - 应显示天气数据

### 125.8 常见错误排查

| 错误现象 | 可能原因 | 解决方案 |
|----------|----------|----------|
| CORS 错误 | 直接双击打开 HTML 文件 | 必须通过 http://localhost:8080 访问 |
| 404 Repository not found | 仓库 slug 不正确 | 确认 slug 为 `weather-api` |
| 404 Endpoint not found | API 平台未重启 | 重启 API 平台后端 |
| 后端服务未配置 | endpoint_url 为空 | 运行 set_weather_backend_url.py |
| 端点显示 0 个 | 端点数据丢失 | 运行 fix_weather_endpoints_sync.py |

---

**文档更新记录**：

| 日期 | 版本 | 更新内容 | 更新人 |
:|------|------|----------|--------|
| 2026-04-21 | V1.3 | 新增问题 122：API平台端口分配说明；问题 123：修正 Developer 文档端口配置 | AI |
| 2026-04-21 | V1.4 | 新增问题 124：开发者工作台余额显示为0，添加缺失的 getConsumptionTrend 方法并优化错误处理 | AI |
| 2026-04-21 | V1.5 | 新增问题 125：Developer测试工具调用Weather API失败，添加动态路由代理、端点修复脚本、后端地址设置、测试工具端点路径修正 | AI |
| 2026-04-21 | V1.6 | 新增问题 126：Developer调用API后平台调用记录为0，添加API调用日志记录功能 | AI |

---

## 问题126：Developer调用API后平台调用记录为0

### 126.1 问题描述

**发现时间**：2026-04-21 23:16

使用 Developer 测试工具成功调用了 Weather API 并获得正常响应，但在 API 平台的 Developer 调用记录页面显示调用次数为 0。

### 126.2 问题原因

API 平台后端的 `proxy_repository_endpoint` 函数在转发请求到仓库后端时，**没有记录 API 调用日志**。

虽然 `AuthService.verify_api_key` 函数有记录日志的功能，但实际转发逻辑（`proxy_repository_endpoint`）没有调用日志记录代码。

### 126.3 解决方案

**修改文件**：`api-platform/src/api/v1/repositories.py`

在 `proxy_repository_endpoint` 函数的 `finally` 块中添加 API 调用日志记录：

```python
finally:
    # 6. 记录 API 调用日志
    try:
        from src.models.billing import APICallLog, Quota
        from datetime import datetime, timedelta

        log_entry = APICallLog(
            repo_id=repo.id,
            api_key_id=key_obj.id,
            user_id=user.id,
            endpoint=f"/{path}",
            method=request.method,
            status_code=status_code,
            response_time=str(response_time),
            cost="0",
        )
        db.add(log_entry)

        # 更新每日配额使用量
        today_start = datetime.utcnow().replace(hour=0, minute=0, second=0, microsecond=0)
        quota_result = await db.execute(
            select(Quota).where(
                and_(
                    Quota.key_id == key_obj.id,
                    Quota.quota_type == "daily",
                    Quota.reset_at >= today_start
                )
            )
        )
        quota = quota_result.scalar_one_or_none()

        if quota:
            quota.quota_used += 1
        else:
            quota = Quota(
                user_id=user.id,
                key_id=key_obj.id,
                repo_id=repo.id,
                quota_type="daily",
                quota_limit=key_obj.daily_quota or 0,
                quota_used=1,
                reset_type="daily",
                reset_at=today_start + timedelta(days=1),
            )
            db.add(quota)

        await db.commit()
    except Exception as log_error:
        await db.rollback()
        print(f"Failed to log API call: {log_error}")
```

**关键修改点**：

1. 提取 `status_code`、`response_time`、`response_data` 到 `finally` 块外
2. 在 `finally` 块中创建 `APICallLog` 记录
3. 更新每日配额 `Quota` 表
4. 添加 `and_` 导入：`from sqlalchemy import select, func, and_`

### 126.4 验证方法

1. **重启 API 平台后端**：
   ```bash
   cd D:\Work_Area\AI\API-Agent\api-platform
   python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
   ```

2. **用 Developer 测试工具调用天气 API**

3. **检查数据库**：
   ```sql
   SELECT * FROM api_call_logs ORDER BY created_at DESC LIMIT 10;
   ```

4. **在 Developer Portal 查看调用记录**：访问 Developer 的调用记录页面，应显示调用次数 > 0

### 126.5 涉及文件清单

| 文件路径 | 修改类型 | 说明 |
| `api-platform/src/api/v1/repositories.py` | 修改 | 添加 API 调用日志记录功能 |

---

**文档更新记录**：

| 日期 | 版本 | 更新内容 | 更新人 |
:|------|------|----------|--------|
| 2026-04-21 | V1.3 | 新增问题 122：API平台端口分配说明；问题 123：修正 Developer 文档端口配置 | AI |
| 2026-04-21 | V1.4 | 新增问题 124：开发者工作台余额显示为0 | AI |
| 2026-04-21 | V1.5 | 新增问题 125：Developer测试工具调用Weather API失败 | AI |
| 2026-04-21 | V1.6 | 新增问题 126：Developer调用API后平台调用记录为0 | AI |
| 2026-04-22 | V1.7 | 新增问题 127：配额使用情况RPM/RPH等数据为空，修复数据源和时区问题 | AI |
| 2026-04-22 | V1.8 | 新增问题 128：配额统计RPM/RPH按Key过滤、每月配额统计修复、Dashboard数据展示问题 | AI |

---

## 问题127：配额使用情况RPM/RPH等数据为空

### 127.1 问题描述

**发现时间**：2026-04-22 00:04

在 Developer Portal 的「配额使用情况」页面，RPM/RPH 等数据为空或不显示。

| 指标 | 显示状态 |
|------|----------|
| 调用日志 | 正常 |
| 每日配额 | 正常 |
| 每月配额 | 正常 |
| 每分钟请求 (RPM) | 为空 |
| 每小时请求 (RPH) | 为空 |

### 127.2 问题排查过程

**第一步**：确认 API 返回数据 - 返回 rpm_used: 0, rph_used: 0

**第二步**：检查数据库 - 有 20 条记录，但时间是昨天 (2026-04-21)

**第三步**：发现时区问题
```python
datetime.now():    2026-04-22 00:14:16 (北京时间)
datetime.utcnow(): 2026-04-21 16:14:16 (UTC时间)
时区差异: 8小时
```

### 127.3 解决方案

#### 修复1：配额 API 时区统一

**文件**：`api-platform/src/api/v1/quota.py`

将 `datetime.now()` 改为 `datetime.utcnow()`

#### 修复2：RPM/RPH 统计优化

改为直接通过 `user_id` 统计

#### 修复3：日志查询表名修复

将 `/quota/logs` API 数据源从 `KeyUsageLog` 改为 `APICallLog`

### 127.4 数据计算规则

| 指标 | 计算规则 | 数据来源 |
|------|---------|---------|
| 每日配额 | Quota 表 quota_type='daily' | Quota 表 |
| 每月配额 | Quota 表 quota_type='monthly' | Quota 表 |
| RPM | 最近1分钟调用次数 | APICallLog 表 |
| RPH | 最近1小时调用次数 | APICallLog 表 |

### 127.5 涉及文件

| 文件路径 | 修改内容 |
|----------|----------|
| `api-platform/src/api/v1/quota.py` | 修复时区、优化RPM/RPH统计、修复日志查询表 |
| `api-platform/src/api/v1/billing.py` | 添加按仓库的消费分布计算 |
| `api-platform/web/src/api/billing.ts` | 更新类型定义 |

### 127.6 验证方法

1. 重启后端服务
2. 插入测试数据
3. 调用 API 验证 rpm_used/rph_used 显示正确

### 127.7 时区问题说明

代码中使用 `datetime.now()`（本地时间），数据库存储用 `datetime.utcnow()`（UTC时间）。北京时区=UTC+8，导致时间错位8小时。

**解决方案**：统一使用 `datetime.utcnow()`

### 127.8 注意事项

1. 修改代码后必须重启后端服务
2. RPM/RPH 需要实际 API 调用才会显示非零值
3. 跨天时查看 RPM/RPH 会显示 0（正常现象）

---

## 问题128：配额统计RPM/RPH按Key过滤、每月配额统计修复、Dashboard数据展示问题

### 128.1 问题描述

**发现时间**：2026-04-22 00:43

修复问题 127 后，发现以下问题：

| 问题 | 描述 |
|------|------|
| RPM/RPH 统计错误 | 显示为 0，虽然有调用记录 |
| 每月配额为 0 | 虽然有调用记录 |
| Dashboard 调用趋势为空 | 依赖 Bill 表，无账单数据则为空 |
| Dashboard Top Repos 未显示 | API 未正确调用 |

### 128.2 问题原因分析

#### 问题 2.1：RPM/RPH 仍显示 0

**原因**：配额 API 使用 `user_id` 过滤，统计了该用户所有 Key 的调用，而不是当前选择的 Key 的调用。

**代码位置**：`quota.py` 第 437-452 行
```python
# 修复前
select(func.count(APICallLog.id)).where(
    APICallLog.user_id == current_user.id,  # 按用户过滤
    ...
)
```

#### 问题 2.2：每月配额为 0

**原因**：每月配额从 `Quota` 表读取，但 `consume_quota` 方法只更新 `daily` 配额，没有更新 `monthly` 配额。

**代码位置**：`quota_service.py` 第 106-156 行
```python
# 只更新了 daily 配额
quota.quota_type == "daily"
```

#### 问题 2.3：Dashboard 调用趋势为空

**原因**：Dashboard 调用 `billingApi.getConsumptionTrend()`，依赖 `Bill` 表（账单系统）。如果没有启用余额扣费，就没有账单数据。

#### 问题 2.4：Dashboard Top Repos 未显示

**原因**：
1. `top-repos` API 使用 `KeyUsageLog` 表，但该表没有被使用
2. Dashboard 的 `Promise.all` 中，`keysRes` 在被赋值前就被使用

### 128.3 解决方案

#### 修复 1：RPM/RPH 改为按 api_key_id 过滤

**文件**：`api-platform/src/api/v1/quota.py`

**修改位置**：`/info/{key_id}` 和 `/overview` 接口

```python
# 修复后 - 按 api_key_id 过滤
rpm_result = await db.execute(
    select(func.count(APICallLog.id)).where(
        APICallLog.api_key_id == api_key.id,  # 按 Key 过滤
        APICallLog.created_at >= one_minute_ago
    )
)
rph_result = await db.execute(
    select(func.count(APICallLog.id)).where(
        APICallLog.api_key_id == api_key.id,  # 按 Key 过滤
        APICallLog.created_at >= one_hour_ago
    )
)
```

#### 修复 2：每日/每月配额直接从 APICallLog 统计

**文件**：`api-platform/src/api/v1/quota.py`

**修改位置**：`/info/{key_id}` 和 `/overview` 接口

```python
# 修复后 - 直接从 APICallLog 统计
today = now.replace(hour=0, minute=0, second=0, microsecond=0)
month_start = today.replace(day=1)

# 今日使用量
daily_result = await db.execute(
    select(func.count(APICallLog.id)).where(
        APICallLog.api_key_id == api_key.id,
        APICallLog.created_at >= today
    )
)
daily_used = daily_result.scalar() or 0

# 本月使用量
monthly_result = await db.execute(
    select(func.count(APICallLog.id)).where(
        APICallLog.api_key_id == api_key.id,
        APICallLog.created_at >= month_start
    )
)
monthly_used = monthly_result.scalar() or 0
```

#### 修复 3：添加 /quota/consumption-trend API

**文件**：`api-platform/src/api/v1/quota.py`

**新增接口**：从 `APICallLog` 表统计每日调用趋势

```python
@router.get("/consumption-trend", response_model=BaseResponse[list])
async def get_consumption_trend(
    days: int = Query(14, ge=1, le=90),
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """
    获取每日调用趋势 - 从 APICallLog 表统计
    """
    # 获取用户所有 Key 的调用记录
    # 按日期聚合
    # 返回每日调用次数
```

#### 修复 4：修复 /quota/top-repos API

**文件**：`api-platform/src/api/v1/quota.py`

```python
# 修复前：使用 KeyUsageLog 表（不存在）
from src.models.api_key import KeyUsageLog

# 修复后：使用 APICallLog 表
from src.models.billing import APICallLog
```

#### 修复 5：修复 Dashboard 前端数据获取

**文件**：`api-platform/web/src/pages/developer/Dashboard.tsx`

**问题**：`keysRes` 在 `Promise.all` 中被使用但还未赋值

```typescript
// 修复前 - keysRes 在被赋值前就被使用
const [accountRes, keysRes, quotaRes, consumptionRes, topReposRes] = await Promise.all([
  billingApi.getAccount(),
  quotaApi.getKeys({ page_size: 5 }),
  quotaApi.getQuotaOverview(),
  billingApi.getConsumptionTrend(7),
  keysRes?.items?.[0]?.id ? quotaApi.getTopRepos(...) : Promise.resolve([]),  // 错误！
])

// 修复后 - 先获取 keysRes
const keysRes = await quotaApi.getKeys({ page_size: 5 })
const [accountRes, quotaRes, consumptionRes] = await Promise.all([
  billingApi.getAccount(),
  quotaApi.getQuotaOverview(),
  billingApi.getConsumptionTrend(7),
])
const topReposRes = keysRes?.items?.[0]?.id 
  ? await quotaApi.getTopRepos(keysRes.items[0].id, 5, 14)
  : []
```

#### 修复 6：Logs 页面添加 API Key 列表

**文件**：`api-platform/web/src/pages/developer/Logs.tsx`

```typescript
// 添加 keys 状态
const [keys, setKeys] = useState<APIKey[]>([])

// 加载 API Key 列表
useEffect(() => {
  const fetchKeys = async () => {
    const data = await quotaApi.getKeys({ page_size: 100 })
    setKeys(data.items)
  }
  fetchKeys()
}, [])

// API Key 选择器显示实际列表
<Select>
  {keys.map(key => (
    <Select.Option key={key.id} value={key.id}>
      {key.key_name || key.key_prefix}
    </Select.Option>
  ))}
</Select>
```

#### 修复 7：数据库时区设置

**文件**：`api-platform/src/config/database.py`

```python
async def init_db():
    async with async_engine.begin() as conn:
        # 设置会话时区为 UTC
        await conn.execute(text("SET TIME ZONE 'UTC'"))
        await conn.run_sync(Base.metadata.create_all)
```

### 128.4 涉及文件清单

| 文件路径 | 修改类型 | 修改内容 |
|----------|----------|----------|
| `api-platform/src/api/v1/quota.py` | 修改 | RPM/RPH 按 Key 过滤、每日/月配额从 APICallLog 统计、新增 consumption-trend API、修复 top-repos API |
| `api-platform/src/config/database.py` | 修改 | 添加 UTC 时区设置 |
| `api-platform/web/src/pages/developer/Dashboard.tsx` | 修改 | 修复数据获取逻辑 |
| `api-platform/web/src/pages/developer/Logs.tsx` | 修改 | 添加 API Key 列表加载和选择器 |
| `api-platform/web/src/api/quota.ts` | 修改 | 添加 getConsumptionTrend 和 getTopRepos 方法 |

### 128.5 验证方法

1. **重启后端服务**
   ```bash
   cd D:\Work_Area\AI\API-Agent\api-platform
   python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
   ```

2. **刷新配额页面**
   - RPM/RPH 应显示正确数值（如 5/1000、52/10000）
   - 每月配额应显示正确数值

3. **刷新 Dashboard 页面**
   - 调用量最高的仓库应显示 weather-api 和调用次数
   - 消费趋势如果无账单数据则显示为空（正常）

4. **刷新调用日志页面**
   - API Key 选择器应显示所有 Key
   - 选择 Key 后应正确过滤日志

### 128.6 数据来源说明

| 数据 | 数据来源 | 说明 |
|------|----------|------|
| 每日配额 | APICallLog 表 | 直接统计当天调用次数 |
| 每月配额 | APICallLog 表 | 直接统计当月调用次数 |
| RPM | APICallLog 表 | 统计最近 1 分钟调用次数，按 api_key_id 过滤 |
| RPH | APICallLog 表 | 统计最近 1 小时调用次数，按 api_key_id 过滤 |
| 调用趋势 | Bill 表 或 APICallLog 表 | 账单系统启用时用 Bill 表，否则为空 |
| Top Repos | APICallLog 表 | 统计调用次数最高的仓库 |

### 128.7 总结

本次修复解决了以下核心问题：

1. **RPM/RPH 统计口径统一**：改为按 `api_key_id` 过滤，与日志查询保持一致
2. **每月配额统计修复**：直接从 `APICallLog` 表统计，不依赖可能未更新的 Quota 表
3. **Dashboard 数据展示**：修复前端数据获取逻辑，正确显示 Top Repos
4. **调用日志 Key 选择器**：加载实际 API Key 列表，支持按 Key 过滤

---

**文档更新记录**：

| 日期 | 版本 | 更新内容 | 更新人 |
::|------|------|----------|--------|
| 2026-04-21 | V1.3 | 新增问题 122：API平台端口分配说明；问题 123：修正 Developer 文档端口配置 | AI |
| 2026-04-21 | V1.4 | 新增问题 124：开发者工作台余额显示为0 | AI |
| 2026-04-21 | V1.5 | 新增问题 125：Developer测试工具调用Weather API失败 | AI |
| 2026-04-21 | V1.6 | 新增问题 126：Developer调用API后平台调用记录为0 | AI |
| 2026-04-22 | V1.7 | 新增问题 127：配额使用情况RPM/RPH等数据为空，修复数据源和时区问题 | AI |
| 2026-04-22 | V1.8 | 新增问题 128：配额统计RPM/RPH按Key过滤、每月配额统计修复、Dashboard数据展示问题 | AI |
| 2026-04-22 | V1.9 | 新增问题 129：API调用无消费账单记录，添加自动计费功能并修复账单类型不一致问题 | AI |
| 2026-04-22 | V1.10 | 新增问题 130：消费趋势图表显示调用次数而非金额、每日调用趋势图表为空，修复数据源问题 | AI |
| 2026-04-22 | V1.11 | 新增问题 131：消费明细页面总费用显示红色错误图标，修复负数显示问题 | AI |
| 2026-04-22 | V1.12 | 新增问题 132：API调用日志记录时间为UTC而非北京时间，统一使用 datetime.now() | AI |

---

## 问题129：API调用无消费账单记录

### 129.1 问题描述

**发现时间**：2026-04-22 01:10

用户调用了 72 次 API，Dashboard 显示调用量统计正常，但**消费金额显示为 0**，提示"没有账单数据"。

### 129.2 问题原因分析

#### 原因 1：系统没有自动计费逻辑

API 调用时，系统只将调用记录保存到 `APICallLog` 表，但**没有代码自动将调用转换为消费账单扣费**。

**数据表说明**：
| 表名 | 作用 | 数据状态 |
|------|------|----------|
| `APICallLog` | 记录 API 调用日志 | ✅ 有 72 条记录 |
| `Bill` | 账单表（充值/消费） | ❌ 没有消费记录 |

#### 原因 2：账单类型名称不一致

`billing_service.py` 中创建账单使用的是 `bill_type="consumption"`，但查询端使用的是 `bill_type="consume"`。

### 129.3 解决方案

#### 修复 1：统一账单类型名称

**文件**：`api-platform/src/services/billing_service.py`

```python
# 修复前
bill_type="consumption"

# 修复后
bill_type="consume"  # 与查询端保持一致
```

#### 修复 2：添加自动计费功能

**文件**：`api-platform/src/api/v1/repositories.py`

添加 `calculate_and_charge` 函数，在每次 API 调用时计算费用并创建消费账单：

```python
async def calculate_and_charge(
    db: AsyncSession,
    user_id: UUID,
    repo_id: UUID,
    api_key_id: UUID,
    tokens_used: int = 0,
) -> tuple:
    """
    计算并扣除API调用费用
    
    1. 从 RepoPricing 表获取仓库定价
    2. 根据计费类型（按次/按Token）计算费用
    3. 检查用户余额
    4. 扣费并创建 Bill 消费记录
    """
```

#### 修复 3：API 调用时调用计费函数

在 `chat` 接口和 `proxy_repository_endpoint` 函数中添加计费调用：

```python
# chat 接口
call_cost, cost_description = await calculate_and_charge(
    db=db,
    user_id=user.id,
    repo_id=repo.id,
    api_key_id=api_key.id,
    tokens_used=tokens_used,
)

log_entry = APICallLog(
    ...
    cost=str(call_cost),  # 记录费用
)
```

### 129.4 计费逻辑说明

#### 计费规则

| 定价类型 | 计费方式 | 说明 |
|----------|----------|------|
| `per_call` | 按次计费 | 每次调用固定费用 `price_per_call` |
| `token` | 按Token计费 | 费用 = `price_per_token` × 使用Token数 |
| `free` | 免费 | 不计费 |
| 无定价配置 | 默认费率 | 每次调用 0.01 元 |

#### 扣费条件

1. 用户必须有 `Account` 账户（`account_type="balance"`）
2. 账户余额必须 >= 费用
3. 余额不足时不扣费，但仍记录 API 调用

### 129.5 涉及文件清单

| 文件路径 | 修改类型 | 修改内容 |
|----------|----------|----------|
| `api-platform/src/services/billing_service.py` | 修改 | 账单类型改为 `consume` |
| `api-platform/src/api/v1/repositories.py` | 修改 | 添加 `calculate_and_charge` 函数、调用计费逻辑 |

### 129.6 验证方法

1. **重启后端服务**
   ```bash
   cd D:\Work_Area\AI\API-Agent\api-platform
   python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
   ```

2. **确保有余额**
   - 检查账户余额 > 0

3. **调用 API**
   ```bash
   curl -X POST "http://localhost:8000/api/v1/repositories/weather-api/api/v1/weather/current?city=北京" \
     -H "X-Access-Key: your-api-key" \
     -H "X-Signature: xxx" \
     -H "X-Timestamp: xxx" \
     -H "X-Nonce: xxx"
   ```

4. **检查账单记录**
   ```sql
   SELECT * FROM bills WHERE bill_type = 'consume' ORDER BY created_at DESC LIMIT 10;
   ```
   应有新的消费记录

5. **检查余额变化**
   ```sql
   SELECT * FROM accounts WHERE account_type = 'balance';
   ```
   余额应减少

### 129.7 注意事项

1. **计费依赖仓库定价**：如果仓库没有配置 `RepoPricing`，使用全局默认费率
2. **余额不足不扣费**：系统不会因为余额不足而阻止 API 调用，只是不会扣费
3. **环境标识**：账单会标记 `environment`（simulation/production），与 `payment_mock_mode` 设置一致
4. **历史数据不追溯**：本次修复只影响修复后的新 API 调用，不会为历史调用补创建账单

---

### 129.8 可配置计费规则

计费规则支持通过配置文件调整，无需修改代码。

#### 配置项说明

**文件**：`api-platform/src/config/settings.py`

```python
# ==================== 计费配置 ====================
# 默认计费规则（当仓库没有配置 RepoPricing 时使用）
billing_default_enabled: bool = True  # 是否启用默认计费
billing_default_type: str = "per_call"  # 计费类型: per_call, token, free
billing_default_price_per_call: float = 0.01  # 按次计费单价（元）
billing_default_price_per_token: float = 0.0001  # 按Token计费单价（元/Token）
billing_default_free_calls: int = 0  # 免费调用次数（每个API Key）
billing_default_free_tokens: int = 0  # 免费Token数（每个API Key）
```

#### 计费优先级

| 优先级 | 数据源 | 说明 |
|--------|--------|------|
| 1 | `RepoPricing` 表 | 仓库级别的定价配置 |
| 2 | `settings.py` | 全局默认计费配置 |

#### 配置示例

**场景1：关闭计费（免费使用）**
```python
billing_default_enabled: bool = False
```

**场景2：调整按次计费价格**
```python
billing_default_type: str = "per_call"
billing_default_price_per_call: float = 0.05  # 每次 0.05 元
```

**场景3：改为按 Token 计费**
```python
billing_default_type: str = "token"
billing_default_price_per_token: float = 0.001  # 每 Token 0.001 元
```

#### 仓库级别配置

对于特定仓库，可以在 `RepoPricing` 表中配置独立的定价：

```sql
-- 查看仓库定价配置
SELECT r.name, p.pricing_type, p.price_per_call, p.price_per_token
FROM repo_pricing p
JOIN repositories r ON r.id = p.repo_id;

-- 为仓库设置按次计费
UPDATE repo_pricing 
SET pricing_type = 'per_call', 
    price_per_call = '0.10'
WHERE repo_id = 'your-repo-id';

-- 为仓库设置免费
UPDATE repo_pricing 
SET pricing_type = 'free'
WHERE repo_id = 'your-repo-id';
```

#### 账单描述说明

| 场景 | 描述示例 |
|------|----------|
| 仓库配置-按次 | `per_call计费-按次` |
| 仓库配置-按Token | `token计费-按Token(1000 tokens)` |
| 仓库配置-免费 | `免费调用` |
| 默认配置-按次 | `按次计费(默认 ¥0.01)` |
| 默认配置-不计费 | `不计费(计费已禁用)` |

---

## 问题130：消费趋势图表数据异常

### 130.1 问题描述

**发现时间**：2026-04-22 01:23

Dashboard 和配额页面存在以下问题：

1. **消费趋势图表显示调用次数而非金额**：图表 Y 轴显示 0-80 次，而不是 ¥ 金额
2. **每日调用趋势图表为空**：配额页面的柱状图显示"暂无调用记录"，但实际有调用数据

### 130.2 问题原因分析

#### 问题1：消费趋势显示次数而非金额

| 组件 | 位置 | 问题 |
|------|------|------|
| Dashboard 消费趋势 | `quota.py` 第 762 行 | `amount: row.call_count` 使用调用次数而非金额 |

后端 `get_consumption_trend` 接口从 `APICallLog` 表统计调用次数，而非从 `Bill` 表统计消费金额。

#### 问题2：每日调用趋势图表为空

| 组件 | 位置 | 问题 |
|------|------|------|
| 配额页面柱状图 | `quota.py` 第 670 行 | 查询 `KeyUsageLog` 表，但实际数据在 `APICallLog` 表 |

`get_usage_history` 接口查询的是 `KeyUsageLog` 表（可能为空），实际 API 调用记录存储在 `APICallLog` 表。

### 130.3 解决方案

#### 修复1：消费趋势改用 Bill 表

**文件**：`api-platform/src/api/v1/quota.py`

```python
# 修改 get_consumption_trend 接口
# 从 APICallLog 统计调用次数 → 从 Bill 表统计消费金额

async def get_consumption_trend(...):
    # 查询消费账单并按日期聚合
    result = await db.execute(
        select(Bill).where(
            Bill.user_id == current_user.id,
            Bill.bill_type == "consume",
            Bill.created_at >= start_date,
        ).order_by(Bill.created_at)
    )
    bills = result.scalars().all()
    
    # 按日期聚合消费金额
    daily_data = {}
    for bill in bills:
        date_str = bill.created_at.strftime("%Y-%m-%d")
        if date_str not in daily_data:
            daily_data[date_str] = {"date": date_str, "amount": 0}
        daily_data[date_str]["amount"] += abs(float(bill.amount))
```

#### 修复2：调用趋势改用 APICallLog 表

**文件**：`api-platform/src/api/v1/quota.py`

```python
# 修改 get_usage_history 接口
# 从 KeyUsageLog 查询 → 从 APICallLog 统计

async def get_usage_history(...):
    # 从 APICallLog 表按日期聚合查询
    result = await db.execute(
        select(
            func.date(APICallLog.created_at).label("date"),
            func.count(APICallLog.id).label("call_count"),
            func.sum(func.cast(APICallLog.cost, Float)).label("total_amount")
        ).where(
            APICallLog.api_key_id == key_id,
            APICallLog.user_id == current_user.id,
            APICallLog.created_at >= start_date,
        ).group_by(func.date(APICallLog.created_at))
    )
```

### 130.4 涉及文件清单

| 文件路径 | 修改类型 | 说明 |
|----------|----------|------|
| `api-platform/src/api/v1/quota.py` | 修改 | `get_consumption_trend` 改用 Bill 表 |
| `api-platform/src/api/v1/quota.py` | 修改 | `get_usage_history` 改用 APICallLog 表 |
| `api-platform/src/api/v1/quota.py` | 修改 | 添加 `Float` 导入 |

### 130.5 验证方法

1. **重启后端服务**
   ```bash
   cd D:\Work_Area\AI\API-Agent\api-platform
   python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
   ```

2. **验证消费趋势显示金额**
   - 访问 Developer Dashboard
   - "消费趋势"图表应显示金额（如 ¥0.72）而非次数

3. **验证调用趋势显示柱状图**
   - 访问配额页面
   - "每日调用趋势"应显示柱状图，右上角显示总调用次数

4. **检查 API 响应**
   ```bash
   curl "http://localhost:8000/api/v1/quota/consumption-trend?days=7" \
     -H "Authorization: Bearer <token>"
   # 应返回 amount 字段（非 call_count）

   curl "http://localhost:8000/api/v1/quota/usage-history/{key_id}?days=14" \
     -H "Authorization: Bearer <token>"
   # 应返回数据而非空数组
   ```

### 130.6 注意事项

1. **时区统一**：两个接口都使用 `datetime.now()` 而非 `datetime.utcnow()`，确保使用北京时间
2. **数据填充**：接口会填充缺失的日期，返回完整的 N 天数据
3. **向后兼容**：返回字段保持 `amount`、`call_count` 等名称不变，前端无需修改

---

## 问题131：消费明细页面总费用显示异常

### 131.1 问题描述

**发现时间**：2026-04-22 01:28

消费明细页面的"总费用"卡片显示红色错误图标（带斜杠的圆圈），虽然金额值看起来正常（如 0.21 ¥）。

### 131.2 问题原因分析

Ant Design 的 `Statistic` 组件在以下情况会显示错误图标：
- 值为负数
- 值为 NaN 或 undefined
- 值不是有效数字

虽然代码中使用了 `(cost || 0)` 处理，但边界情况仍可能产生问题。

### 131.3 解决方案

**文件**：`api-platform/web/src/pages/developer/ConsumptionDetails.tsx`

修复所有费用显示位置，使用 `Math.abs()` 确保值为正数：

```typescript
// 汇总统计行
value: Math.abs(repoTotal.total_cost || 0),

// 仓库汇总表格
render: (cost: number) => (
  <Text type="danger" strong>¥{Math.abs(cost || 0).toFixed(...)}</Text>
)

// 接口汇总表格
render: (cost: number) => (
  <Text type="danger">¥{Math.abs(cost || 0).toFixed(...)}</Text>
)

// 日期汇总表格
render: (cost: number) => (
  <Text type="danger" strong>¥{Math.abs(cost || 0).toFixed(...)}</Text>
)

// 明细表格
render: (cost: number) => (
  <Text type="danger">¥{Math.abs(cost || 0).toFixed(...)}</Text>
)
```

### 131.4 涉及文件清单

| 文件路径 | 修改类型 | 说明 |
|----------|----------|------|
| `api-platform/web/src/pages/developer/ConsumptionDetails.tsx` | 修改 | 修复所有费用显示的负数问题 |

### 131.5 验证方法

1. 刷新消费明细页面
2. "总费用"卡片应正常显示金额，无红色错误图标
3. 各表格中的费用列应正常显示

---

## 问题132：API调用日志记录时间为UTC而非北京时间

### 132.1 问题描述

**发现时间**：2026-04-22 01:33

使用 Developer 工具调用 Weather API 时，数据库 `APICallLog` 表中记录的时间是 UTC 时间，与北京时间相差 8 小时。

### 132.2 问题原因分析

`APICallLog` 模型的 `created_at` 字段默认值是 `datetime.utcnow()`：

```python
# billing.py
created_at = Column(DateTime, default=datetime.utcnow, index=True)
```

调用 API 时没有显式设置 `created_at`，导致使用默认的 UTC 时间。

### 132.3 解决方案

**文件**：`api-platform/src/api/v1/repositories.py`

修改 API 调用记录日志的位置，显式设置 `created_at` 为北京时间：

```python
# chat 接口
log_entry = APICallLog(
    ...
    created_at=datetime.now(),  # 使用北京时间
)
db.add(log_entry)

# 更新每日配额使用量
today_start = datetime.now().replace(hour=0, minute=0, second=0, microsecond=0)

# weather 接口
log_entry = APICallLog(
    ...
    created_at=datetime.now(),  # 使用北京时间
)
db.add(log_entry)

# 更新每日配额使用量
today_start = datetime.now().replace(hour=0, minute=0, second=0, microsecond=0)
```

### 132.4 涉及文件清单

| 文件路径 | 修改类型 | 说明 |
|----------|----------|------|
| `api-platform/src/api/v1/repositories.py` | 修改 | chat 接口和 weather 接口的日志记录改为北京时间 |


### 132.5 验证方法

1. 重启后端服务
2. 调用 Weather API
3. 查询数据库：
   ```sql
   SELECT id, endpoint, created_at FROM api_call_logs ORDER BY id DESC LIMIT 5;
   ```
4. 确认 `created_at` 显示的是北京时间


## 问题133：日志页面请求ID显示异常

### 133.1 问题描述

**发现时间**：2026-04-22

**问题现象**：开发者日志页面中"请求ID"列显示的值异常：
- 显示类似 `98_` 或 `97_` 这样的不完整值
- 这些值被截断或显示不正确

**期望行为**：
- 应该显示完整的请求追踪ID（UUID格式）
- 如：`a1b2c3d4-e5f6-7890-abcd-ef1234567890`

### 133.2 问题原因分析

#### 根因分析

**问题链路**：

1. **middleware.py 生成 request_id**
   ```python
   # src/core/middleware.py
   request_id = str(uuid.uuid4())
   request.state.request_id = request_id
   response.headers["X-Request-ID"] = request_id
   ```

2. **APICallLog 表缺少 request_id 字段**
   ```python
   # src/models/billing.py (修复前)
   class APICallLog(Base):
       __tablename__ = "api_call_logs"
       id = Column(BigInteger, primary_key=True, autoincrement=True)
       # 缺少: request_id = Column(String(64), nullable=True)
   ```

3. **前端显示的是自增 id，不是 request_id**
   ```typescript
   // web/src/pages/developer/Logs.tsx (修复前)
   {
     title: '请求ID',
     dataIndex: 'id',  // 错误：显示的是自增主键
     key: 'id',
   }
   ```

4. **后端日志接口未返回 request_id**
   ```python
   # src/api/v1/quota.py (修复前)
   {
       "id": str(log.id),
       # 缺少: "request_id": log.request_id,
   }
   ```

### 133.3 解决方案

#### 步骤1：修改 APICallLog 模型，添加 request_id 字段

**文件**：`src/models/billing.py`

```python
class APICallLog(Base):
    """API Call Log model - API调用日志表"""
    __tablename__ = "api_call_logs"

    id = Column(BigInteger, primary_key=True, autoincrement=True)

    # 请求追踪ID (由 middleware 生成的全链路追踪ID)
    request_id = Column(String(64), nullable=True, index=True)

    # ... 其他字段保持不变
```

#### 步骤2：修改 repositories.py，记录日志时保存 request_id

**文件**：`src/api/v1/repositories.py`

**修改位置1：chat 接口（第683行）**
```python
# 使用北京时间记录，保存 request_id 用于追踪
log_entry = APICallLog(
    repo_id=repo.id,
    api_key_id=api_key.id,
    user_id=user.id,
    request_id=getattr(request.state, "request_id", None),  # 新增
    endpoint="/chat",
    method="POST",
    status_code=status_code,
    response_time=str(response_time),
    cost=str(call_cost),
    created_at=datetime.now(),  # 使用北京时间
)
```

**修改位置2：proxy_repository_endpoint 接口（第932行）**
```python
# 使用北京时间记录，保存 request_id 用于追踪
log_entry = APICallLog(
    repo_id=repo.id,
    api_key_id=key_obj.id,
    user_id=user.id,
    request_id=getattr(request.state, "request_id", None),  # 新增
    endpoint=f"/{path}",
    method=request.method,
    status_code=status_code,
    response_time=str(response_time),
    cost=str(call_cost),
    created_at=datetime.now(),
)
```

#### 步骤3：修改日志查询接口，返回 request_id

**文件**：`src/api/v1/quota.py`

```python
return BaseResponse(
    data={
        "items": [
            {
                "id": str(log.id),
                "request_id": log.request_id,  # 新增：返回追踪ID
                "api_key_id": str(log.api_key_id) if log.api_key_id else None,
                # ... 其他字段
            }
            for log in logs
        ],
        # ...
    }
)
```

#### 步骤4：修改前端类型定义

**文件**：`web/src/api/quota.ts`

```typescript
export interface APICallLog {
  id: string
  request_id: string | null  // 新增：全链路追踪ID
  api_key_id: string
  // ... 其他字段
}
```

#### 步骤5：修改前端日志表格显示

**文件**：`web/src/pages/developer/Logs.tsx`

```typescript
{
  title: '请求ID',
  dataIndex: 'request_id',
  key: 'request_id',
  width: 140,
  render: (requestId: string | null, record: any) => {
    // 优先显示 request_id，没有则降级到 id
    const displayId = requestId || record.id
    return (
      <Tooltip title={displayId}>
        <Text copyable={{ text: displayId, tooltips: '复制ID' }} style={{ fontSize: 11 }}>
          {displayId?.substring(0, 16)}...
        </Text>
      </Tooltip>
    )
  },
},
```

### 133.4 数据库迁移

**迁移脚本**：`migrations/add_request_id_column.py`

```sql
-- 添加字段
ALTER TABLE api_call_logs 
ADD COLUMN request_id VARCHAR(64);

-- 创建索引
CREATE INDEX idx_api_call_logs_request_id ON api_call_logs(request_id);
```

**执行命令**：
```bash
cd api-platform
python migrations/add_request_id_column.py upgrade
```

### 133.5 验证方法

1. **执行数据库迁移**
   ```bash
   python migrations/add_request_id_column.py upgrade
   ```

2. **重启后端服务**

3. **调用 API 接口**
   ```bash
   curl -X POST "http://localhost:8000/api/v1/weather/current" \
     -H "X-Access-Key: your_key" \
     -H "X-Signature: xxx" \
     -H "X-Timestamp: xxx" \
     -H "X-Nonce: xxx"
   ```

4. **检查响应头**
   ```bash
   curl -I "http://localhost:8000/api/v1/weather/current" ...
   ```
   应该看到：`X-Request-ID: a1b2c3d4-e5f6-7890-abcd-ef1234567890`

5. **查询数据库**
   ```sql
   SELECT id, request_id, endpoint, created_at 
   FROM api_call_logs 
   ORDER BY id DESC LIMIT 5;
   ```
   应该看到 `request_id` 字段有值

6. **检查前端日志页面**
   - 请求ID列应显示完整的UUID格式
   - 如：`a1b2c3d4-e5f6-78...`

### 133.6 相关文件清单

| 文件路径 | 修改类型 | 说明 |
|---------|---------|------|
| `src/models/billing.py` | 修改 | APICallLog 模型添加 request_id 字段 |
| `src/api/v1/repositories.py` | 修改 | 两处日志记录添加 request_id |
| `src/api/v1/quota.py` | 修改 | 日志查询接口返回 request_id |
| `web/src/api/quota.ts` | 修改 | APICallLog 类型添加 request_id |
| `web/src/pages/developer/Logs.tsx` | 修改 | 日志表格显示 request_id |
| `migrations/add_request_id_column.py` | 新增 | 数据库迁移脚本 |

### 133.7 版本记录

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| V2.13 | 2026-04-22 | 首次发布，修复请求ID显示问题 |

---

## 问题134：开发统一可扩展的API测试工具

### 134.1 问题描述

**发现时间**：2026-04-22 09:34

随着 API 平台仓库数量的增长，当前的 Developer 测试工具（`CallWeatherTest Tool`）存在以下问题：

| 问题 | 描述 | 影响 |
|------|------|------|
| 功能单一 | 仅支持 Weather API | 无法测试其他仓库 |
| 扩展困难 | 硬编码端点配置 | 新增仓库需修改代码 |
| 维护成本高 | 表单和逻辑耦合 | 每次修改风险大 |
| 分类缺失 | 无法区分仓库类型 | 用户体验差 |

### 134.2 解决方案概述

开发一个**可扩展的统一 API 测试工具**，采用配置驱动的架构设计。

### 134.3 架构设计

#### 架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           API 测试工具 (前端)                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        UI 组件层                                      │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌──────────┐     │   │
│  │  │ Repository  │  │  Endpoint   │  │  Response   │  │ Dynamic  │     │   │
│  │  │    Card     │  │    List     │  │   Panel     │  │  Form    │     │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └──────────┘     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        配置管理层                                      │   │
│  │  ┌────────────────────────────────────────────────────────────────┐│   │
│  │  │              repositories.config.ts                             ││   │
│  │  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         ││   │
│  │  │  │  weather     │  │  logistics   │  │     sms      │         ││   │
│  │  │  │  (自研)      │  │  (自研)       │  │  (第三方)     │         ││   │
│  │  │  └──────────────┘  └──────────────┘  └──────────────┘         ││   │
│  │  └────────────────────────────────────────────────────────────────┘│   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 目录结构

```
api-platform/web/src/
├── config/
│   ├── repositories.config.ts      # 仓库配置中心（统一导出）
│   └── repos/
│       ├── weather.config.ts       # 自研：天气API
│       ├── logistics.config.ts     # 自研：物流API
│       └── third-party/
│           ├── sms.config.ts       # 第三方：短信API
│           └── payment.config.ts   # 第三方：支付API
├── types/
│   └── api-tester.ts               # 类型定义
├── components/
│   └── api-tester/
│       ├── DynamicForm.tsx         # 动态表单组件
│       ├── RepositoryCard.tsx      # 仓库卡片组件
│       ├── EndpointList.tsx         # 端点列表组件
│       └── ResponsePanel.tsx       # 响应面板组件
└── pages/
    └── developer/
        ├── ApiTester.tsx           # 主测试工具页面
        └── ApiTester.module.css    # 页面样式
```

### 134.4 新旧方案对比

| 特性 | 旧方案 (CallWeatherTest) | 新方案 (ApiTester) |
|------|--------------------------|-------------------|
| **仓库支持** | 仅 Weather API | 所有仓库 |
| **添加新仓库** | 修改代码 | 添加配置文件 |
| **表单生成** | 硬编码 HTML | 动态渲染 |
| **分类展示** | 无 | 自研/第三方 |
| **响应示例** | 无 | 支持 |
| **历史记录** | 基础 | 支持+可回放 |
| **curl 生成** | 支持 | 支持 |
| **维护成本** | 高 | 低 |
| **扩展工时** | 2-4小时/仓库 | 15-30分钟/仓库 |

### 134.5 访问地址

| 环境 | 地址 |
|------|------|
| 测试工具 | http://localhost:3000/developer/api-tester |
| API 平台后端 | http://localhost:8000 |
| Weather API | http://localhost:8001 |

### 134.6 涉及文件清单

| 文件路径 | 操作 | 说明 |
|----------|------|------|
| `web/src/types/api-tester.ts` | 新增 | 类型定义 |
| `web/src/config/repositories.config.ts` | 新增 | 仓库配置中心 |
| `web/src/config/repos/weather.config.ts` | 新增 | 天气API配置 |
| `web/src/config/repos/logistics.config.ts` | 新增 | 物流API配置 |
| `web/src/config/repos/third-party/sms.config.ts` | 新增 | 短信API配置 |
| `web/src/config/repos/third-party/payment.config.ts` | 新增 | 支付API配置 |
| `web/src/components/api-tester/DynamicForm.tsx` | 新增 | 动态表单组件 |
| `web/src/components/api-tester/RepositoryCard.tsx` | 新增 | 仓库卡片组件 |
| `web/src/components/api-tester/EndpointList.tsx` | 新增 | 端点列表组件 |
| `web/src/components/api-tester/ResponsePanel.tsx` | 新增 | 响应面板组件 |
| `web/src/pages/developer/ApiTester.tsx` | 新增 | 主测试工具页面 |
| `web/src/pages/developer/ApiTester.module.css` | 新增 | 页面样式 |
| `web/src/App.tsx` | 修改 | 添加路由配置 |
| `Developer/API测试工具/01_需求规格说明书.md` | 新增 | 需求文档 |
| `Developer/API测试工具/03_发布说明_v1.0.0.md` | 新增 | 发布说明 |
| `Developer/API测试工具/04_API测试工具设计.md` | 新增 | 设计文档 |
| `Developer/API测试工具/06_开发FAQ.md` | 新增 | FAQ文档 |

### 134.7 验证方法

1. **启动前端开发服务器**
   ```bash
   cd api-platform/web
   npm run dev
   ```

2. **访问测试工具**
   浏览器访问：`http://localhost:3000/developer/api-tester`

3. **测试功能**
   - [ ] 切换仓库（Weather → Logistics → SMS）
   - [ ] 分类筛选（全部/自研/第三方）
   - [ ] 选择端点并填写参数
   - [ ] 发送请求并查看响应
   - [ ] 复制 curl 命令
   - [ ] 查看历史记录

---

**文档更新记录**：

| 日期 | 版本 | 更新内容 | 更新人 |
:------|------|----------|--------|
| 2026-04-22 | V2.14 | 新增问题 134：开发统一可扩展的API测试工具 | AI |

---

## 问题135：API测试工具UI优化

### 135.1 问题描述

**发现时间**：2026-04-22 13:43

API测试工具功能已实现，但界面存在以下问题：

| 问题 | 描述 | 影响 |
|------|------|------|
| 布局混乱 | Tailwind类名冲突，样式不一致 | 用户体验差 |
| 视觉平淡 | 缺少层次感和视觉反馈 | 吸引力不足 |
| 交互模糊 | 选中状态、悬停效果不明确 | 操作不清晰 |
| 响应式差 | 不支持移动端和平板 | 设备兼容性差 |

### 135.2 优化方案

采用 **CSS Modules** 替代内联样式，提升样式的可维护性和可预测性。

### 135.3 优化内容

#### 3.1 整体布局优化

```
┌────────────────────────────────────────────────────────────────────┐
│  🧪 API 测试工具                                                   │
│  📦 4 个仓库  🔗 12 个接口                                         │
├────────────────────────────────────────────────────────────────────┤
│  [全部] [🏠 自研] [🌐 第三方]                    [📜 历史 (0)]     │
├────────────────────────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌──────────────────────────────────┐   │
│  │ 仓库列表 │  │端点列表 │  │                                  │   │
│  ├─────────┤  ├─────────┤  │  请求配置 & 响应展示              │   │
│  │ ☁️天气API│  │GET 实时 │  │                                  │   │
│  │ 🚚物流API│  │GET 预报 │  │  [发送请求]  [清空]              │   │
│  │ 💬短信   │  │POST发送 │  │                                  │   │
│  └─────────┘  └─────────┘  └──────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘
```

#### 3.2 视觉升级对比表

| 元素 | 优化前 | 优化后 |
|------|--------|--------|
| **背景** | 纯灰色 `#f9fafb` | 渐变 `linear-gradient(135deg, #f5f7fa, #e4e8ec)` |
| **面板** | 简单阴影 | 圆角14px + 柔和阴影 + 头部渐变 |
| **按钮** | 普通边框样式 | 渐变背景 + 悬停动画 + 阴影 |
| **标签** | 边框按钮 | 胶囊式标签 + 选中高亮 |
| **统计** | 纯文本 | 图标徽章 + 圆角背景 |
| **加载** | 简单遮罩 | 毛玻璃效果 + 中心加载框 |

#### 3.3 组件样式优化

| 组件 | 优化项 | 实现方式 |
|------|--------|----------|
| **仓库卡片** | 图标背景 | 44x44px 圆角容器，悬停上浮效果 |
| | 选中指示器 | 蓝色圆角徽章 `✓` |
| | 悬停效果 | 边框变色 + 阴影 + 轻微上移 |
| **端点列表** | 分组标题 | 小字号 + 大写 + 字母间距 |
| | 路径显示 | 深色背景 + 等宽字体 |
| | 选中状态 | 左侧蓝色边框 + 浅蓝背景 |
| **动态表单** | 参数分组 | 浅灰背景 + 圆角容器 |
| | 输入框 | 圆角8px + 焦点蓝色发光 |
| | 必填标记 | 红色星号 `*` |
| **响应面板** | 状态栏 | 大号状态码 + 图标 + 颜色编码 |
| | curl区域 | 深色主题 `#1e293b` |
| | JSON展示 | 深色主题 + 语法高亮风格 |

### 135.4 文件变更清单

| 文件路径 | 操作 | 说明 |
|----------|------|------|
| `web/src/pages/developer/ApiTester.tsx` | 重写 | 使用CSS Modules替代内联样式 |
| `web/src/pages/developer/ApiTester.module.css` | 新增 | 主页面完整样式定义 |
| `web/src/components/api-tester/RepositoryCard.tsx` | 重写 | 添加样式模块导入 |
| `web/src/components/api-tester/RepositoryCard.module.css` | 新增 | 仓库卡片完整样式 |
| `web/src/components/api-tester/EndpointList.tsx` | 重写 | 添加样式模块导入 |
| `web/src/components/api-tester/EndpointList.module.css` | 新增 | 端点列表完整样式 |
| `web/src/components/api-tester/DynamicForm.tsx` | 重写 | 添加样式模块导入 |
| `web/src/components/api-tester/DynamicForm.module.css` | 新增 | 动态表单完整样式 |
| `web/src/components/api-tester/ResponsePanel.tsx` | 重写 | 添加样式模块导入 |
| `web/src/components/api-tester/ResponsePanel.module.css` | 新增 | 响应面板完整样式 |

### 135.5 CSS Modules 使用示例

```tsx
// 组件中使用
import styles from './Component.module.css';

export const Component: React.FC = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>标题</h1>
      <button className={styles.button}>按钮</button>
    </div>
  );
};
```

```css
/* Component.module.css */
.container {
  padding: 20px;
  background: white;
  border-radius: 14px;
}

.title {
  font-size: 24px;
  font-weight: 700;
  color: #1e293b;
}

.button {
  padding: 10px 20px;
  background: linear-gradient(135deg, #3b82f6, #2563eb);
  color: white;
  border: none;
  border-radius: 10px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.button:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(59, 130, 246, 0.4);
}
```

### 135.6 响应式布局断点

```css
/* 大屏 (>= 1200px) */
.mainGrid {
  grid-template-columns: 280px 320px 1fr;
}

/* 中屏 (1024px - 1199px) */
@media (max-width: 1200px) {
  .mainGrid {
    grid-template-columns: 260px 300px 1fr;
  }
}

/* 平板 (768px - 1023px) */
@media (max-width: 1024px) {
  .mainGrid {
    grid-template-columns: 1fr 1fr;
  }
}

/* 手机 (< 768px) */
@media (max-width: 768px) {
  .mainGrid {
    grid-template-columns: 1fr;
  }
}
```

### 135.7 验证方法

1. **启动前端开发服务器**
   ```bash
   cd api-platform/web
   npm run dev
   ```

2. **访问测试工具**
   浏览器访问：`http://localhost:3000/developer/api-tester`

3. **验证优化效果**
   - [ ] 布局是否整齐（三栏布局）
   - [ ] 分类标签是否为胶囊样式
   - [ ] 仓库卡片是否有悬停动画
   - [ ] 选中状态是否有蓝色边框和背景
   - [ ] 发送按钮是否有渐变和阴影
   - [ ] 历史记录面板是否可以关闭
   - [ ] 响应面板是否显示curl命令

### 135.8 浏览器兼容性

| 浏览器 | 最低版本 | 备注 |
|--------|----------|------|
| Chrome | 90+ | 完全支持 |
| Firefox | 88+ | 完全支持 |
| Safari | 14+ | 完全支持 |
| Edge | 90+ | 完全支持 |

---

## 问题136：Weather API 仓库配置

### 136.1 问题描述

**发现时间**：2026-04-22 11:03

为可扩展API测试工具配置 Weather API 仓库，需要与 CallWeatherTest Tool 源码保持一致。

### 136.2 配置映射

#### 端点配置对比

| CallWeatherTest Tool | weather.config.ts | 参数 |
|---------------------|-------------------|------|
| `API_ENDPOINTS.current` | `path: '/current'` | city, language, unit |
| `API_ENDPOINTS.forecast` | `path: '/forecast'` | city, days, unit |
| `API_ENDPOINTS.aqi` | `path: '/aqi'` | city |
| `API_ENDPOINTS.alerts` | `path: '/alerts'` | city |

#### 参数详情

| 端点 | 参数名 | 类型 | 必填 | 默认值 | 说明 |
|------|--------|------|------|--------|------|
| **/current** | city | string | ✅ | - | 城市名称（支持中文/拼音） |
| | language | select | ❌ | `zh-CN` | 返回语言 |
| | unit | select | ❌ | `c` | 温度单位 |
| **/forecast** | city | string | ✅ | - | 城市名称 |
| | days | select | ❌ | `3` | 预报天数(1-7) |
| | unit | select | ❌ | `c` | 温度单位 |
| **/aqi** | city | string | ✅ | - | 城市名称 |
| **/alerts** | city | string | ✅ | - | 城市名称 |

#### 响应数据结构

根据 Weather API 后端 `OwnerServer/weather-api/src/models/response.py`：

```typescript
// 实时天气响应
interface CurrentWeatherData {
  city: string;
  city_code: string;
  country: string;
  weather: string;
  weather_code: string;
  temperature: number;
  temperature_f: number;
  feels_like: number;
  humidity: number;
  humidity_percent: number;
  wind_speed: number;
  wind_speed_unit: string;
  wind_direction: string;
  wind_direction_degree: number;
  pressure: number;
  visibility: number;
  uv_index: number;
  aqi?: number;
  aqi_level?: string;
  update_time: string;
  sunrise?: string;
  sunset?: string;
}

// 天气预报响应
interface ForecastData {
  city: string;
  city_code: string;
  forecast_days: number;
  forecasts: ForecastDayData[];
  update_time: string;
}

// AQI响应
interface AQIData {
  city: string;
  city_code: string;
  aqi: number;
  level: string;
  level_color: string;
  primary_pollutant?: string;
  health_advice: string;
  pollutants: Record<string, PollutantData>;
  update_time: string;
}

// 预警响应
interface AlertData {
  city: string;
  has_alerts: boolean;
  alerts_count: number;
  alerts: AlertItem[];
  update_time: string;
}
```

### 136.3 配置文件位置

**文件**：`api-platform/web/src/config/repos/weather.config.ts`

### 136.4 配置示例

```typescript
export const weatherRepository: Repository = {
  id: 'weather-api',
  slug: 'weather',
  name: '天气API',
  description: '提供实时天气、天气预报、空气质量、天气预警等全方位天气数据服务',
  icon: 'cloud',
  category: 'self',
  baseUrl: '/api/v1/weather',
  authType: 'api_key',
  authHeader: 'X-Access-Key',
  enabled: true,
  version: '1.0.0',
  endpoints: [
    {
      id: 'current',
      name: '实时天气',
      description: '获取指定城市的实时天气信息...',
      path: '/current',
      method: 'GET',
      params: [
        { name: 'city', type: 'string', required: true, in: 'query' },
        { name: 'language', type: 'select', required: false, defaultValue: 'zh-CN', options: [...], in: 'query' },
        { name: 'unit', type: 'select', required: false, defaultValue: 'c', options: [...], in: 'query' }
      ],
      responseExample: { /* 响应示例 */ },
      tags: ['天气']
    },
    // ... 其他端点
  ]
};
```

---

## 问题137：API测试工具模块导出修复

### 137.1 问题描述

**发现时间**：2026-04-22 11:07

页面加载报错：
```
Uncaught SyntaxError: The requested module '/src/pages/developer/ApiTester.tsx' 
does not provide an export named 'default'
```

### 137.2 问题原因

| 问题 | 说明 |
|------|------|
| **文件导出** | `ApiTester.tsx` 使用命名导出 `export const ApiTester` |
| **App.tsx导入** | 使用默认导入 `import ApiTester from './pages/developer/ApiTester'` |
| **结果** | 导出/导入方式不匹配 |

### 137.3 解决方案

在 `ApiTester.tsx` 末尾添加默认导出：

```typescript
export const ApiTester: React.FC = () => {
  // ... 组件代码
};

export default ApiTester;  // 添加这一行
```

### 137.4 最佳实践建议

| 做法 | 说明 |
|------|------|
| 同时保留两种导出 | 命名导出 + 默认导出，兼容性更好 |
| 组件名与文件名一致 | 便于调试和代码跳转 |
| TypeScript 配置 | 确保 `esModuleInterop: true` |

```typescript
// 推荐写法：同时支持命名和默认导入
export const ApiTester: React.FC = () => { /* ... */ };
export default ApiTester;

// 使用时可以：
import { ApiTester } from './ApiTester';      // 命名导入
import ApiTester from './ApiTester';           // 默认导入
import ApiTester, { ApiTester as ApiTesterComponent } from './ApiTester'; // 混用
```

---

**文档更新记录**：

| 日期 | 版本 | 更新内容 | 更新人 |
:------|------|----------|--------|
| 2026-04-22 | V2.14 | 新增问题 134：开发统一可扩展的API测试工具 | AI |
| 2026-04-22 | V2.15 | 新增问题 135：API测试工具UI优化 | AI |
| 2026-04-22 | V2.16 | 新增问题 136：Weather API 仓库配置 | AI |
| 2026-04-22 | V2.17 | 新增问题 137：API测试工具模块导出修复 | AI |
| 2026-04-22 | V2.18 | 新增问题 138：API Key 配置功能 | AI |
| 2026-04-22 | V2.19 | 新增问题 139：Weather API 404错误修复 | AI |
| 2026-04-22 | V2.20 | 新增问题 140：API测试工具详细使用步骤 | AI |
| 2026-04-22 | V2.21 | 新增问题 141：历史记录和调用明细增强 | AI |

---

## 问题138：API Key 配置功能

### 138.1 问题描述

**发现时间**：2026-04-22 14:17

用户在使用 API 测试工具时，发现界面显示 "⚠️ 未配置API Key"，但没有地方可以配置。

### 138.2 解决方案

新增 **API Key 配置面板**，允许用户在界面上直接配置访问密钥。

#### 界面展示

```
┌─────────────────────────────────────────────────────────────────┐
│ 🧪 API 测试工具  [📦 4个仓库] [🔗 12个接口]                     │
├─────────────────────────────────────────────────────────────────┤
│ [全部] [🏠 自研] [🌐 第三方]     [📜 历史(0)]  [⚠️ 配置Key]     │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🔑 API Key 配置                                    [×]     │ │
│ ├─────────────────────────────────────────────────────────────┤ │
│ │ 访问密钥 (X-Access-Key)                                     │ │
│ │ [••••••••••••••••••••] [👁️]                               │ │
│ │ 请输入 Owner Server 的访问密钥，用于调用受保护的 API 接口。   │ │
│ │                                                             │ │
│ │ [💾 保存]  [🗑️ 清除]  [取消]                               │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                  │
│ ┌──────────┐ ┌──────────┐ ┌─────────────────────────────────┐   │
│ │ 仓库列表  │ │端点列表  │ │ 请求配置                        │   │
│ └──────────┘ └──────────┘ └─────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 138.3 文件变更

| 文件 | 操作 | 说明 |
|------|------|------|
| `stores/apiKey.ts` | 新增 | API Key 状态管理，使用 localStorage 持久化 |
| `pages/developer/ApiTester.tsx` | 修改 | 添加配置按钮和面板 UI |
| `pages/developer/ApiTester.module.css` | 修改 | 添加配置面板样式 |

### 138.4 核心代码

#### stores/apiKey.ts

```typescript
/**
 * API Key 状态管理
 * 用于 API 测试工具的访问密钥配置
 */

import { create } from 'zustand'
import { persist } from 'zustand/middleware'

interface ApiKeyState {
  apiKey: string | null
  setApiKey: (key: string | null) => void
  clearApiKey: () => void
}

export const useApiKeyStore = create<ApiKeyState>()(
  persist(
    (set) => ({
      apiKey: null,
      
      setApiKey: (key) => {
        set({ apiKey: key || null })
      },
      
      clearApiKey: () => {
        set({ apiKey: null })
      },
    }),
    {
      name: 'api-key-storage',  // localStorage key
    }
  )
)
```

### 138.5 功能特性

| 功能 | 说明 |
|------|------|
| **配置按钮** | 工具栏右侧显示 "⚠️ 配置Key" / "🔑 已配置" |
| **密码可见切换** | 点击 👁️ 按钮切换显示/隐藏 |
| **持久化存储** | 使用 localStorage 保存，刷新页面后保留 |
| **清除功能** | 一键清除已保存的 Key |
| **状态指示** | 绿色高亮表示已配置 |

---

## 问题139：Weather API 404 错误修复

### 139.1 问题描述

**发现时间**：2026-04-22 14:22

调用 Weather API 时返回 404 错误：
```json
{
  "detail": "Not Found"
}
```

### 139.2 问题原因

| 问题 | 说明 |
|------|------|
| **Weather API 端口** | 运行在 `8001` 端口 |
| **前端默认端口** | 请求 `8000` 端口 |
| **结果** | API 平台没有 Weather API 路由，导致 404 |

**架构差异**：
```
API Platform (端口 8000)
├── /api/v1/repositories  ← 仓库管理API
├── /api/v1/...           ← 其他API
└── Weather API?          ← ❌ 不存在

OwnerServer Weather API (端口 8001)
├── /api/v1/weather/current   ← ✅ 天气API
├── /api/v1/weather/forecast ← ✅ 天气预报
├── /api/v1/weather/aqi      ← ✅ 空气质量
└── /api/v1/weather/alerts   ← ✅ 天气预警
```

### 139.3 解决方案

#### 方案1：仓库配置添加 apiUrl 字段

修改类型定义和仓库配置，支持不同仓库使用不同 API 地址：

**types/api-tester.ts**：
```typescript
export interface Repository {
  // ... 其他字段
  /** API 基础地址，如 /api/v1/weather */
  baseUrl: string;
  /** 完整的 API 服务地址，如 http://localhost:8001 */
  apiUrl?: string;
  // ... 其他字段
}
```

**repos/weather.config.ts**：
```typescript
export const weatherRepository: Repository = {
  id: 'weather-api',
  // ...
  baseUrl: '/api/v1/weather',
  apiUrl: 'http://localhost:8001',  // 新增：指定 Weather API 地址
  // ...
};
```

#### 方案2：前端请求逻辑适配

**ApiTester.tsx**：
```typescript
const handleSendRequest = async () => {
  if (!selectedRepo || !selectedEndpoint) return;

  // 根据仓库配置选择 API 地址
  const apiUrl = selectedRepo.apiUrl || 'http://localhost:8000';
  const url = buildUrl(selectedRepo, selectedEndpoint);
  const fullUrl = `${apiUrl}${url}`;
  // ...
};
```

### 139.4 完整请求流程

```
┌──────────────────────────────────────────────────────────────────┐
│                     API 测试工具请求流程                          │
└──────────────────────────────────────────────────────────────────┘

1. 用户选择仓库（如：天气API）
         ↓
2. 系统读取仓库配置
   {
     baseUrl: '/api/v1/weather',
     apiUrl: 'http://localhost:8001'  ← Weather API 专用端口
   }
         ↓
3. 用户选择端点（如：/current）
         ↓
4. 构建完整 URL
   http://localhost:8001 + /api/v1/weather + /current
   = http://localhost:8001/api/v1/weather/current
         ↓
5. 发送请求并获取响应
```

### 139.5 前提条件

确保 Weather API 服务已启动：

```bash
# 终端1：启动 Weather API (端口 8001)
cd OwnerServer/weather-api
uvicorn src.main:app --reload --host 0.0.0.0 --port 8001

# 终端2：启动 API Platform (端口 8000)
cd api-platform
uvicorn src.main:app --reload --host 0.0.0.0 --port 8000

# 终端3：启动前端 (端口 3000)
cd api-platform/web
npm run dev
```

---

## 问题140：API测试工具详细使用步骤

### 140.1 工具概述

**API 测试工具**是一个统一的接口调试平台，支持：

| 功能 | 说明 |
|------|------|
| 多仓库管理 | 支持自研仓库和第三方仓库 |
| 端点浏览 | 展示所有可用 API 端点 |
| 动态表单 | 根据端点参数自动生成输入表单 |
| 请求发送 | 一键发送 API 请求 |
| 响应预览 | 美化的 JSON 响应展示 |
| 历史记录 | 保存最近 50 条请求记录 |
| curl 命令 | 自动生成可复制的 curl 命令 |
| API Key 配置 | 界面化配置访问密钥 |

### 140.2 界面布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧪 API 测试工具                                                               │
│  📦 4 个仓库  🔗 12 个接口                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  [全部] [🏠 自研] [🌐 第三方]                    [📜 历史 (0)]  [⚠️ 配置Key] │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────────┐  ┌─────────────────────────────────┐  │
│  │    仓库列表    │  │     API 端点      │  │         请求配置 & 响应         │  │
│  ├──────────────┤  ├──────────────────┤  ├─────────────────────────────────┤  │
│  │ ☁️ 天气API   │  │  📡 GET /current │  │ GET /api/v1/weather/current    │  │
│  │              │  │                  │  │                                 │  │
│  │ 🚚 物流API   │  │  📡 GET /forecast│  │ ┌─ Query 参数 ───────────────┐ │  │
│  │              │  │  📡 GET /aqi     │  │ │ city *  [北京____________]  │ │  │
│  │ 💬 短信服务   │  │  📡 GET /alerts  │  │ │ language [中文_________▼]   │ │  │
│  │              │  │                  │  │ │ unit    [摄氏度(°C)___▼]   │ │  │
│  │ 💳 支付服务   │  │  💬 POST /send   │  │ └─────────────────────────────┘ │  │
│  │              │  │                  │  │                                 │  │
│  │  [分类筛选]   │  │  💳 POST /pay   │  │ [🚀 发送请求]  [清空]           │  │
│  └──────────────┘  └──────────────────┘  │                                 │  │
│                                         ├─────────────────────────────────┤  │
│                                         │  💡 响应示例                      │  │
│                                         │  {                              │  │
│                                         │    "code": 200,                 │  │
│                                         │    "data": { ... }              │  │
│                                         │  }                              │  │
│                                         └─────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 140.3 使用步骤

#### 第一步：启动服务

```bash
# 1. 启动 Weather API (如果需要测试天气接口)
cd OwnerServer/weather-api
uvicorn src.main:app --reload --port 8001

# 2. 启动 API Platform
cd api-platform
uvicorn src.main:app --reload --port 8000

# 3. 启动前端
cd api-platform/web
npm run dev
```

#### 第二步：访问工具

```
浏览器访问：http://localhost:3000/developer/api-tester
```

#### 第三步：配置 API Key（可选）

如果调用受保护的 API，需要配置访问密钥：

1. 点击工具栏右侧的 **"⚠️ 配置Key"** 按钮
2. 在弹出的配置面板中输入 API Key
3. 点击 **"💾 保存"** 按钮
4. 按钮状态变为 **"🔑 已配置"**

```
配置面板示意：
┌─────────────────────────────────────────────┐
│ 🔑 API Key 配置                    [×]     │
├─────────────────────────────────────────────┤
│ 访问密钥 (X-Access-Key)                     │
│ [••••••••••••••••••••••••] [👁️]           │
│ 请输入 Owner Server 的访问密钥...            │
│                                             │
│ [💾 保存]  [🗑️ 清除]  [取消]              │
└─────────────────────────────────────────────┘
```

#### 第四步：选择仓库

1. 在左侧 **"仓库列表"** 中查看所有仓库
2. 可以使用顶部标签筛选：
   - **全部**：显示所有仓库
   - **🏠 自研**：只显示自研仓库（如天气API、物流API）
   - **🌐 第三方**：只显示第三方仓库（如短信、支付）
3. 点击目标仓库（如：天气API）选中

```
仓库卡片示意：
┌──────────────────────────────┐
│ ☁️                            │
│ 天气API                       │
│ 提供实时天气、天气预报...      │
│ ✓已选择                       │
└──────────────────────────────┘
```

#### 第五步：选择端点

1. 在中间 **"API 端点"** 面板中查看选中仓库的所有端点
2. 点击目标端点（如：GET /current 实时天气）选中

```
端点列表示意：
┌──────────────────────────────┐
│ GET  📡 实时天气              │
│ /current                      │
├──────────────────────────────┤
│ GET  📡 天气预报              │
│ /forecast                     │
├──────────────────────────────┤
│ GET  🌿 空气质量              │
│ /aqi                          │
├──────────────────────────────┤
│ GET  ⚠️ 天气预警              │
│ /alerts                       │
└──────────────────────────────┘
```

#### 第六步：填写参数

右侧 **"请求配置"** 面板会自动显示该端点的参数表单：

1. **必填参数**：标有红色 `*` 的字段必须填写
2. **可选参数**：有默认值的字段可以不填
3. **参数类型**：
   - `string`：文本输入框
   - `select`：下拉选择框
   - `number`：数字输入框
   - `boolean`：开关控件

```
参数表单示意：
┌─ Query 参数 ─────────────────────────────┐
│                                         │
│ city *                                  │
│ [北京________________________________]  │
│ 城市名称（支持中文或拼音）                │
│                                         │
│ language                                │
│ [中文________________________▼]          │
│ 返回语言                                 │
│                                         │
│ unit                                    │
│ [摄氏度(°C)________________▼]           │
│ 温度单位                                 │
│                                         │
└─────────────────────────────────────────┘
```

#### 第七步：发送请求

1. 点击 **"🚀 发送请求"** 按钮
2. 等待请求完成（显示加载动画）
3. 查看响应结果

```
发送按钮：
┌─────────────────┐
│ 🚀 发送请求      │
└─────────────────┘
```

#### 第八步：查看响应

响应面板会自动显示请求结果：

| 内容 | 说明 |
|------|------|
| **状态码** | HTTP 状态码（200=成功，400=参数错误，401=未授权） |
| **响应时间** | 请求耗时（毫秒） |
| **curl 命令** | 可复制的 curl 命令 |
| **JSON 响应** | 美化的响应数据 |

```
响应面板示意：
┌─────────────────────────────────────────────┐
│ 200 OK                          245ms      │
├─────────────────────────────────────────────┤
│ 📋 curl 命令                      [复制]   │
│ ┌─────────────────────────────────────────┐ │
│ │ curl -X GET "http://localhost:8001/..." │ │
│ └─────────────────────────────────────────┘ │
├─────────────────────────────────────────────┤
│ 📦 响应体                                  │
│ ┌─────────────────────────────────────────┐ │
│ │ {                                       │ │
│ │   "code": 200,                         │ │
│ │   "data": {                            │ │
│ │     "city": "北京",                     │ │
│ │     "temperature": 25.5,               │ │
│ │     "humidity": 60,                    │ │
│ │     ...                                │ │
│ │   }                                    │ │
│ │ }                                       │ │
│ └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

#### 第九步：使用历史记录

1. 点击 **"📜 历史"** 按钮打开历史记录面板
2. 点击任意历史记录可以恢复之前的请求状态
3. 最多保存最近 50 条记录

```
历史记录面板：
┌──────────────────────────────────────────────────────────────┐
│ 📜 最近请求                                           [×]   │
├──────────────────────────────────────────────────────────────┤
│ 状态  时间      仓库     接口      测试人员      耗时         │
│ 200  14:30:25  天气API  实时天气  张三         245ms        │
│ 200  14:28:10  天气API  天气预报  李四         312ms        │
│ 401  14:25:05  短信服务 发送短信  王五         89ms         │
└──────────────────────────────────────────────────────────────┘
```

### 140.4 常见问题

#### Q1：显示 "⚠️ 未配置API Key" 怎么办？

**A**：如果调用的 API 需要认证（authType: api_key），需要先配置 API Key。点击工具栏的 "⚠️ 配置Key" 按钮进行配置。

#### Q2：请求返回 404 Not Found

**A**：
1. 确认目标 API 服务是否已启动
2. 确认仓库配置中的 `apiUrl` 是否正确
3. Weather API 运行在端口 8001，不是 8000

#### Q3：请求返回 401 Unauthorized

**A**：API Key 无效或未配置。请检查：
1. 是否已配置正确的 API Key
2. API Key 是否在 Owner Portal 中创建
3. API Key 是否已启用

#### Q4：如何测试需要认证的 API？

1. 首先在 Owner Portal 创建 API Key
2. 在测试工具中点击 "⚠️ 配置Key" 输入密钥
3. 然后正常选择仓库和端点发送请求

### 140.5 快捷操作

| 操作 | 说明 |
|------|------|
| **发送请求** | 点击 "🚀 发送请求" 或按 `Ctrl + Enter` |
| **清空响应** | 点击 "清空" 按钮 |
| **复制 curl** | 点击响应面板中的 "复制" 按钮 |
| **复制 JSON** | 点击响应体中的 "复制" 按钮 |
| **查看历史** | 点击 "📜 历史" 按钮或按 `Ctrl + H` |
| **切换仓库** | 点击左侧不同的仓库卡片 |
| **切换端点** | 点击中间不同的端点 |

---

## 问题141：历史记录和调用明细增强

### 141.1 需求背景

**发现时间**：2026-04-22 14:31

用户反馈两个问题：
1. 历史记录需要显示时间和测试人员
2. 消费明细页面需要显示测试的请求参数内容

### 141.2 解决方案

#### 2.1 历史记录增强

**类型定义更新** - `types/api-tester.ts`：
```typescript
export interface RequestHistory {
  id: string;
  timestamp: string;
  tester?: string;           // 新增：测试人员
  repository: string;
  repositoryId?: string;      // 新增：仓库ID
  endpoint: string;
  endpointId?: string;        // 新增：端点ID
  method: HttpMethod;
  params: Record<string, any>;
  requestBody?: Record<string, any>;  // 新增：请求体参数
  status: number;
  duration: number;
  response: any;
  error?: string;
}
```

**历史记录显示优化**：
| 列 | 说明 |
|----|------|
| 状态 | HTTP 状态码，带颜色标识（绿色=成功，红色=失败） |
| 时间 | 请求时间（格式：HH:mm:ss） |
| 仓库 | 调用所属的仓库名称 |
| 接口 | 调用的端点名称 |
| 测试人员 | 当前登录用户名 |
| 耗时 | 请求耗时（毫秒） |

#### 2.2 消费明细增强

**类型定义更新** - `api/billing.ts`：
```typescript
export interface ConsumptionDetail {
  id: number
  repo_id: string | null
  repo_name: string
  endpoint: string
  request_params?: string  // 新增：请求参数 (JSON字符串)
  tester?: string           // 新增：测试人员
  tokens_used: number
  cost: number
  created_at: string
}
```

**消费明细表格新增列**：

| 列 | 说明 |
|----|------|
| 测试人员 | 调用者的用户名 |
| 请求参数 | 请求参数预览（鼠标悬停显示完整JSON） |

### 141.3 界面展示

#### 历史记录面板

```
┌──────────────────────────────────────────────────────────────┐
│ 📜 最近请求                                           [×]   │
├──────────────────────────────────────────────────────────────┤
│ 状态  时间      仓库      接口      测试人员     耗时        │
├──────────────────────────────────────────────────────────────┤
│ 200  14:30:25  天气API   实时天气  👤 张三     245ms        │
│ 200  14:28:10  天气API   天气预报  👤 李四     312ms        │
│ 200  14:25:55  物流API   快递查询  👤 张三     189ms        │
│ 401  14:25:05  短信服务  发送短信  👤 王五     89ms         │
└──────────────────────────────────────────────────────────────┘
```

#### 消费明细表格

| 时间 | 仓库 | 接口 | 测试人员 | 请求参数 | 费用 |
|------|------|------|----------|----------|------|
| 2026/4/22 14:30:25 | 天气API | /current | 张三 | city=北京, unit=c | ¥0.10 |
| 2026/4/22 14:28:10 | 天气API | /forecast | 李四 | city=上海, days=3 | ¥0.15 |
| 2026/4/22 14:25:55 | 物流API | /query | 张三 | order_no=SF123... | ¥0.08 |

**请求参数悬停效果**：
```
┌─────────────────────────────────────┐
│ 完整请求参数：                      │
│ {                                   │
│   "city": "北京",                   │
│   "unit": "c",                      │
│   "language": "zh-CN"               │
│ }                                   │
└─────────────────────────────────────┘
```

### 141.4 文件变更清单

| 文件路径 | 操作 | 说明 |
|----------|------|------|
| `types/api-tester.ts` | 修改 | RequestHistory 添加 tester、repositoryId 等字段 |
| `api/billing.ts` | 修改 | ConsumptionDetail 添加 request_params、tester 字段 |
| `pages/developer/ApiTester.tsx` | 修改 | 集成用户信息、显示测试人员 |
| `pages/developer/ApiTester.module.css` | 修改 | 添加时间和测试人员样式 |
| `pages/developer/ConsumptionDetails.tsx` | 修改 | 添加测试人员和请求参数列 |

### 141.5 后端依赖（待实现）

消费明细页面需要后端 API 支持以下字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `request_params` | string | 请求参数字符串（JSON格式） |
| `tester` | string | 测试人员用户名 |

**后端需要修改的位置**：
- `src/api/v1/quota.py` - 消费明细查询接口
- 数据库表 `api_usage_logs` 或 `consumption_records` - 添加对应字段

### 141.6 测试验证

1. **历史记录测试**：
   - [ ] 发送请求后查看历史记录是否显示当前用户
   - [ ] 查看时间格式是否正确
   - [ ] 点击历史记录是否正确恢复请求状态

2. **消费明细测试**（后端完成后）：
   - [ ] 消费明细表格是否显示测试人员列
   - [ ] 鼠标悬停请求参数是否显示完整 JSON
   - [ ] 消费明细列表是否包含请求参数预览


## 问题142：仓库管理显示已上线但工作台显示下线

### 142.1 问题描述

**发现时间**：2026-04-22 21:51

登录 Owner 平台后发现：
- **仓库管理页面**：weather-api 仓库显示状态为 "已上线"（绿色）
- **工作台页面**：weather-api 仓库显示状态为 "已下线"（橙色）

同一仓库在不同页面显示的状态不一致。

### 142.2 问题原因分析

经代码分析，发现问题在于 **Dashboard.tsx 和 Repos.tsx 使用了不同的状态判断逻辑**：

| 页面 | 文件 | 判断条件 | 数据库状态值 | 结果 |
|------|------|----------|-------------|------|
| 仓库管理 | `Repos.tsx` | `status === 'online'` → "已上线" | `online` | ✅ 正确 |
| 工作台 | `Dashboard.tsx` | `status === 'active'` → "运行中" | `online` | ❌ 错误 |

**根本原因**：
- 数据库存储的仓库状态是 `online`
- `Repos.tsx` 正确使用 `status === 'online'`
- `Dashboard.tsx` 错误使用 `status === 'active'`，导致无法匹配，显示为"已下线"

### 142.3 解决方案

**修改文件**：`api-platform/web/src/pages/owner/Dashboard.tsx`

将错误的状态判断逻辑修改为与 `Repos.tsx` 保持一致：

```typescript
// 修复前
{ 
  title: '状态', 
  dataIndex: 'status', 
  key: 'status',
  render: (status: string) => (
    <Tag color={status === 'active' ? 'green' : 'orange'}>
      {status === 'active' ? '运行中' : '已下线'}
    </Tag>
  )
},

// 修复后
{ 
  title: '状态', 
  dataIndex: 'status', 
  key: 'status',
  render: (status: string) => {
    const statusMap: Record<string, { color: string; text: string }> = {
      pending: { color: 'orange', text: '待审核' },
      approved: { color: 'blue', text: '已审核（待上线）' },
      rejected: { color: 'red', text: '已拒绝' },
      online: { color: 'green', text: '已上线' },
      offline: { color: 'default', text: '已下线' },
    }
    const config = statusMap[status] || { color: 'default', text: status || '未知' }
    return <Tag color={config.color}>{config.text}</Tag>
  }
},
```

### 142.4 涉及文件清单

| 文件路径 | 修改类型 | 说明 |
|----------|----------|------|
| `api-platform/web/src/pages/owner/Dashboard.tsx` | 修改 | 状态判断逻辑统一为与 Repos.tsx 一致 |

### 142.5 状态值说明

系统中仓库可能的状态值：

| 状态值 | 说明 | 标签颜色 |
|--------|------|----------|
| `pending` | 待审核 | 橙色 |
| `approved` | 已审核（待上线） | 蓝色 |
| `rejected` | 已拒绝 | 红色 |
| `online` | 已上线 | 绿色 |
| `offline` | 已下线 | 灰色 |

### 142.6 验证方法

1. **刷新工作台页面**
2. 确认 weather-api 仓库状态显示为 "已上线"（绿色）
3. 确认与仓库管理页面状态一致

### 142.7 预防措施

为避免类似问题，建议：
1. **统一状态枚举**：在类型定义文件中定义统一的状态映射
2. **组件复用**：将状态显示逻辑抽取为独立组件
3. **代码审查**：修改状态相关代码时对比多个页面的实现

---

**文档更新记录**：

| 日期 | 版本 | 更新内容 | 更新人 |
:|------|------|----------|--------|
| 2026-04-22 | V2.22 | 新增问题 142：仓库管理显示已上线但工作台显示下线 | AI |

---

## 问题143：工作台与仓库管理仓库名称显示不一致

### 143.1 问题描述

**发现时间**：2026-04-22 21:54

同一仓库在不同页面显示的名称不同：
- **仓库管理页面**：显示 "天气 API"（显示名称）
- **工作台页面**：显示 "weather-api"（仓库标识）

### 143.2 问题原因

两个页面使用了不同的仓库名称显示逻辑：

| 页面 | 文件 | 显示逻辑 | 示例 |
|------|------|----------|------|
| 仓库管理 | `Repos.tsx` | `record.display_name \|\| name` | "天气 API" |
| 工作台 | `Dashboard.tsx` | `name` | "weather-api" |

### 143.3 解决方案

**修改文件**：`api-platform/web/src/pages/owner/Dashboard.tsx`

将仓库名称列的渲染逻辑修改为与 `Repos.tsx` 保持一致：

```typescript
// 修复前
{ 
  title: '仓库', 
  dataIndex: 'name', 
  key: 'name',
  render: (name: string) => <Text strong>{name}</Text>
},

// 修复后
{ 
  title: '仓库', 
  dataIndex: 'name', 
  key: 'name',
  render: (name: string, record: any) => (
    <Space direction="vertical" size={0}>
      <Text strong>{record.display_name || name}</Text>
      <Text type="secondary" style={{ fontSize: 12 }}>{name}</Text>
    </Space>
  )
},
```

### 143.4 涉及文件清单

| 文件路径 | 修改类型 | 说明 |
|----------|----------|------|
| `api-platform/web/src/pages/owner/Dashboard.tsx` | 修改 | 仓库名称显示逻辑与 Repos.tsx 保持一致 |

### 143.5 验证方法

1. 刷新工作台页面
2. 确认仓库名称显示为 "天气 API"（而非 "weather-api"）
3. 确认仓库标识以次要文本显示在下方

---

## 问题144：工作台统计数据显示为0但实际有调用

### 144.1 问题描述

**发现时间**：2026-04-22 21:57

用户使用集成测试工具调用了天气API，但 Owner 工作台显示的统计数据全为0：
- 今日调用：0
- 本周调用：0
- 总调用：0

实际数据库中已有 API 调用记录。

### 144.2 问题原因分析

**时区不一致导致的数据错位**：

| 操作 | 使用的时区 | 问题 |
|------|-----------|------|
| API调用日志记录 | `datetime.now()` 北京时间 | 记录时间正确 |
| 统计查询 | `datetime.utcnow()` UTC时间 | 查询范围错误 |

**具体表现**（假设当前是北京时间 21:57 2026-04-22）：

```
日志记录时间：2026-04-22 21:57:00 (北京时间)
UTC时间：2026-04-22 13:57:00 (UTC时间)

统计查询的 "今天"：
  - UTC: 2026-04-22 00:00:00 ~ 2026-04-22 23:59:59
  - 北京: 2026-04-22 08:00:00 ~ 2026-04-23 07:59:59

结果：UTC "今天" 不包含北京时间 21:57 的记录！
```

### 144.3 解决方案

统一使用 **北京时间**（`datetime.now()`）进行统计查询，与日志记录时间保持一致。

#### 修复1：repositories.py - 仓库统计接口

**文件**：`api-platform/src/api/v1/repositories.py`

```python
# 修复前
today = datetime.utcnow().date()
week_ago = datetime.utcnow() - timedelta(days=7)

# 修复后
now = datetime.now()  # 北京时间
today = now.date()
week_ago = now - timedelta(days=7)
```

#### 修复2：quota.py - /info/{key_id} 接口

**文件**：`api-platform/src/api/v1/quota.py`

```python
# 修复前
now = datetime.utcnow()
today = now.replace(hour=0, minute=0, second=0, microsecond=0)

# 修复后
now = datetime.now()  # 北京时间
today = now.replace(hour=0, minute=0, second=0, microsecond=0)
```

#### 修复3：quota.py - /overview 接口

**文件**：`api-platform/src/api/v1/quota.py`

```python
# 修复前
now = datetime.utcnow()
today = now.replace(hour=0, minute=0, second=0, microsecond=0)

# 修复后
now = datetime.now()  # 北京时间
today = now.replace(hour=0, minute=0, second=0, microsecond=0)
```

#### 修复4：quota.py - /top-repos 接口

**文件**：`api-platform/src/api/v1/quota.py`

```python
# 修复前
start_date = datetime.utcnow() - timedelta(days=days)

# 修复后
start_date = datetime.now() - timedelta(days=days)  # 北京时间
```

### 144.4 涉及文件清单

| 文件路径 | 修改内容 |
|----------|----------|
| `api-platform/src/api/v1/repositories.py` | 仓库统计接口 `/repositories/{repo_id}/stats` |
| `api-platform/src/api/v1/quota.py` | 配额接口 `/quota/info/{key_id}`、`/quota/overview`、`/quota/top-repos` |

### 144.5 时区规则总结

| 场景 | 应使用的时区 | 原因 |
|------|-------------|------|
| 日志记录 `created_at` | `datetime.now()` | 存储北京时间，便于阅读 |
| 统计查询 `>= start_date` | `datetime.now()` | 与日志记录保持一致 |
| 日期比较 `== today` | `datetime.now().date()` | 与日志记录保持一致 |

### 144.6 验证方法

1. **重启后端服务**
   ```bash
   cd D:\Work_Area\AI\API-Agent\api-platform
   python -m uvicorn src.main:app --reload --port 8000
   ```

2. **调用测试API**
   ```bash
   curl "http://localhost:8000/api/v1/repositories/weather-api/api/v1/weather/current?city=北京"
   ```

3. **检查工作台数据**
   - 今日调用应显示 > 0
   - 刷新页面后数据应正确更新

4. **检查数据库**
   ```sql
   -- 查看日志记录时间
   SELECT id, created_at, repo_id FROM api_call_logs ORDER BY id DESC LIMIT 5;
   ```

### 144.7 预防措施

为避免类似问题：
1. **统一时区规范**：整个项目统一使用北京时间（`datetime.now()`）
2. **代码审查**：新增统计查询代码时检查时区使用
3. **测试用例**：添加时区边界测试用例

---

**文档更新记录**：

| 日期 | 版本 | 更新内容 | 更新人 |
:|------|------|----------|--------|
| 2026-04-22 | V2.23 | 新增问题 143：工作台与仓库管理仓库名称显示不一致 | AI |
| 2026-04-22 | V2.24 | 新增问题 144：工作台统计数据显示为0但实际有调用，修复时区不一致问题 | AI |

---

## 问题145：工作台统计数据API返回404

### 145.1 问题描述

**发现时间**：2026-04-22 22:03

重启后端服务后，工作台页面仍然无法获取统计数据，浏览器控制台报错：

```
GET /repositories/60022f1f-309e-4792-ae35-bd534f0f83e5/stats 404
"detail": "Repository '60022f1f-309e-4792-ae35-bd534f0f83e5' not found"
```

### 145.2 问题原因分析

**FastAPI 路由顺序冲突**：

| 路由 | 行号 | 定义顺序 |
|------|------|----------|
| `/{repo_slug}` | 440 | 先定义 |
| `/{repo_id}/stats` | 1202 | 后定义 |

当请求 `/repositories/60022f1f-309e-4792-ae35-bd534f0f83e5/stats` 时：

1. FastAPI 按照定义顺序匹配路由
2. `/{repo_slug}` 先被匹配
3. UUID 被当作 `repo_slug` 参数
4. 仓库查询失败，返回 404

### 145.3 解决方案

**修改文件**：`api-platform/src/api/v1/repositories.py`

将 `/{repo_id}/stats` 路由移动到 `/{repo_slug}` 之前：

```python
# 在第440行之前插入
@router.get("/{repo_id}/stats", response_model=BaseResponse[dict])
async def get_repository_stats(...):
    # ... 原有代码 ...

# 删除原位置（第1202行之后）的重复函数定义
```

**修改原则**：FastAPI 按照路由定义顺序匹配，**更具体的路径应该放在前面**。

### 145.4 涉及文件清单

| 文件路径 | 修改类型 | 说明 |
|----------|----------|------|
| `api-platform/src/api/v1/repositories.py` | 路由重排序 | 将 `/{repo_id}/stats` 移到 `/{repo_slug}` 之前 |

### 145.5 验证方法

1. 重启后端服务
2. 刷新工作台页面
3. 打开浏览器开发者工具，确认 `/stats` 请求返回 200
4. 统计数据应正确显示（非0）

### 145.6 预防措施

为避免类似问题：

1. **路由顺序规范**：在 FastAPI 中，更具体的路由（如 `/{id}/stats`）应该放在更通用的路由（如 `/{slug}`）之前
2. **代码审查**：新增路由时检查是否与现有路由存在路径冲突
3. **自动化测试**：添加路由匹配测试用例

---

**文档更新记录**：

| 日期 | 版本 | 更新内容 | 更新人 |
:|------|------|----------|--------|
| 2026-04-22 | V2.25 | 新增问题 145：工作台统计数据API返回404，修复FastAPI路由顺序问题 | AI |

---





















