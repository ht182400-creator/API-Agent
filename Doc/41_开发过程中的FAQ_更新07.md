# 开发过程中的FAQ - 更新07

本文档记录开发过程中遇到的问题和解决方案。

**文档更新历史**：
- 2026-05-08：创建本文档，记录数据库和本地时间统一问题

---

## 问题151：数据库时间与本地时间统一

**更新时间**：2026-05-08

### 151.1 问题描述

**用户反馈**：
1. 主动刷新二维码失败
2. 前后端倒计时不同步，显示剩余时间不准确
3. 对账和充值记录时间不统一

### 151.2 问题分析

**根本原因**：
1. **Naive datetime 问题**：代码中使用 `datetime.utcnow()` 返回的是 naive datetime（无时区信息），数据库存储时间与实际 UTC 时间不一致
2. **时区混淆**：
   - 服务器时区为 Asia/Shanghai（UTC+8）
   - 代码使用 `datetime.utcnow()` 返回的时间被当作本地时间处理
   - 导致时间计算偏差 8 小时
3. **前后端时间基准不同**：
   - 前端使用页面加载时间开始倒计时
   - 后端使用数据库 `created_at + 10分钟` 判定过期
   - 两者时间基准不一致

**问题代码示例**：
```python
# 错误：返回 naive datetime
created_at = Column(DateTime, default=datetime.utcnow)

# 错误：naive datetime 比较
if datetime.utcnow() > expiry_time:
    raise ValidationError("订单已过期")
```

### 151.3 解决方案

#### 151.3.1 添加 UTC 时间辅助函数

**修改文件**：`src/utils/helpers.py`

**修改内容**：
```python
def utc_now() -> datetime:
    """
    获取当前 UTC 时间（aware datetime，带时区信息）
    用于代码中获取当前时间

    Returns:
        datetime: 当前 UTC 时间（带 timezone.utc 时区信息）
    """
    return datetime.now(timezone.utc)


def get_utc_now() -> callable:
    """
    返回一个用于 SQLAlchemy default 参数的可调用对象
    SQLAlchemy 要求 default 必须是可调用对象，不能是具体值

    Usage:
        created_at = Column(DateTime, default=get_utc_now())

    Returns:
        callable: 返回当前 UTC 时间的可调用对象
    """
    return lambda: datetime.now(timezone.utc)
```

#### 151.3.2 统一替换所有时间函数

**替换范围**：30个文件，88处代码

| 替换前 | 替换后 |
|--------|--------|
| `datetime.utcnow` | `datetime.now(timezone.utc)` |
| `default=datetime.utcnow` | `default=get_utc_now()` |
| `onupdate=datetime.utcnow` | `onupdate=get_utc_now()` |

**修改的模型文件**（12个）：
- `src/models/payment.py`
- `src/models/billing.py`
- `src/models/user.py`
- `src/models/api_key.py`
- `src/models/repository.py`
- `src/models/audit_log.py`
- `src/models/notification.py`
- `src/models/role.py`
- `src/models/system_config.py`
- `src/models/adapter.py`
- `src/models/pricing_config.py`
- `src/models/user_operation_log.py`

**修改的服务文件**（18个）：
- `src/services/payment_service.py`
- `src/services/billing_service.py`
- `src/services/auth_service.py`
- `src/services/account_service.py`
- `src/services/notification_service.py`
- `src/services/quota_service.py`
- `src/services/repo_service.py`
- `src/services/reconciliation_scheduler.py`
- `src/api/v1/payment.py`
- `src/api/v1/billing.py`
- `src/api/v1/user.py`
- `src/api/v1/repositories.py`
- `src/api/v1/logs.py`
- `src/api/v1/superadmin.py`
- `src/api/v1/admin.py`
- `src/api/v1/admin_billing.py`
- `src/api/v1/admin_reconciliation.py`
- `src/api/v1/admin_pricing_config.py`

#### 151.3.3 修复支付订单过期检查

**修改文件**：`src/services/payment_service.py`

**修改内容**：
```python
if payment.created_at:
    from datetime import timedelta, timezone
    # 将 naive UTC datetime 转换为 aware datetime 以便正确比较
    created_at_utc = payment.created_at.replace(tzinfo=timezone.utc)
    expiry_time = created_at_utc + timedelta(minutes=10)
    now_utc = datetime.now(timezone.utc)
    if now_utc > expiry_time:
        raise ValidationError("订单已过期，请重新下单")
```

### 151.4 时间处理原则

| 场景 | 处理方式 | 说明 |
|------|---------|------|
| 数据库存储 | UTC 时间 | 使用 aware datetime（带时区信息） |
| 代码获取当前时间 | `datetime.now(timezone.utc)` | 明确的 UTC 时间 |
| 时间比较 | 统一用 UTC 时间比较 | 确保时区一致性 |
| 前端显示 | 转换为本地时区 | 根据用户浏览器时区显示 |
| API 返回 | 后端计算 `expires_in` | 前端直接使用，避免时区转换 |

### 151.5 相关文件修改清单

| 文件路径 | 修改内容 | 说明 |
|----------|----------|------|
| `src/utils/helpers.py` | 新增函数 | 添加 `get_utc_now()` 函数 |
| `src/models/*.py` (12个) | 修改 | 所有模型的时间字段使用 `get_utc_now()` |
| `src/services/*.py` (8个) | 修改 | 所有服务代码使用 `datetime.now(timezone.utc)` |
| `src/api/v1/*.py` (10个) | 修改 | 所有 API 代码使用 `datetime.now(timezone.utc)` |

### 151.5.1 额外修复：timezone 导入缺失

**问题**：替换 `datetime.utcnow` 为 `datetime.now(timezone.utc)` 后，部分文件未正确导入 `timezone`，导致运行时错误：`NameError: name 'timezone' is not defined`

**修复**：在以下文件中添加 `timezone` 到 datetime 导入：

**服务文件** (8个)：
- `src/services/payment_service.py`
- `src/services/account_service.py`
- `src/services/auth_service.py`
- `src/services/billing_service.py`
- `src/services/notification_service.py`
- `src/services/quota_service.py`
- `src/services/reconciliation_scheduler.py`
- `src/services/repo_service.py`

**API文件** (9个)：
- `src/api/v1/admin.py`
- `src/api/v1/admin_billing.py`
- `src/api/v1/admin_pricing_config.py`
- `src/api/v1/admin_reconciliation.py`
- `src/api/v1/billing.py`
- `src/api/v1/logs.py`
- `src/api/v1/repositories.py`
- `src/api/v1/superadmin.py`
- `src/api/v1/user.py`

### 151.6 验证方法

#### 151.6.1 时间字段验证

```sql
-- 检查数据库时间是否为 UTC 时间
SELECT created_at, NOW(), created_at AT TIME ZONE 'UTC' 
FROM payments 
ORDER BY created_at DESC LIMIT 5;
```

#### 151.6.2 支付二维码验证

1. 创建支付订单，观察 `expires_in` 是否正确显示
2. 等待 5 分钟，检查前端倒计时是否与后端一致
3. 点击刷新二维码，确认未过期订单可以正常刷新
4. 过期订单应该提示"订单已过期，请重新下单"

#### 151.6.3 前后端同步验证

1. 查看浏览器开发者工具 Network 面板
2. 创建订单后检查响应中的 `expires_in` 值
3. 使用 JavaScript 控制台验证：
```javascript
// 验证前端计算
const expiresIn = 599; // API 返回值
const endTime = Date.now() + expiresIn * 1000;
console.log('到期时间:', new Date(endTime).toLocaleString());
```

### 151.7 Naive vs Aware Datetime 区别

```python
# Naive datetime（无时区信息）- 问题根源
>>> from datetime import datetime
>>> datetime.utcnow()
datetime.datetime(2026, 5, 8, 1, 24, 30, 123456)  # 无 tzinfo

# Aware datetime（有时区信息）- 正确做法
>>> from datetime import datetime, timezone
>>> datetime.now(timezone.utc)
datetime.datetime(2026, 5, 8, 1, 24, 30, 123456, tzinfo=datetime.timezone.utc)

# 时间比较问题
>>> import datetime
>>> naive = datetime.datetime(2026, 5, 8, 9, 24)  # 假设是 UTC 时间
>>> aware = datetime.datetime(2026, 5, 8, 9, 24, tzinfo=datetime.timezone.utc)
>>> naive > aware  # RuntimeWarning: comparison of naive and aware datetimes
```

### 151.8 数据库时区配置

**PostgreSQL 会话时区设置**：
```python
# src/config/database.py
async def init_db():
    async with async_engine.begin() as conn:
        # 设置会话时区为 UTC
        await conn.execute(text("SET TIME ZONE 'UTC'"))
        await conn.run_sync(Base.metadata.create_all)
```

**建议**：在数据库连接字符串中指定时区参数：
```
postgresql://user:pass@host:5432/dbname?options=-c%20timezone%3Dutc
```

### 151.9 后续优化建议

1. **时区配置化**：将时区设置移到配置文件
2. **统一日志时间**：所有日志使用 UTC 时间
3. **前端时间组件**：统一使用 `dayjs` 或 `date-fns` 处理时区
4. **监控告警**：添加时间相关异常的告警规则

---
