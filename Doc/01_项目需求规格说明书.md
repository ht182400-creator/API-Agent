# API服务改造项目需求规格说明书

## 文档控制

| 项目 | 内容 |
|------|------|
| 文档编号 | PRD-API-2026-001 |
| 版本号 | V1.0 |
| 创建日期 | 2026-04-16 |
| 状态 | 已批准 |
| 保密级别 | 内部使用 |

---

## 第一章 项目概述

### 1.1 项目背景

本项目旨在将现有的课程项目代码仓库改造成可对外交付的商业化API服务产品。随着产品化需求的明确，需要建立完整的API服务体系，包括用户认证、权限管理、业务接口、管理后台、监控运维等能力，以支撑未来的商业化运营。

### 1.2 项目目标

**总体目标**：在12周内完成从代码仓库到可交付API服务产品的改造，建立完整的运营支撑体系。

**具体目标**：

| 目标编号 | 目标描述 | 量化指标 |
|----------|----------|----------|
| OBJ-001 | 完成代码仓库Docker容器化 | 镜像构建成功率100% |
| OBJ-002 | 实现标准化的API服务体系 | 支持JWT+API Key双认证 |
| OBJ-003 | 建立完整的部署架构 | 支持Docker Compose一键部署 |
| OBJ-004 | 开发管理后台 | 支持租户、API Key、统计管理 |
| OBJ-005 | 交付完整文档体系 | 部署文档、API文档、运维手册 |
| OBJ-006 | 通过验收测试 | 测试覆盖率>=70%，P0用例100%通过 |

### 1.3 项目范围

**纳入范围**：
- 现有代码仓库的Docker容器化改造
- 用户认证模块（JWT）
- API Key管理与鉴权
- 核心业务API接口
- Nginx反向代理网关
- PostgreSQL数据库集成
- Redis缓存集成
- MinIO对象存储集成
- 管理后台开发
- 部署架构设计与实施
- 文档体系建设

**不纳入范围**：
- 原有课程项目业务逻辑的大规模重构
- 移动端应用开发
- 多语言国际化
- 高端商业功能（如计费结算）

---

## 第二章 业务需求详述

### 2.1 用户角色分析

| 角色 | 描述 | 主要操作 |
|------|------|----------|
| API调用方 | 使用API服务的外部开发者或系统 | 注册、申请Key、调用API、查看使用统计 |
| 租户管理员 | 管理租户内的用户和资源 | 租户用户管理、Key分配、权限配置 |
| 系统管理员 | 负责整个系统的运维管理 | 系统配置、监控查看、问题处理 |
| 开发者 | 进行API调用开发的人员 | 阅读文档、测试API、集成开发 |

### 2.2 业务场景

**场景一：外部开发者接入**
张三是一名第三方开发者，希望接入本API服务为自己的应用提供能力支持。他首先需要注册账号，申请API Key，然后阅读API文档，在他的应用中集成API调用。系统会记录他的使用量，他可以通过后台查看调用统计。

**场景二：多租户管理**
某企业A购买了API服务，需要给内部多个开发团队分配不同的访问权限。租户管理员可以创建子账号，为每个子账号分配不同的API Key，设置不同的调用配额，实现灵活的权限管理。

**场景三：运营监控**
系统管理员需要实时监控系统运行状态，包括API响应时间、错误率、各租户使用量等。当出现异常时，需要能够快速定位问题，查看相关日志。

### 2.3 业务流程

```
┌─────────────────────────────────────────────────────────────────────┐
│                        API服务使用流程                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  1. 注册/登录                                                       │
│     填写信息 ──► 验证邮箱 ──► 创建账号 ──► 登录系统                  │
│                                                                      │
│  2. API Key申请                                                    │
│     登录 ──► 进入控制台 ──► 申请API Key ──► 获取Key和Secret        │
│                                                                      │
│  3. API调用                                                         │
│     阅读文档 ──► 获取调用示例 ──► 集成到应用 ──► 调用API            │
│                                                                      │
│  4. 使用统计                                                        │
│     进入控制台 ──► 查看调用统计 ──► 下载报表                        │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 第三章 功能需求详述

### 3.1 用户认证模块

**FR-AUTH-001 用户注册**
- 支持邮箱注册
- 密码强度校验（至少8位，包含大小写字母和数字）
- 注册后发送验证邮件
- 邮箱验证后才可使用

**FR-AUTH-002 用户登录**
- 邮箱+密码登录
- 登录失败锁定（连续5次失败锁定30分钟）
- JWT Token生成（access_token有效期1小时，refresh_token有效期7天）

**FR-AUTH-003 Token管理**
- Token刷新机制
- Token撤销功能
- 多设备登录管理

### 3.2 API Key管理模块

**FR-KEY-001 Key生成**
- 自动生成32位唯一Key
- 生成对应Secret用于签名验证
- 支持自定义Key名称和备注

**FR-KEY-002 Key管理**
- 查看已申请的Key列表
- 设置Key的有效期
- 启用/禁用Key
- 删除Key
- 每个租户最多支持10个Key

**FR-KEY-003 Key认证**
- API Key + Secret签名验证
- 支持在请求头或参数中传递Key
- 支持签名时效性校验（防止重放攻击）

### 3.3 核心业务API模块

**FR-API-001 RESTful API设计**
- 统一的URL规范：/api/v1/{resource}
- 统一的请求格式：JSON
- 统一的响应格式：{code, message, data}
- 标准的HTTP状态码使用

**FR-API-002 API文档**
- Swagger/OpenAPI 3.0规范
- 自动从代码生成文档
- 支持在线调试
- 支持文档导出

**FR-API-003 限流机制**
- 基于API Key的限流
- 默认QPS限制：100次/秒
- 默认日调用限制：10000次
- 限流后返回429状态码

### 3.4 管理后台模块

**FR-ADMIN-001 租户管理**
- 租户列表查看
- 租户创建、编辑、禁用
- 租户资源配置（QPS、调用量限制）
- 租户使用量统计

**FR-ADMIN-002 用户管理**
- 租户内用户列表
- 用户创建、编辑、禁用
- 角色分配
- 权限配置

**FR-ADMIN-003 统计报表**
- 实时调用量统计
- 按时间维度汇总（日/周/月）
- 按接口维度统计
- 按租户维度统计
- 导出Excel报表

### 3.5 系统管理模块

**FR-SYS-001 系统配置**
- API版本管理
- 全局限流配置
- 邮件服务配置
- 存储配置

**FR-SYS-002 日志管理**
- API调用日志
- 错误日志
- 操作审计日志
- 日志查询和导出

---

## 第四章 非功能需求详述

### 4.1 性能指标

| 指标名称 | 指标要求 | 说明 |
|----------|----------|------|
| API响应时间 | P95 < 200ms | 排除网络延迟 |
| API吞吐量 | 支持500 QPS | 峰值情况 |
| 并发支持 | 支持200并发 | 同时在线用户 |
| 数据库查询 | P95 < 50ms | 索引优化后 |
| 缓存命中率 | >= 80% | 热数据缓存 |

### 4.2 可用性指标

| 指标名称 | 指标要求 | 说明 |
|----------|----------|------|
| 系统可用性 | >= 99.5% | 年度统计 |
| 部署时间 | <= 30分钟 | 新服务器部署 |
| 故障恢复时间 | <= 30分钟 | MTTR |
| 计划内维护窗口 | 每月不超过4小时 | 凌晨时段 |

### 4.3 安全指标

| 指标名称 | 指标要求 | 说明 |
|----------|----------|------|
| 认证失败处理 | 5次锁定30分钟 | 防暴力破解 |
| 密码存储 | bcrypt加密 | 不可逆加密 |
| 敏感数据传输 | HTTPS加密 | 全站强制HTTPS |
| API签名验证 | HMAC-SHA256 | 防篡改 |
| 日志脱敏 | 敏感信息打码 | 手机号、身份证等 |

### 4.4 可维护性指标

| 指标名称 | 指标要求 | 说明 |
|----------|----------|------|
| 代码覆盖率 | >= 70% | 单元测试 |
| 部署方式 | 一键自动化 | docker-compose |
| 文档完整性 | 100% | API文档覆盖率 |
| 监控覆盖率 | >= 90% | 核心指标 |

---

## 第五章 数据需求

### 5.1 数据模型

**用户表（users）**：
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| email | VARCHAR(255) | 邮箱，唯一 |
| password_hash | VARCHAR(255) | 密码哈希 |
| name | VARCHAR(100) | 姓名 |
| role | ENUM | admin/tenant/user |
| tenant_id | UUID | 租户ID |
| status | ENUM | active/inactive/locked |
| email_verified | BOOLEAN | 邮箱已验证 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

**租户表（tenants）**：
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| name | VARCHAR(100) | 租户名称 |
| code | VARCHAR(50) | 租户代码 |
| status | ENUM | active/inactive |
| quota_qps | INTEGER | QPS配额 |
| quota_daily | INTEGER | 日调用配额 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

**API Key表（api_keys）**：
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| key | VARCHAR(64) | API Key |
| secret_hash | VARCHAR(255) | Secret哈希 |
| user_id | UUID | 所属用户 |
| name | VARCHAR(100) | Key名称 |
| status | ENUM | active/inactive/expired |
| expires_at | TIMESTAMP | 过期时间 |
| created_at | TIMESTAMP | 创建时间 |

**调用日志表（api_logs）**：
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| request_id | UUID | 请求ID |
| api_key_id | UUID | API Key ID |
| method | VARCHAR(10) | HTTP方法 |
| path | VARCHAR(255) | 请求路径 |
| status_code | INTEGER | 响应状态码 |
| response_time | INTEGER | 响应时间(ms) |
| ip | VARCHAR(45) | 请求IP |
| created_at | TIMESTAMP | 请求时间 |

### 5.2 数据安全

- 数据库定期备份（每日全量，每周增量）
- 备份数据保留30天
- 敏感数据加密存储
- 日志数据保留30天

---

## 第六章 接口规格

### 6.1 API基础规范

**基础URL**：https://api.example.com

**认证方式**：
1. Header方式：`Authorization: Bearer {access_token}`
2. API Key方式：`X-API-Key: {api_key}`

**通用请求头**：
```
Content-Type: application/json
Accept: application/json
X-Request-ID: {uuid}
X-API-Key: {api_key}
X-Signature: {hmac_signature}
X-Timestamp: {unix_timestamp}
```

**通用响应格式**：
```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "request_id": "uuid",
  "timestamp": 1713200000
}
```

**错误码规范**：
| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未认证或认证失败 |
| 403 | 无权限访问 |
| 404 | 资源不存在 |
| 429 | 请求过于频繁 |
| 500 | 服务器内部错误 |

### 6.2 主要API接口

**认证接口**：
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/auth/register | 用户注册 |
| POST | /api/v1/auth/login | 用户登录 |
| POST | /api/v1/auth/refresh | 刷新Token |
| POST | /api/v1/auth/logout | 登出 |

**API Key接口**：
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/keys | 创建API Key |
| GET | /api/v1/keys | 获取Key列表 |
| GET | /api/v1/keys/{id} | 获取Key详情 |
| PUT | /api/v1/keys/{id} | 更新Key |
| DELETE | /api/v1/keys/{id} | 删除Key |

**业务接口**（待定，根据原仓库功能定义）

---

## 第七章 约束条件

### 7.1 技术约束

- 后端语言：Python/Java/Node.js（基于原仓库技术栈）
- 数据库：PostgreSQL 14+
- 缓存：Redis 6+
- 消息队列：可选（视具体需求）
- 部署环境：Docker + Linux

### 7.2 时间约束

- 项目总工期：12周
- 第一阶段：2周
- 第二阶段：3周
- 第三阶段：2周
- 第四阶段：2周
- 第五阶段：3周

### 7.3 资源约束

- 开发团队：2-3人
- 测试人员：1人
- 运维支持：1人
- 预算：视公司情况

### 7.4 许可约束

- 必须确认原仓库许可证类型
- 商业使用需获得授权或进行代码重写
- 开源组件需遵守其许可证要求

---

## 第八章 验收标准

### 8.1 功能验收标准

| 序号 | 功能点 | 验收条件 |
|------|--------|----------|
| 1 | 用户注册登录 | 可正常注册、登录、登出 |
| 2 | JWT认证 | Token生成、验证、刷新正常 |
| 3 | API Key管理 | 可创建、查看、禁用、删除Key |
| 4 | API调用 | 带Key的请求可正常调用业务API |
| 5 | Swagger文档 | 在线文档可访问、可测试 |
| 6 | Nginx网关 | 反向代理、限流生效 |
| 7 | 数据库 | CRUD操作正常，索引有效 |
| 8 | Redis缓存 | 缓存读写正常，命中率>80% |
| 9 | MinIO存储 | 文件上传下载正常 |
| 10 | 管理后台 | 租户、用户、统计功能正常 |
| 11 | 一键部署 | docker-compose up可完成部署 |
| 12 | 文档交付 | 部署文档、API文档齐全可用 |

### 8.2 性能验收标准

| 序号 | 指标 | 验收条件 |
|------|------|----------|
| 1 | API响应时间 | P95 < 200ms |
| 2 | 并发能力 | 200并发无错误 |
| 3 | 部署时间 | 新环境30分钟内完成 |
| 4 | 故障恢复 | 故障后30分钟内恢复 |

### 8.3 安全验收标准

| 序号 | 指标 | 验收条件 |
|------|------|----------|
| 1 | 密码安全 | 密码加密存储，不可逆 |
| 2 | API认证 | 无Key请求被拒绝 |
| 3 | 数据隔离 | 租户间数据完全隔离 |
| 4 | 日志安全 | 敏感信息已脱敏 |

---

## 第九章 附录

### 9.1 术语表

| 术语 | 说明 |
|------|------|
| JWT | JSON Web Token，一种开放标准用于身份验证 |
| API Key | 用于识别调用方的密钥 |
| Secret | 用于签名验证的密钥 |
| 限流 | 控制API调用频率的机制 |
| 租户 | 共享系统资源但数据隔离的客户单位 |

### 9.2 参考文档

| 文档名称 | 文档编号 |
|----------|----------|
| 技术架构设计文档 | ARCH-DOC-001 |
| 接口设计文档 | API-DOC-001 |
| 数据库设计文档 | DB-DOC-001 |
| 测试计划文档 | TEST-PLAN-001 |
| 部署实施文档 | DEPLOY-DOC-001 |

---

**文档结束**
