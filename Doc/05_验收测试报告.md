# API服务改造项目验收测试报告

## 文档控制

| 项目 | 内容 |
|------|------|
| 文档编号 | TEST-RPT-API-2026-001 |
| 版本号 | V1.0 |
| 创建日期 | 2026-04-16 |
| 测试周期 | Week 9-12 |

---

## 第一章 测试概述

### 1.1 测试目的

本测试报告旨在全面验证API服务改造项目的功能完整性、性能达标情况、安全合规性，以及最终交付质量，确保项目满足需求规格说明书中的所有验收标准。

### 1.2 测试范围

| 测试类型 | 覆盖范围 | 占比 |
|----------|----------|------|
| 单元测试 | 核心模块、认证、工具函数 | 70% |
| 集成测试 | API接口、数据库、缓存、存储 | 20% |
| E2E测试 | 核心业务流程 | 10% |

### 1.3 测试环境

| 环境 | 配置 | 用途 |
|------|------|------|
| 测试环境 | 2核4G × 3台 | 功能测试、集成测试 |
| 预生产环境 | 4核8G × 3台 | 性能测试、E2E测试 |

---

## 第二章 功能测试用例

### 2.1 用户认证模块

#### 2.1.1 用户注册

| 用例ID | TC-AUTH-001 | 模块 | 用户认证 |
|--------|-------------|------|----------|
| 用例名称 | 正常用户注册 | 优先级 | P0 |

**测试步骤**：
1. 访问注册页面
2. 填写邮箱：test@example.com
3. 填写密码：Test1234
4. 确认密码：Test1234
5. 点击注册按钮

**预期结果**：
- 系统创建用户成功
- 返回Token
- 发送验证邮件到test@example.com

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-AUTH-002 | 模块 | 用户认证 |
|--------|-------------|------|----------|
| 用例名称 | 重复邮箱注册 | 优先级 | P0 |

**测试步骤**：
1. 使用已注册的邮箱test@example.com注册

**预期结果**：
- 系统返回错误："该邮箱已被注册"
- 不创建重复用户

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-AUTH-003 | 模块 | 用户认证 |
|--------|-------------|------|----------|
| 用例名称 | 弱密码注册 | 优先级 | P1 |

**测试步骤**：
1. 输入密码：123
2. 点击注册

**预期结果**：
- 系统返回错误："密码强度不足"
- 提示密码要求：至少8位，包含大小写字母和数字

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

#### 2.1.2 用户登录

| 用例ID | TC-AUTH-004 | 模块 | 用户认证 |
|--------|-------------|------|----------|
| 用例名称 | 正常登录 | 优先级 | P0 |

**测试步骤**：
1. 输入邮箱：test@example.com
2. 输入密码：Test1234
3. 点击登录

**预期结果**：
- 登录成功
- 返回access_token和refresh_token
- 跳转至用户首页

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-AUTH-005 | 模块 | 用户认证 |
|--------|-------------|------|----------|
| 用例名称 | 错误密码登录 | 优先级 | P0 |

**测试步骤**：
1. 输入邮箱：test@example.com
2. 输入密码：WrongPassword
3. 连续尝试5次

**预期结果**：
- 第5次失败后账号锁定30分钟
- 返回错误："账号已被锁定"

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

#### 2.1.3 Token管理

| 用例ID | TC-AUTH-006 | 模块 | Token管理 |
|--------|-------------|------|----------|
| 用例名称 | Token刷新 | 优先级 | P0 |

**测试步骤**：
1. 使用有效的refresh_token
2. 调用刷新接口

**预期结果**：
- 返回新的access_token
- 原refresh_token可继续使用或失效（根据配置）

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-AUTH-007 | 模块 | Token管理 |
|--------|-------------|------|----------|
| 用例名称 | 过期Token访问 | 优先级 | P0 |

**测试步骤**：
1. 使用过期的access_token
2. 调用受保护的API

**预期结果**：
- 返回401 Unauthorized
- 提示Token已过期

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

### 2.2 API Key管理模块

#### 2.2.1 Key创建

| 用例ID | TC-KEY-001 | 模块 | API Key管理 |
|--------|-------------|------|-------------|
| 用例名称 | 创建API Key | 优先级 | P0 |

**前置条件**：用户已登录

**测试步骤**：
1. 进入API Key管理页面
2. 点击"创建Key"按钮
3. 填写Key名称："测试Key"
4. 提交创建

**预期结果**：
- 成功创建Key
- 显示Key和Secret
- 提示Secret仅显示一次，请妥善保存
- Key格式：APIK_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-KEY-002 | 模块 | API Key管理 |
|--------|-------------|------|-------------|
| 用例名称 | Key数量限制 | 优先级 | P1 |

**前置条件**：用户已有10个Key

**测试步骤**：
1. 尝试创建第11个Key

**预期结果**：
- 系统返回错误："每个租户最多10个Key"
- 不创建新Key

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

#### 2.2.2 Key验证

| 用例ID | TC-KEY-003 | 模块 | API Key验证 |
|--------|-------------|------|-------------|
| 用例名称 | 有效Key调用API | 优先级 | P0 |

**测试步骤**：
1. 使用有效Key发起API请求
2. Header添加：X-API-Key: {valid_key}

**预期结果**：
- 请求成功
- 返回业务数据
- 调用日志记录成功

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-KEY-004 | 模块 | API Key验证 |
|--------|-------------|------|-------------|
| 用例名称 | 无效Key调用API | 优先级 | P0 |

**测试步骤**：
1. 使用无效Key发起API请求

**预期结果**：
- 返回403 Forbidden
- 错误信息："无效的API Key"

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-KEY-005 | 模块 | API Key验证 |
|--------|-------------|------|-------------|
| 用例名称 | 已禁用Key调用API | 优先级 | P0 |

**前置条件**：Key状态为已禁用

**测试步骤**：
1. 使用已禁用的Key发起API请求

**预期结果**：
- 返回403 Forbidden
- 错误信息："API Key已被禁用"

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

### 2.3 业务API模块

#### 2.3.1 核心接口

| 用例ID | TC-API-001 | 模块 | 业务API |
|--------|-------------|------|---------|
| 用例名称 | 获取业务数据 | 优先级 | P0 |

**测试步骤**：
1. 使用有效Token
2. 调用GET /api/v1/business/data

**预期结果**：
- 返回200状态码
- 返回JSON格式的业务数据

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-API-002 | 模块 | 业务API |
|--------|-------------|------|---------|
| 用例名称 | 参数校验失败 | 优先级 | P0 |

**测试步骤**：
1. 调用POST /api/v1/business/action
2. 传入无效参数

**预期结果**：
- 返回400 Bad Request
- 返回详细的参数校验错误信息

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-API-003 | 模块 | 业务API |
|--------|-------------|------|---------|
| 用例名称 | Swagger文档访问 | 优先级 | P0 |

**测试步骤**：
1. 访问 http://api.example.com/swagger-ui.html

**预期结果**：
- 返回Swagger UI页面
- 可查看所有API接口
- 可在线测试接口

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

### 2.4 限流模块

| 用例ID | TC-RATE-001 | 模块 | 限流 |
|--------|-------------|------|------|
| 用例名称 | 正常频率请求 | 优先级 | P0 |

**测试步骤**：
1. 每秒发送10个请求
2. 持续发送100个请求

**预期结果**：
- 所有请求成功
- 返回正常业务数据

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-RATE-002 | 模块 | 限流 |
|--------|-------------|------|------|
| 用例名称 | 超QPS限制 | 优先级 | P0 |

**测试步骤**：
1. 在1秒内发送200个请求（超过100 QPS限制）

**预期结果**：
- 部分请求返回429 Too Many Requests
- 返回Retry-After头
- 限流生效

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

### 2.5 管理后台模块

| 用例ID | TC-ADMIN-001 | 模块 | 管理后台 |
|--------|-------------|------|----------|
| 用例名称 | 创建租户 | 优先级 | P0 |

**前置条件**：管理员已登录

**测试步骤**：
1. 进入租户管理页面
2. 点击"创建租户"
3. 填写租户信息
4. 提交

**预期结果**：
- 租户创建成功
- 租户列表显示新租户
- 可对租户进行后续操作

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-ADMIN-002 | 模块 | 管理后台 |
|--------|-------------|------|----------|
| 用例名称 | 查看调用统计 | 优先级 | P1 |

**前置条件**：存在调用数据

**测试步骤**：
1. 进入统计面板
2. 选择时间范围

**预期结果**：
- 显示调用量图表
- 数据准确
- 可导出Excel报表

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

### 2.6 部署测试模块

| 用例ID | TC-DEPLOY-001 | 模块 | 部署 |
|--------|---------------|------|------|
| 用例名称 | 一键部署 | 优先级 | P0 |

**测试环境**：全新服务器

**测试步骤**：
1. 在新服务器执行：git clone {repo}
2. 配置环境变量
3. 执行：docker-compose up -d
4. 等待服务启动

**预期结果**：
- 所有容器成功启动
- 服务可访问
- 部署时间 <= 30分钟

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-DEPLOY-002 | 模块 | 部署 |
|--------|---------------|------|------|
| 用例名称 | 数据备份 | 优先级 | P1 |

**测试步骤**：
1. 执行备份脚本
2. 检查备份文件

**预期结果**：
- 备份成功
- 备份文件包含数据库和上传文件
- 备份文件可验证

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

| 用例ID | TC-DEPLOY-003 | 模块 | 部署 |
|--------|---------------|------|------|
| 用例名称 | 数据恢复 | 优先级 | P1 |

**前置条件**：存在备份文件

**测试步骤**：
1. 删除部分数据
2. 执行恢复脚本
3. 验证数据恢复

**预期结果**：
- 恢复成功
- 数据完整恢复
- 服务正常运行

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

## 第三章 性能测试用例

### 3.1 性能测试场景

| 场景ID | 场景名称 | 并发数 | 持续时间 | 目标TPS | 目标响应时间 |
|--------|----------|--------|----------|---------|--------------|
| PT-001 | 基础性能测试 | 50 | 5分钟 | 500 | P95<200ms |
| PT-002 | 峰值压力测试 | 200 | 3分钟 | 2000 | P95<500ms |
| PT-003 | 限流场景测试 | 300 | 1分钟 | 限流生效 | - |
| PT-004 | 缓存命中率测试 | 100 | 5分钟 | - | 命中率>=80% |

### 3.2 性能测试用例详情

#### PT-001 基础性能测试

**测试目的**：验证系统在正常负载下的性能表现

**测试工具**：JMeter/Locust

**测试脚本**：
```
场景：API性能测试
线程组：
  - 线程数：50
  -  ramp-up时间：30秒
  -  持续时间：5分钟
  -  循环次数：无限

请求配置：
  - 请求1：GET /api/v1/business/data
  - Think Time：1秒
  - 响应断言：状态码200
```

**监控指标**：
- TPS（每秒事务数）
- 响应时间分布（P50、P90、P95、P99）
- 错误率
- CPU使用率
- 内存使用率
- 网络带宽

**验收标准**：
| 指标 | 标准值 | 测试结果 | 判定 |
|------|--------|----------|------|
| 平均响应时间 | < 100ms | | |
| P95响应时间 | < 200ms | | |
| P99响应时间 | < 500ms | | |
| 错误率 | < 0.1% | | |
| TPS | >= 500 | | |

---

#### PT-002 峰值压力测试

**测试目的**：验证系统在峰值负载下的稳定性

**测试脚本**：
```
场景：压力测试
线程组：
  - 初始线程数：50
  - 每30秒增加：50
  - 最大线程数：200
  - 达到最大后持续：3分钟
```

**验收标准**：
| 指标 | 标准值 | 测试结果 | 判定 |
|------|--------|----------|------|
| 最大TPS | >= 1500 | | |
| 响应时间增长 | 可接受 | | |
| 无系统崩溃 | 通过 | | |
| 无数据丢失 | 通过 | | |

---

## 第四章 安全测试用例

### 4.1 安全测试场景

| 场景ID | 场景名称 | 测试类型 | 威胁等级 |
|--------|----------|----------|----------|
| SEC-001 | SQL注入测试 | 渗透测试 | 高 |
| SEC-002 | XSS攻击测试 | 渗透测试 | 高 |
| SEC-003 | 暴力破解测试 | 渗透测试 | 高 |
| SEC-004 | 越权访问测试 | 渗透测试 | 高 |
| SEC-005 | 数据加密测试 | 代码审查 | 中 |

### 4.2 安全测试用例详情

#### SEC-001 SQL注入测试

| 用例ID | SEC-001 | 模块 | 安全测试 |
|--------|---------|------|----------|
| 用例名称 | SQL注入攻击防护 | 威胁等级 | 高 |

**测试步骤**：
1. 在API参数中输入：`' OR '1'='1`
2. 在API参数中输入：`'; DROP TABLE users; --`
3. 在API参数中输入：`UNION SELECT * FROM users`

**预期结果**：
- 所有SQL注入尝试被拦截
- 返回400或500错误
- 数据库未被注入
- 系统日志记录攻击行为

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

#### SEC-002 XSS攻击测试

| 用例ID | SEC-002 | 模块 | 安全测试 |
|--------|---------|------|----------|
| 用例名称 | XSS攻击防护 | 威胁等级 | 高 |

**测试步骤**：
1. 在参数中输入：`<script>alert('XSS')</script>`
2. 在参数中输入：`<img src=x onerror=alert(1)>`
3. 在参数中输入：`javascript:alert('XSS')`

**预期结果**：
- XSS脚本被转义或过滤
- 不会在前端执行
- 返回400错误或转义后的内容

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

#### SEC-003 暴力破解测试

| 用例ID | SEC-003 | 模块 | 安全测试 |
|--------|---------|------|----------|
| 用例名称 | 防暴力破解 | 威胁等级 | 高 |

**测试步骤**：
1. 连续使用错误密码登录同一账号5次
2. 等待1分钟后再尝试1次
3. 再连续错误4次

**预期结果**：
- 第5次失败后账号锁定30分钟
- 锁定期间无法登录
- 系统发送安全通知

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

#### SEC-004 越权访问测试

| 用例ID | SEC-004 | 模块 | 安全测试 |
|--------|---------|------|----------|
| 用例名称 | 租户数据隔离 | 威胁等级 | 高 |

**测试步骤**：
1. 租户A登录，获取Token
2. 尝试访问租户B的数据
3. 尝试修改租户B的资源

**预期结果**：
- 无法访问租户B的数据
- 无法修改租户B的资源
- 返回403 Forbidden

**测试结果**：□ 通过 □ 失败 □ 阻塞

---

## 第五章 测试结果汇总

### 5.1 测试执行统计

| 类别 | 用例总数 | 通过数 | 失败数 | 阻塞数 | 通过率 |
|------|----------|--------|--------|--------|--------|
| 功能测试 | 30 | | | | |
| 性能测试 | 4 | | | | |
| 安全测试 | 5 | | | | |
| 部署测试 | 3 | | | | |
| **总计** | **42** | | | | |

### 5.2 缺陷统计

| 严重程度 | 数量 | 已修复 | 待修复 |
|----------|------|--------|--------|
| 严重（Critical） | | | |
| 高（High） | | | |
| 中（Medium） | | | |
| 低（Low） | | | |
| **总计** | | | |

### 5.3 测试覆盖率

| 模块 | 代码行数 | 覆盖行数 | 覆盖率 |
|------|----------|----------|--------|
| 认证模块 | | | |
| API Key模块 | | | |
| 业务API模块 | | | |
| 缓存模块 | | | |
| 存储模块 | | | |
| **总计** | | | >= 70% |

---

## 第六章 测试结论

### 6.1 测试总结

| 测试类型 | 结论 | 说明 |
|----------|------|------|
| 功能测试 | | |
| 性能测试 | | |
| 安全测试 | | |
| 部署测试 | | |

### 6.2 系统评级

| 评级维度 | 评级 | 说明 |
|----------|------|------|
| 功能完整性 | | |
| 性能达标情况 | | |
| 安全性合规性 | | |
| 部署便利性 | | |
| 文档完整性 | | |
| **综合评级** | | |

### 6.3 测试结论

**□ 通过验收**：所有测试通过，满足验收标准，可以交付。

**□ 有条件通过**：存在少量低优先级缺陷，不影响正常使用，建议交付后修复。

**□ 不通过验收**：存在严重缺陷，需要修复后重新测试。

---

## 第七章 附录

### 7.1 测试环境配置

```
服务器配置：
- CPU: 4核
- 内存: 8GB
- 磁盘: 100GB SSD
- 操作系统: Ubuntu 22.04

软件版本：
- Docker: 24.0
- Docker Compose: 2.20
- PostgreSQL: 14
- Redis: 7
- MinIO: 最新稳定版
- Nginx: 1.25
```

### 7.2 测试工具清单

| 工具名称 | 用途 | 版本 |
|----------|------|------|
| Postman | API测试 | 最新版 |
| JMeter | 性能测试 | 5.6 |
| SQLMap | 安全测试 | 1.7 |
| Burp Suite | 渗透测试 | 专业版 |
| Locust | 负载测试 | 最新版 |

---

**测试人员签字**：

| 姓名 | 角色 | 签字 | 日期 |
|------|------|------|------|
| | 测试负责人 | | |
| | 测试工程师 | | |

**评审人员签字**：

| 姓名 | 角色 | 签字 | 日期 |
|------|------|------|------|
| | 技术负责人 | | |
| | 项目经理 | | |

---

**文档结束**
